diff --git a/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/DashboardSidebar.tsx b/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/DashboardSidebar.tsx
index 0a67f7f..32c3856 100644
--- a/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/DashboardSidebar.tsx
+++ b/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/DashboardSidebar.tsx
@@ -51,6 +51,7 @@ interface SortableProjectWrapperProps {
 	workspaceShortcutLabels: Map<string, string>;
 	onWorkspaceHover: (workspaceId: string) => void | Promise<void>;
 	onToggleCollapse: (projectId: string) => void;
+	onMenuOpenChange: (open: boolean) => void;
 }
 
 const SortableProjectWrapper = memo(function SortableProjectWrapper({
@@ -60,6 +61,7 @@ const SortableProjectWrapper = memo(function SortableProjectWrapper({
 	workspaceShortcutLabels,
 	onWorkspaceHover,
 	onToggleCollapse,
+	onMenuOpenChange,
 }: SortableProjectWrapperProps) {
 	const {
 		attributes,
@@ -86,6 +88,7 @@ const SortableProjectWrapper = memo(function SortableProjectWrapper({
 				workspaceShortcutLabels={workspaceShortcutLabels}
 				onWorkspaceHover={onWorkspaceHover}
 				onToggleCollapse={onToggleCollapse}
+				onMenuOpenChange={onMenuOpenChange}
 				dragHandleListeners={listeners}
 				dragHandleAttributes={attributes}
 			/>
@@ -120,21 +123,65 @@ export function DashboardSidebar({
 	const [activeProject, setActiveProject] =
 		useState<DashboardSidebarProject | null>(null);
 
-	// Local project order — syncs from groups, updated on drag end
+	// (BB active-first sort) The sidebar is "busy" while the user is dragging a
+	// project or hovering the list. We must NOT re-order rows under the cursor,
+	// so the visible order is frozen while busy and committed when idle.
+	const [isSidebarHovered, setIsSidebarHovered] = useState(false);
+	// A project context menu is open (right-click). Tracked because moving the
+	// pointer onto the portalled menu fires the list's onPointerLeave, which would
+	// otherwise unfreeze and reorder rows behind the open menu before the user
+	// picks an item.
+	const [isProjectMenuOpen, setIsProjectMenuOpen] = useState(false);
+	const isSidebarBusy =
+		activeProject != null || isSidebarHovered || isProjectMenuOpen;
+
+	// `groups` arrives ALREADY tier-sorted (pinned -> active -> idle, recency)
+	// from useDashboardSidebarData. `projectOrder` is the committed visible order.
 	const [projectOrder, setProjectOrder] = useState(() =>
 		groups.map((p) => p.id),
 	);
+	const groupsOrder = useMemo(() => groups.map((p) => p.id), [groups]);
+	const groupsOrderKey = groupsOrder.join("\n");
+
+	// Commit the tier-sorted order ONLY when the sidebar is idle (not dragging,
+	// hovering, or showing a project context menu), so background recency/pin
+	// reordering never moves a row under the cursor. We deliberately do NOT flush
+	// reorders while busy — not even for pin/add/remove — because committing the
+	// full order would also release any queued recency moves under the cursor.
+	// Membership changes (a new or removed project) are instead reflected at render
+	// by `orderedGroups` below, and pin/recency reorders apply on the next idle
+	// commit (pointer-leave / drop / menu-close). Fires on mount/launch too, since
+	// the sidebar starts idle.
+	// biome-ignore lint/correctness/useExhaustiveDependencies: re-keyed on groupsOrderKey
 	useEffect(() => {
-		setProjectOrder(groups.map((p) => p.id));
-	}, [groups]);
+		if (isSidebarBusy) return;
+		setProjectOrder(groupsOrder);
+	}, [groupsOrderKey, isSidebarBusy]);
 
 	const orderedGroups = useMemo(() => {
 		const byId = new Map(groups.map((g) => [g.id, g]));
-		return projectOrder
-			.map((id) => byId.get(id))
-			.filter((g): g is DashboardSidebarProject => g != null);
+		const seen = new Set<string>();
+		const result: DashboardSidebarProject[] = [];
+		for (const id of projectOrder) {
+			const project = byId.get(id);
+			if (project) {
+				result.push(project);
+				seen.add(id);
+			}
+		}
+		// Append any groups missing from the frozen order (e.g. a project created
+		// while the list was hovered) so it is never hidden.
+		for (const project of groups) {
+			if (!seen.has(project.id)) result.push(project);
+		}
+		return result;
 	}, [groups, projectOrder]);
 
+	const orderedGroupIds = useMemo(
+		() => orderedGroups.map((project) => project.id),
+		[orderedGroups],
+	);
+
 	const workspaceShortcutLabels = useDashboardSidebarShortcuts(orderedGroups);
 
 	const activeV2Project = useMemo(() => {
@@ -168,17 +215,20 @@ export function DashboardSidebar({
 	const handleDragEnd = useCallback(
 		({ active, over }: DragEndEvent) => {
 			if (over && active.id !== over.id) {
-				const oldIndex = projectOrder.indexOf(String(active.id));
-				const newIndex = projectOrder.indexOf(String(over.id));
+				// Base the move on the RENDERED order (orderedGroupIds = frozen order
+				// plus any ids appended while busy), not the raw frozen projectOrder,
+				// so a project that appeared while hovering is still draggable.
+				const oldIndex = orderedGroupIds.indexOf(String(active.id));
+				const newIndex = orderedGroupIds.indexOf(String(over.id));
 				if (oldIndex !== -1 && newIndex !== -1) {
-					const reordered = arrayMove(projectOrder, oldIndex, newIndex);
+					const reordered = arrayMove(orderedGroupIds, oldIndex, newIndex);
 					setProjectOrder(reordered);
 					reorderProjects(reordered);
 				}
 			}
 			setActiveProject(null);
 		},
-		[projectOrder, reorderProjects],
+		[orderedGroupIds, reorderProjects],
 	);
 
 	return (
@@ -188,7 +238,12 @@ export function DashboardSidebar({
 					<div className="flex h-full flex-col border-r border-border bg-muted/45 dark:bg-muted/35">
 						<DashboardSidebarHeader isCollapsed={isCollapsed} />
 
-						<div className="flex-1 overflow-y-auto hide-scrollbar">
+						{/** biome-ignore lint/a11y/noStaticElementInteractions: pointer enter/leave only freeze/unfreeze the active-first re-sort; no semantic interaction. */}
+						<div
+							className="flex-1 overflow-y-auto hide-scrollbar"
+							onPointerEnter={() => setIsSidebarHovered(true)}
+							onPointerLeave={() => setIsSidebarHovered(false)}
+						>
 							<DndContext
 								sensors={sensors}
 								collisionDetection={closestCenter}
@@ -203,7 +258,7 @@ export function DashboardSidebar({
 								onDragCancel={() => setActiveProject(null)}
 							>
 								<SortableContext
-									items={projectOrder}
+									items={orderedGroupIds}
 									strategy={verticalListSortingStrategy}
 								>
 									{orderedGroups.map((project) => (
@@ -215,6 +270,7 @@ export function DashboardSidebar({
 											workspaceShortcutLabels={workspaceShortcutLabels}
 											onWorkspaceHover={refreshWorkspacePullRequest}
 											onToggleCollapse={toggleProjectCollapsed}
+											onMenuOpenChange={setIsProjectMenuOpen}
 										/>
 									))}
 								</SortableContext>
diff --git a/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/components/DashboardSidebarProjectSection/DashboardSidebarProjectSection.tsx b/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/components/DashboardSidebarProjectSection/DashboardSidebarProjectSection.tsx
index 2f50737..b074b8b 100644
--- a/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/components/DashboardSidebarProjectSection/DashboardSidebarProjectSection.tsx
+++ b/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/components/DashboardSidebarProjectSection/DashboardSidebarProjectSection.tsx
@@ -22,6 +22,9 @@ interface DashboardSidebarProjectSectionProps {
 	workspaceShortcutLabels: Map<string, string>;
 	onWorkspaceHover: (workspaceId: string) => void | Promise<void>;
 	onToggleCollapse: (projectId: string) => void;
+	/** (BB) Notifies the sidebar when this project's context menu opens/closes so
+	 * the active-first re-sort stays frozen while the menu is up. */
+	onMenuOpenChange?: (open: boolean) => void;
 	dragHandleListeners?: DraggableSyntheticListeners;
 	dragHandleAttributes?: DraggableAttributes;
 }
@@ -33,6 +36,7 @@ export function DashboardSidebarProjectSection({
 	workspaceShortcutLabels,
 	onWorkspaceHover,
 	onToggleCollapse,
+	onMenuOpenChange,
 	dragHandleListeners,
 	dragHandleAttributes,
 }: DashboardSidebarProjectSectionProps) {
@@ -63,6 +67,7 @@ export function DashboardSidebarProjectSection({
 	const {
 		toggleProjectSectionFlag,
 		setProjectSectionFlag,
+		toggleProjectPinned,
 		unsnoozeAllInProject,
 		unarchiveWorkspaces,
 	} = useDashboardSidebarState();
@@ -103,6 +108,9 @@ export function DashboardSidebarProjectSection({
 				onOpenSettings={handleOpenSettings}
 				onRemoveFromSidebar={confirmRemoveFromSidebar}
 				onRename={startRename}
+				isPinned={project.isPinned}
+				onTogglePin={() => toggleProjectPinned(project.id)}
+				onOpenChange={onMenuOpenChange}
 			>
 				<div className={cn("border-b border-border last:border-b-0")}>
 					<DashboardSidebarCollapsedProjectContent
@@ -128,6 +136,9 @@ export function DashboardSidebarProjectSection({
 				onOpenSettings={handleOpenSettings}
 				onRemoveFromSidebar={confirmRemoveFromSidebar}
 				onRename={startRename}
+					isPinned={project.isPinned}
+					onTogglePin={() => toggleProjectPinned(project.id)}
+					onOpenChange={onMenuOpenChange}
 					showSnoozed={project.showSnoozed}
 					showArchived={project.showArchived}
 					onToggleSnoozed={() =>
@@ -141,6 +152,7 @@ export function DashboardSidebarProjectSection({
 					projectName={project.name}
 					iconUrl={project.iconUrl}
 					totalWorkspaceCount={totalWorkspaceCount}
+					isPinned={project.isPinned}
 					isCollapsed={project.isCollapsed}
 					isRenaming={isRenaming}
 					renameValue={renameValue}
diff --git a/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/components/DashboardSidebarProjectSection/components/DashboardSidebarProjectContextMenu/DashboardSidebarProjectContextMenu.tsx b/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/components/DashboardSidebarProjectSection/components/DashboardSidebarProjectContextMenu/DashboardSidebarProjectContextMenu.tsx
index f79d755..6758300 100644
--- a/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/components/DashboardSidebarProjectSection/components/DashboardSidebarProjectContextMenu/DashboardSidebarProjectContextMenu.tsx
+++ b/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/components/DashboardSidebarProjectSection/components/DashboardSidebarProjectContextMenu/DashboardSidebarProjectContextMenu.tsx
@@ -11,6 +11,8 @@ import {
 	LuFolderOpen,
 	LuFolderPlus,
 	LuPencil,
+	LuPin,
+	LuPinOff,
 	LuSettings,
 	LuX,
 } from "react-icons/lu";
@@ -20,6 +22,12 @@ interface DashboardSidebarProjectContextMenuProps {
 	 * renders no sections — so the menu items are hidden there too. */
 	showSnoozed?: boolean;
 	showArchived?: boolean;
+	/** (BB) Pin-to-top state + toggle. Omitted => the Pin item is hidden. */
+	isPinned?: boolean;
+	onTogglePin?: () => void;
+	/** (BB) Forwarded to the Radix ContextMenu so the sidebar can freeze its
+	 * active-first re-sort while this menu is open. */
+	onOpenChange?: (open: boolean) => void;
 	onCreateSection: () => void;
 	onOpenInFinder: () => void;
 	onOpenSettings: () => void;
@@ -33,6 +41,9 @@ interface DashboardSidebarProjectContextMenuProps {
 export function DashboardSidebarProjectContextMenu({
 	showSnoozed,
 	showArchived,
+	isPinned,
+	onTogglePin,
+	onOpenChange,
 	onCreateSection,
 	onOpenInFinder,
 	onOpenSettings,
@@ -43,13 +54,28 @@ export function DashboardSidebarProjectContextMenu({
 	children,
 }: DashboardSidebarProjectContextMenuProps) {
 	return (
-		<ContextMenu>
+		<ContextMenu onOpenChange={onOpenChange}>
 			<ContextMenuTrigger asChild>{children}</ContextMenuTrigger>
 			<ContextMenuContent onCloseAutoFocus={(event) => event.preventDefault()}>
 				<ContextMenuItem onSelect={onRename}>
 					<LuPencil className="size-4 mr-2" />
 					Rename
 				</ContextMenuItem>
+				{onTogglePin && (
+					<ContextMenuItem onSelect={onTogglePin}>
+						{isPinned ? (
+							<>
+								<LuPinOff className="size-4 mr-2" />
+								Unpin
+							</>
+						) : (
+							<>
+								<LuPin className="size-4 mr-2" />
+								Pin to top
+							</>
+						)}
+					</ContextMenuItem>
+				)}
 				<ContextMenuSeparator />
 				<ContextMenuItem onSelect={onOpenInFinder}>
 					<LuFolderOpen className="size-4 mr-2" />
diff --git a/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/components/DashboardSidebarProjectSection/components/DashboardSidebarProjectRow/DashboardSidebarProjectRow.tsx b/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/components/DashboardSidebarProjectSection/components/DashboardSidebarProjectRow/DashboardSidebarProjectRow.tsx
index 6a3339d..ddd5c25 100644
--- a/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/components/DashboardSidebarProjectSection/components/DashboardSidebarProjectRow/DashboardSidebarProjectRow.tsx
+++ b/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/components/DashboardSidebarProjectSection/components/DashboardSidebarProjectRow/DashboardSidebarProjectRow.tsx
@@ -2,6 +2,7 @@ import { Tooltip, TooltipContent, TooltipTrigger } from "@superset/ui/tooltip";
 import { cn } from "@superset/ui/utils";
 import { type ComponentPropsWithoutRef, forwardRef } from "react";
 import { HiChevronRight, HiMiniPlus } from "react-icons/hi2";
+import { LuPin } from "react-icons/lu";
 import { ProjectThumbnail } from "renderer/routes/_authenticated/components/ProjectThumbnail";
 import { RenameInput } from "renderer/screens/main/components/WorkspaceSidebar/RenameInput";
 
@@ -10,6 +11,7 @@ interface DashboardSidebarProjectRowProps
 	projectName: string;
 	iconUrl: string | null;
 	totalWorkspaceCount: number;
+	isPinned: boolean;
 	isCollapsed: boolean;
 	isRenaming: boolean;
 	renameValue: string;
@@ -30,6 +32,7 @@ export const DashboardSidebarProjectRow = forwardRef<
 			projectName,
 			iconUrl,
 			totalWorkspaceCount,
+			isPinned,
 			isCollapsed,
 			isRenaming,
 			renameValue,
@@ -96,6 +99,13 @@ export const DashboardSidebarProjectRow = forwardRef<
 					)}
 				</div>
 
+				{!isRenaming && isPinned && (
+					<LuPin
+						className="mr-0.5 size-3 shrink-0 -rotate-45 text-muted-foreground/70"
+						aria-label="Pinned"
+					/>
+				)}
+
 				{!isRenaming && (
 					<div className="ml-1 flex size-6 shrink-0 items-center justify-center">
 						<Tooltip delayDuration={500}>
diff --git a/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/hooks/useDashboardSidebarData/useDashboardSidebarData.ts b/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/hooks/useDashboardSidebarData/useDashboardSidebarData.ts
index bc5f30d..d67dbc1 100644
--- a/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/hooks/useDashboardSidebarData/useDashboardSidebarData.ts
+++ b/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/hooks/useDashboardSidebarData/useDashboardSidebarData.ts
@@ -12,6 +12,7 @@ import {
 	getWorkspaceSidebarBucket,
 } from "renderer/routes/_authenticated/providers/CollectionsProvider/dashboardSidebarLocal";
 import { useLocalHostService } from "renderer/routes/_authenticated/providers/LocalHostServiceProvider";
+import { useV2NotificationStore } from "renderer/stores/v2-notifications";
 import { useWorkspaceTransactionsStore } from "renderer/stores/workspace-creates";
 import type {
 	DashboardSidebarProject,
@@ -19,6 +20,7 @@ import type {
 	DashboardSidebarSection,
 	DashboardSidebarWorkspace,
 } from "../../types";
+import { getProjectChildrenWorkspaces } from "../../utils/projectChildren";
 import {
 	derivePullRequestQueryTargets,
 	getDashboardSidebarPullRequestQueryKey,
@@ -184,6 +186,8 @@ export function useDashboardSidebarData() {
 						showArchived: sidebarProjects.showArchived,
 						snoozedCollapsed: sidebarProjects.snoozedCollapsed,
 						archivedCollapsed: sidebarProjects.archivedCollapsed,
+						isPinned: sidebarProjects.isPinned,
+						lastAgentActivityAt: sidebarProjects.lastAgentActivityAt,
 				})),
 		[collections],
 	);
@@ -200,6 +204,10 @@ export function useDashboardSidebarData() {
 				showArchived: project.showArchived ?? false,
 				snoozedCollapsed: project.snoozedCollapsed ?? false,
 				archivedCollapsed: project.archivedCollapsed ?? false,
+				// (BB) Same heal — v2SidebarProjects has no withReadHeal, so rows
+				// persisted before these fields existed read back undefined.
+				isPinned: project.isPinned ?? false,
+				lastAgentActivityAt: project.lastAgentActivityAt ?? null,
 			})),
 		[rawSidebarProjects],
 	);
@@ -461,6 +469,45 @@ export function useDashboardSidebarData() {
 		workspaceTransactionsById,
 	]);
 
+	// (BB active-first sort) Persist each project's most-recent agent-activity
+	// time (epoch ms) so the active/idle tiers can be ordered most-recent-first
+	// immediately at launch — the v2-notifications store is in-memory and resets
+	// every launch, so the sort signal must be persisted to survive a restart.
+	// Fold the per-source `occurredAt` to a per-project max and write to
+	// v2SidebarProjects ONLY when it strictly increases: a quiet sidebar does no
+	// writes, and after a write the recomputed max equals the stored value so the
+	// effect converges (its deps don't change from its own write).
+	const notificationSources = useV2NotificationStore((state) => state.sources);
+	const projectIdByWorkspaceId = useMemo(() => {
+		const map = new Map<string, string>();
+		for (const workspace of rawSidebarWorkspaces) {
+			map.set(workspace.id, workspace.projectId);
+		}
+		for (const workspace of rawLocalMainWorkspaces) {
+			map.set(workspace.id, workspace.projectId);
+		}
+		return map;
+	}, [rawSidebarWorkspaces, rawLocalMainWorkspaces]);
+	useEffect(() => {
+		const maxByProject = new Map<string, number>();
+		for (const entry of Object.values(notificationSources)) {
+			const projectId = projectIdByWorkspaceId.get(entry.workspaceId);
+			if (!projectId) continue;
+			const current = maxByProject.get(projectId) ?? 0;
+			if (entry.occurredAt > current) {
+				maxByProject.set(projectId, entry.occurredAt);
+			}
+		}
+		for (const [projectId, occurredAt] of maxByProject) {
+			const row = collections.v2SidebarProjects.get(projectId);
+			if (!row) continue;
+			if ((row.lastAgentActivityAt ?? 0) >= occurredAt) continue;
+			collections.v2SidebarProjects.update(projectId, (draft) => {
+				draft.lastAgentActivityAt = occurredAt;
+			});
+		}
+	}, [notificationSources, projectIdByWorkspaceId, collections]);
+
 	const visibleSidebarWorkspaces = useMemo(() => {
 		const sidebarProjectIds = new Set(
 			sidebarProjects.map((project) => project.id),
@@ -716,7 +763,7 @@ export function useDashboardSidebarData() {
 			);
 		}
 
-		return sidebarProjects.flatMap((project) => {
+		const builtProjects = sidebarProjects.flatMap((project) => {
 			const resolvedProject = projectsById.get(project.id);
 			if (!resolvedProject) return [];
 			const {
@@ -771,6 +818,36 @@ export function useDashboardSidebarData() {
 			);
 			return [sidebarProject];
 		});
+
+		// (BB active-first sort) Tier the top-level project lane:
+		//   0 = pinned (manual drag order among themselves),
+		//   1 = active (open-chats badge > 0), 2 = idle (badge === 0).
+		// Active/idle are ordered most-recent-agent-activity first, ties broken by
+		// manual order. `orderIndex` is each project's position in `sidebarProjects`
+		// (already tabOrder ASC), i.e. the manual drag order — so pinned ordering
+		// and the tiebreak reuse tabOrder without selecting it. Active/idle uses the
+		// SAME count the badge shows (getProjectChildrenWorkspaces(children).length)
+		// so the tier never disagrees with the displayed number.
+		const orderIndex = new Map(
+			sidebarProjects.map((project, index) => [project.id, index] as const),
+		);
+		const projectTier = (project: DashboardSidebarProject): number => {
+			if (project.isPinned) return 0;
+			return getProjectChildrenWorkspaces(project.children).length > 0 ? 1 : 2;
+		};
+		builtProjects.sort((left, right) => {
+			const leftTier = projectTier(left);
+			const rightTier = projectTier(right);
+			if (leftTier !== rightTier) return leftTier - rightTier;
+			const leftIndex = orderIndex.get(left.id) ?? 0;
+			const rightIndex = orderIndex.get(right.id) ?? 0;
+			if (leftTier === 0) return leftIndex - rightIndex;
+			const leftActivity = left.lastAgentActivityAt ?? 0;
+			const rightActivity = right.lastAgentActivityAt ?? 0;
+			if (rightActivity !== leftActivity) return rightActivity - leftActivity;
+			return leftIndex - rightIndex;
+		});
+		return builtProjects;
 	}, [
 		machineId,
 		nowMs,
diff --git a/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/types.ts b/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/types.ts
index 7e00685..bbef620 100644
--- a/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/types.ts
+++ b/apps/desktop/src/renderer/routes/_authenticated/_dashboard/components/DashboardSidebar/types.ts
@@ -99,4 +99,9 @@ export interface DashboardSidebarProject {
 	showArchived: boolean;
 	snoozedCollapsed: boolean;
 	archivedCollapsed: boolean;
+	// (BB active-first sort) Pinned projects sort to the very top (manual order
+	// among themselves); lastAgentActivityAt (epoch ms, max across the project's
+	// workspaces) orders the active/idle tiers most-recent-first.
+	isPinned: boolean;
+	lastAgentActivityAt: number | null;
 }
diff --git a/apps/desktop/src/renderer/routes/_authenticated/hooks/useDashboardSidebarState/useDashboardSidebarState.ts b/apps/desktop/src/renderer/routes/_authenticated/hooks/useDashboardSidebarState/useDashboardSidebarState.ts
index 4288061..bdcffa0 100644
--- a/apps/desktop/src/renderer/routes/_authenticated/hooks/useDashboardSidebarState/useDashboardSidebarState.ts
+++ b/apps/desktop/src/renderer/routes/_authenticated/hooks/useDashboardSidebarState/useDashboardSidebarState.ts
@@ -237,6 +237,20 @@ export function useDashboardSidebarState() {
 		[collections],
 	);
 
+	// (BB active-first sort) Pin / unpin a project to the top of the sidebar.
+	// `!draft.isPinned` flips correctly even for legacy rows that read back
+	// undefined (this collection has no read-heal): undefined -> pinned first.
+	const toggleProjectPinned = useCallback(
+		(projectId: string) => {
+			const existing = collections.v2SidebarProjects.get(projectId);
+			if (!existing) return;
+			collections.v2SidebarProjects.update(projectId, (draft) => {
+				draft.isPinned = !draft.isPinned;
+			});
+		},
+		[collections],
+	);
+
 	const reorderProjects = useCallback(
 		(projectIds: string[]) => {
 			projectIds.forEach((projectId, index) => {
@@ -654,6 +668,7 @@ export function useDashboardSidebarState() {
 		setSectionColor,
 		snoozeWorkspace,
 		toggleProjectCollapsed,
+		toggleProjectPinned,
 		toggleProjectSectionFlag,
 		toggleSectionCollapsed,
 		unarchiveWorkspace,
diff --git a/apps/desktop/src/renderer/routes/_authenticated/providers/CollectionsProvider/dashboardSidebarLocal/schema.ts b/apps/desktop/src/renderer/routes/_authenticated/providers/CollectionsProvider/dashboardSidebarLocal/schema.ts
index eac11ce..207fd32 100644
--- a/apps/desktop/src/renderer/routes/_authenticated/providers/CollectionsProvider/dashboardSidebarLocal/schema.ts
+++ b/apps/desktop/src/renderer/routes/_authenticated/providers/CollectionsProvider/dashboardSidebarLocal/schema.ts
@@ -21,6 +21,15 @@ export const dashboardSidebarProjectSchema = z.object({
 	showArchived: z.boolean().default(false),
 	snoozedCollapsed: z.boolean().default(false),
 	archivedCollapsed: z.boolean().default(false),
+	// (BB active-first sort) Pin a project to the top of the sidebar (above the
+	// active/idle tiers), and the last time any agent activity occurred in the
+	// project (epoch ms, max across its workspaces) used to order the
+	// active/idle tiers most-recent-first. Persisted so the order survives a
+	// restart. Local-only, like every other sidebar preference. NOTE: this
+	// collection has no withReadHeal, so the data hook must heal both fields
+	// (rows persisted before they existed read back undefined).
+	isPinned: z.boolean().default(false),
+	lastAgentActivityAt: z.number().nullable().default(null),
 });
 
 const paneWorkspaceStateSchema = z.custom<WorkspaceState<unknown>>();
