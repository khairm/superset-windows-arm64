<agent-message from="arch-design">
# Blueprint: stuck red dot (MUST-1) + broken manual "Clear Status" (MUST-2)

## Correction to the investigation report — MUST-1's mechanism is different

The report's cause ("POST times out under host saturation; add retry/queue") is wrong, which changes the fix. Evidence from this machine:

- Host `127.0.0.1:55331`: healthy until 03:00 today, then **zero** successful posts. Hourly: `02` = 786 ok / 0 err; `03` = 1 ok / 926 err; `04`-`12` = 0 ok / 3880 err. A cliff, not 32% loss.
- `netstat -ano`: **55331 is not listening.** Live host is `48056` (pid 21352).
- `~/.superset/host/30e16b5a-.../manifest.json` → `"endpoint":"http://127.0.0.1:48056"`, rewritten 03:01. One org, one manifest.
- 6 terminals still POST to the dead 55331. All 6 have `terminal_sessions` rows in the **live** host's `host.db`; 2 are `status=active`.

This is the known **frozen hook URL** bug — `C:\Users\khair\Code Projects\fork-superset-windows-arm64\plans\agent-hook-url-frozen-port.md`. The env var is baked into the PTY at creation; host-service restarted onto a new port; PTYs survive in the pty-daemon with the dead address.

That plan's fix shipped **only to the shell templates**. `C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\agent-setup\templates\notify-hook.template.sh:170-205` builds candidates from every `~/.superset/host/*/manifest.json`. The fork's Python hook, which owns every dot decision, was never ported: `C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\main\lib\agent-jsonl-watcher\pane-map-hook.ts:2430` reads the env var, `:2247-2253` posts to it, done.

**Why the symptom is specifically a stuck red.** `~/.claude/settings.json` routes `PermissionRequest` → `notify.sh` (has failover) and every other event → `superset-notify.py` (does not). The red-**setter** still reaches the live host; the red-**clearer** (`pane-map-hook.ts:2152-2159`) is lost. Confirmed in `~/.superset/agent-dot-render.log`: `d3ce7c25` red 10:36:33, answer 10:55:38 logged `post-error`, cleared 11:13:02 (37 min). `72d85ab1` red 10:39:05, still red past 11:14. Control terminal `d603171b` on the live host flips `permission`↔`working` in seconds.

The report's "SubagentActive flood" is a red herring — the flooded host runs 19,459 posted / 8 errors.

## Clear-status trace: confirmed, with two refinements

Your trace is correct on every line reference I checked. Two refinements:

1. **`outstandingReadyAt` and the companion ready-alert are already handled today**, not missing. The hook-local `clearWorkspaceAttention` (`...\useDashboardSidebarWorkspaceItemActions.ts:62-79`) calls `markTerminalSeenAndReportRead` per binding (`...\V2NotificationController\lib\companionAlertSync.ts:390-412`), which bumps `terminalSeenAt`, reports read to the host, and clears `outstandingReadyAt` **on acceptance**. Green and its phone alert do come down. Only red/yellow/blue survive. Widening the store purge is still worth doing as belt-and-braces for the rejected-report case, but it is not the defect.
2. **`shellRunningTerminals` genuinely needs clearing, for plain shells only.** The agent-terminal gate at `store.ts:971-974` suppresses shell-blue on agent terminals, so a stuck agent dot is unaffected. But `selectV2WorkspaceDisplayStatus:1183-1190` yields a status for open terminals with **no** source entry, so a plain shell's blue renders even after every source is purged.

Also confirmed: `clearV2TerminalRunStatus` (`store.ts:812-826`) has exactly two references — its own definition and the barrel re-export at `...\stores\v2-notifications\index.ts:2`. Dead, and its doc comment falsely claims interrupt and pane close route through it.

**Scope correction:** there is **one** Clear Status item, on sidebar workspace rows (`...\DashboardSidebarWorkspaceContextMenu.tsx:293-298`). No tab-level clear exists. The v1 `...\screens\main\...\WorkspaceListItem.tsx:223` drives the tabs store and is dead under the v2-only rule. The item *is* reachable when a red is stuck — its `hasStatus` gate reads `useV2WorkspaceDisplayStatus` (`store.ts:1196-1205`), the same store holding the latched red. The bug is the handler, not the gate.

Two re-assertion vectors survive the click, both from the surviving marker:
- `...\resyncAgentStatus.ts:678-685` re-applies `PermissionRequest` from `pendingPermission`, derived from the marker dir at `...\agent-status-snapshot.ts:127-167`. Forcing the binding to `Stop` cannot help — `pendingPermission` is deliberately independent of `lastEventType` (`agent-status-snapshot.ts:13-19`). The `pendingPermission === null` branch at `:660-677` re-applies only `preEntry.axes.permission`, so once the renderer clears locally it re-latches nothing; it is a hazard only while the local clear is missing.
- The hook's central guard: while the marker exists, untagged tool completions return `SubagentActive` (`pane-map-hook.ts:2214-2216`), which clears nothing (`statusTransitions.ts:64-74`). The red is self-perpetuating.

---

## 1. Recommended approach

### MUST-1 — port the manifest failover into the embedded Python hook

Env URL first (unchanged fast path), then each `~/.superset/host/*/manifest.json` endpoint, stopping at the first response not containing `"ignored": true`.

Alternatives and why each loses:
- **Queue + retry in the hook** (report's proposal): retrying a permanently dead port never delivers, and a background drain process is the `(HOOK-FORK-DIET)` fork cascade this fork banned.
- **Raise the 1.5 s timeout**: nothing is slow, the port is gone. Pure agent stall, zero delivery.
- **Reduce the SubagentActive flood**: the flooded host has a 0.04% error rate.
- **Marker-driven resync alone**: heals the dot but every other event from those terminals still vanishes.
- **Stable socket / port pinning**: rejected at `plans\agent-hook-url-frozen-port.md:90-91`; reasons hold.

Decisive advantage: `writeNotifyScriptIfChanged()` (`pane-map-hook.ts:2736`) rewrites the script every desktop start and Python re-reads it per fire, so **this heals the 6 already-orphaned live terminals on their next event** with no terminal restart.

`(HOOK-FORK-DIET)` risk: **none.** That footgun is `.sh` templates forking subprocesses under emulated msys2. This is one Python process doing stdlib file reads and in-process `urlopen`.

Cost: fast path unchanged. A dead env URL already costs 1.5 s (3 s when the companion retry fires, `:2623-2642`); drop the per-candidate timeout to **0.75 s** and the worst case halves while actually delivering.

### MUST-2 — make the marker the thing manual clear deletes, and give the renderer the axis clear it never performed

**Host half (authoritative).** New mutation `terminalAgents.dismissWorkspaceStatuses`: per terminal in scope, delete the `<terminalId>.askq` directory, force the binding to `Stop` via the existing `store.clearWorkspaceStatuses`, and settle any pending companion question via `markStale`.

**Renderer half.** `handleClearStatus` awaits that mutation, then calls the already-existing, uncalled `useV2NotificationStore.clearWorkspaceStatuses(workspaceId)` (`store.ts:648-690`) — your name-collision finding.

On sharing one path with the automatic answer-clear: **they cannot be one function**, because they differ on the only axis that matters — manual may drop a live red, automatic may not. What they share, and what removes the duplication that matters, is the **marker mutation and its path derivation**. The `.askq` path is built in three places today: the Python hook (`pane-map-hook.ts:602-629`), the Electron watcher (`agent-jsonl-watcher.ts:505-546`), and the host reader (`agent-status-snapshot.ts:127-138`). A fourth ad-hoc copy is exactly the parallel mechanism to avoid, so the new writer goes **in `agent-status-snapshot.ts`, sharing one path builder and one `SAFE_MARKER_SEGMENT` guard with the reader.** Full unification across Python/main/host needs a rewrite and is out of scope; I am not pretending otherwise.

Host-side rather than Electron main because the marker lives on the machine the agent runs on — for a relay or remote host, main's `homedir()` is the wrong disk. The host also already holds the binding and the companion store, so one round trip does all three.

`markStale` not `resolve`: `resolve` stamps answered-by provenance (`question-store.ts:757-761`) and nobody answered. `markStale` (`:1976-1991`) is terminal, non-provenance-bearing, and still routes through `settle()` → `onSettled` → `push.cancelPending` (`companion\index.ts:663-676`), so the phone notification retracts. Same retraction, honest record.

**Deliberately not deleted: the run-dir subagent markers.** They do not re-assert red on resync — resync replays `lastEventType`, forced to `Stop`. They only shape the *next* hook decision, and a running subagent re-asserting yellow at its next event is correct. Deleting them would corrupt live subagent tracking.

**Cross-window propagation by convergence, not broadcast.** Marker gone plus binding `Stop` means any other window's next resync converges to cleared. I considered broadcasting `Detached` (clears both axes at `statusTransitions.ts:51-62`, chime-exempt at `lifecycleEvents.ts:74-88`) — rejected, because `Detached` also means "session ended" (`map-event-type.ts:11`) and asserting it about a live agent is a lie told to fix a refresh delay.

---

## 2. Files to create/modify

### MUST-1

**`...\apps\desktop\src\main\lib\agent-jsonl-watcher\pane-map-hook.ts`** — embedded `NOTIFY_SCRIPT` (`:312`+):
- New `_hook_candidate_urls(env_url)` near `_post_hook` (`:2238`): `[env_url]` + `<endpoint>/trpc/notifications.hook` per manifest glob, deduped, order-preserving. Root = `os.environ.get("SUPERSET_HOME_DIR")` or `Path.home()/".superset"`. Per-manifest failure skips and logs, never raises.
- New `_deliver(candidates, body)`: POST each in order; return on the first body **not** containing `"ignored": true` / `"ignored":true`. Raise the last exception if all fail. Keying on *absence of `ignored:true`* rather than presence of `ignored:false` means an upstream body change degrades to "delivered to the first host that answered" instead of "probes forever". `notifications.ts:357` returns `ignored:false` on delivery; `:221-225`, `:275-279`, `:293-297` return `ignored:true` on drops.
- `_post_hook:2253`: timeout `1.5` → `0.75`.
- Call site `:2606-2649`: `_post_hook` → `_deliver`; log the delivering url in the `posted` record. The companion strip-and-retry at `:2623-2642` re-runs through `_deliver` with the same list.
- Total-failure path `:2643-2648`: also emit one `_decision_log` line naming every candidate tried. Reuses the always-on rotating audit channel (`:415`).
- **`_log` rotation** (`:401-412`): copy the rotate-at-1 MB idiom from `_decision_log:428-438`. Live file is **508,757,788 bytes**. MUST, not SHOULD — two lines, same function family, and an unbounded append inside the agent's hook window is itself latency.

**`...\agent-jsonl-watcher\pane-map-hook.test.ts`** — new `describe("superset-notify endpoint resolution")`. Harness at `:12-97` already gives real Python, throwaway `HOME`, real `Bun.serve` sink. Cases: dead env URL → delivered via manifest; live env URL → manifests unprobed; wrong-org sink returning `ignored:true` → probing continues; all dead → `post-error` + a `dot-decisions.log` line. The existing sink at `:43` returns no `ignored` field, which counts as delivered under the predicate, so existing tests pass unchanged — the wrong-org case must return `ignored:true` explicitly.

### MUST-2

**`...\packages\host-service\src\trpc\router\notifications\agent-status-snapshot.ts`** — extract the path builder inline at `:133-138` into `askqMarkerDir(terminalId)`, used by both the existing `hasPendingQuestionMarker` and a new `clearPendingQuestionMarkers(terminalId): Promise<boolean>` (`rm` recursive+force; `ENOENT` is success; any other error throws so the caller fails loud). Reader and writer share one derivation and one `SAFE_MARKER_SEGMENT` check (`:80`).

**`...\packages\host-service\src\trpc\router\terminal-agents\terminal-agents.ts`** — new `dismissWorkspaceStatuses` mutation beside `clearWorkspaceStatuses` (`:277-287`), input `{workspaceId, terminalId?}`. Per terminal, in order: `clearPendingQuestionMarkers` → existing `ctx.terminalAgentStore.clearWorkspaceStatuses` → companion dismissal. Returns `{cleared, markersRemoved, questionsDismissed}` so the renderer can surface partial failure. A marker delete that throws fails the whole mutation loud — a green UI over a surviving marker is the exact bug being fixed. `clearWorkspaceStatuses` stays **unchanged**: `useTerminalInterruptClear.ts:46` depends on its non-red-dropping semantics.

**`...\packages\host-service\src\trpc\router\notifications\companion-question-sink.ts`** — add `dismissByTerminal({hostTerminalId, reason})` to the `CompanionQuestionSink` interface (`:78-87`) and a `forwardCompanionDismissal(...)` mirroring `forwardCompanionCapture` (`:264-311`). No-op when no bridge is registered.

**`...\packages\host-service\src\companion\index.ts`** — implement `dismissByTerminal` on the sink registered at `:1161`: `store.byHostTerminal(id)` then `store.markStale(id, reason)`. Both already on `QuestionStore` (`question-store.ts:749`, `:777`); no question-store change.

**`...\useDashboardSidebarWorkspaceItemActions.ts`** — `handleClearStatus` (`:292-307`) calls `dismissWorkspaceStatuses`, and **on success** the store's `clearWorkspaceStatuses(workspaceId)` plus the existing `clearWorkspaceAttention()` and query invalidation. Host first, renderer second: a failed host call must not leave a cleared-looking UI the next resync un-clears. Existing `toast.error` at `:302-306` is the loud path.

**`...\apps\desktop\src\renderer\stores\v2-notifications\store.ts`** — `clearWorkspaceStatuses` (`:648-690`) additionally clears `shellRunningTerminals` (for plain-shell blue, per the refinement above) and `outstandingReadyAt` for the workspace's terminals. Add the `ndots` mutation log line the sibling `clearWorkspaceAttention` writes at `:704-713`.

**Dead code (same PR).** Delete `clearV2TerminalRunStatus` (`store.ts:812-826`) and its barrel re-export (`...\stores\v2-notifications\index.ts:2`). Zero callers; its doc comment at `:808-811` and `:827-829` falsely documents the interrupt and pane-close paths, which is actively misleading anyone debugging exactly this bug. Deleting beats fixing the comment: the correct behaviour it describes now lives in the dismiss path and in `useTerminalInterruptClear`.

**Tests**: `...\stores\v2-notifications\store.test.ts` (all maps cleared); a router test for the host mutation (marker dir gone, binding `Stop`, `markStale` called, throw on undeletable marker).

**`FEATURES.md`** — two feature rows plus markers, tab-separated per `:150-215`:
```
(HOOK-ENDPOINT-HEAL)	apps/desktop/src/main
(HOOK-ENDPOINT-HEAL)	apps/desktop/src/main/lib/agent-jsonl-watcher/pane-map-hook.test.ts
(MANUAL-DISMISS)	packages/host-service/src
(MANUAL-DISMISS)	apps/desktop/src/renderer
```
The test-pinned marker matters: without it a merge could take upstream's router and leave the dispatch untested — the reasoning already written at `notifications.ts:16-22` for `(COMPANION-CAPTURE-HOOK)`.

### SHOULD (separable, user's call)

**A. `resyncAgentStatus.ts:660-685`** — add the missing `else` for `pendingPermission === false`, clearing the permission axis via `applySourceAxes(..., {set:[], clear:["permission"]}, row.lastEventAt)`, shaped like `:664-676`. Today resync can only add red. Safety rests on the marker being the invariant; residual risk is a question opening between the host's `readdir` and the apply. **Verify the `preEntry.occurredAt > row.lastEventAt` fence at `:460-463` covers a mid-flight `PermissionRequest` before shipping.** If not, unsafe — do not ship.

**B. `HostNotificationSubscriber.tsx:135-172`** — A only helps on reconnect: `runResync` fires on socket open or workspace-set growth. On a stable socket it never runs, which is today's stuck case. Add a bounded 60 s re-run reusing `runResync` and its generation guard.

**C. `useTerminalInterruptClear.ts:40-58`** — same host-only defect you found: clears the binding, never the renderer's latched axes. Fix by clearing the terminal's own axes locally after the mutation resolves. **Do not point it at the new dismiss mutation** — an interrupt is not a dismissal and must keep no-drop-live-red semantics. Partly mitigated today: the watcher clears `_main` on a detected interrupt (`agent-jsonl-watcher.ts:1043-1044`).

### Explicitly do not do

**Leaked `.askq/_main` reap.** 24 leaked markers, oldest 2026-07-07. The no-reap policy is argued at `agent-status-snapshot.ts:104-122`: nothing host-side can prove a leak, and the mtime-vs-`startedAt` fence drops live reds in exactly the case it targets. Not an aggravator here — markers on both stuck terminals were correct throughout. MUST-2 gives the user a deliberate way to clear them, which is the right answer.

---

## 3. Data flow

**MUST-1, today** — user answers → `PostToolUse` → `_remove(askq/_main)` **[local, succeeds]** → `"Start"` **[correct]** → POST `55331` **[dead, 1.5 s]** → `post-error` into a 508 MB file. Binding stays `PermissionRequest`; renderer stays red.

**MUST-1, after** — … → `"Start"` → candidates `[55331/…, 48056/…]` → `_deliver` candidate 2 → `200 {"ignored":false}` → `notifications.ts:312` broadcast, `:320` binding persisted → `statusTransitions.ts:76-87` sets working, clears permission. Sub-second.

**MUST-2, after** — right-click → Clear Status → `dismissWorkspaceStatuses` → per terminal: `rm -rf <terminalId>.askq` → binding `Stop` → `byHostTerminal` + `markStale` → `settle()` → `onSettled` → `push.cancelPending` (phone retracts). On resolve: `clearWorkspaceStatuses(workspaceId)` drops sources, background, shell, manual, outstanding-ready; `clearWorkspaceAttention()` marks seen and reports read; bindings query invalidated. Next resync reads `pendingPermission: false` and re-asserts nothing; the hook's central guard sees an empty dir and stops holding `SubagentActive`.

Validation stays at the tRPC input boundary; ownership stays the DB read (`notifications.ts:256-261`); the `.askq` directory remains the single source of truth for red.

---

## 4. Build order

1. `_log` rotation. Verify: suite green; shipped script rotates at 1 MB.
2. `_hook_candidate_urls`, unused. Verify: candidate list over a temp `SUPERSET_HOME_DIR` with 0/1/2 manifests plus one malformed.
3. `_deliver` + 0.75 s, wired into the call site and companion retry. Verify: four new cases; full `bun test apps/desktop/src/main/lib/agent-jsonl-watcher/`.
4. Mutation check (matching `plans\agent-hook-url-frozen-port.md:116`): break the manifest glob, confirm exactly the failover tests fail, restore.
5. **Live heal, no rebuild needed**: start the desktop so `writeNotifyScriptIfChanged` lands the script, answer a question in `72d85ab1` or `d3ce7c25`, watch `~/.superset/agent-dot-render.log` for `permission → working` within a second and a `posted` line naming `48056`.
6. `clearPendingQuestionMarkers` + shared path builder. Verify: unit test incl. traversal-guard rejection.
7. `dismissByTerminal` on sink and bridge. Verify: `markStale` fires, push retraction observed.
8. `dismissWorkspaceStatuses`. Verify: marker gone, binding `Stop`, question stale, throw on undeletable marker.
9. Renderer store widening + `handleClearStatus` + dead-code deletion. Verify: store test, then CDP per `.agents/skills/cdp-verification/SKILL.md` — stick a red, click Clear Status, confirm the dot dies and **survives a forced resync**.
10. `FEATURES.md`; `node scripts/check-feature-markers.mjs`.
11. SHOULD A (gated on the step-7 fence check in §2), then B, then C.
12. `bun run typecheck`, `bun run lint`, `/simplify` before commit.

---

## 5. Risks and edge cases

**MUST-1**
- Multi-org double delivery impossible — a terminal id lives in one host's `terminal_sessions`; others answer `ignored:true`.
- A stale manifest from a crashed host adds 0.75 s. Manifests are rewritten per start (`apps\desktop\src\main\host-service\index.ts:126-138`), one per org. Do not add a cache for a two-entry loop.
- Predicate drift: if upstream ever returns a body containing literal `"ignored": true` for a delivered event, detection inverts. Covered by tests.
- Ordering: `_deliver` must stay strictly after the marker mutations in `_decide_event_type` (`:2500-2513`) so markers survive total delivery failure.
- `SUPERSET_HOME_DIR` honoured for the manifest glob, **not** for marker paths — the host reads markers at a hardcoded `homedir()` (`agent-status-snapshot.ts:133-138`), so unifying would silently break the resync. Comment it; do not "clean it up".

**MUST-2**
- **Dropping a live red is intended** and is the user's explicit decision. A genuinely blocked agent shows red again at its next hook event; between click and event the dot lies. This is why no automatic path may reuse it.
- Marker delete racing a *new* owner mid-dismiss destroys that question's red. Bounded to milliseconds, self-heals at the next event. Do not add locking.
- Partial failure across N terminals: fail loud on the first marker error and report which succeeded. Silent partial success is what produced this bug class.
- Remote/relay hosts: markers deleted on the **host's** disk — the reason this is not in Electron main.
- Other windows lag until their next resync: ≤60 s with SHOULD-B, until reconnect without it.
- No schema change, no migration, no new persisted key (`partialize` at `store.ts:745-753` already covers the maps).
- Three similarly-named functions (host `clearWorkspaceStatuses`, renderer `clearWorkspaceStatuses`, new `dismissWorkspaceStatuses`) is a real readability hazard — the name collision already caused this bug. Cross-reference all three in comments.

---

## 6. Out of scope

- Events lost between 03:00 and the fix: gone. MUST-1 heals forward only.
- Preventing the port from moving (pinning, stable socket) — rejected in the plan doc.
- The `SubagentActive` volume — not a fault at 0.04% error.
- The other three shell templates — already fixed.
- Adding a **tab-level** Clear Status. None exists; the mutation takes an optional `terminalId` so it is cheap later, but the UI is a scope change.
- The v1 `screens/main` context menu and tabs store.
- Full unification of the four `.askq` path implementations across Python, Electron main, and host.
- Surfacing "agent status unavailable" in the UI (`plans\agent-hook-url-frozen-port.md:138`).
- Leaked-marker reaping and the `SUPERSET_AGENT_HOOK_PORT` cosmetic gap.
</agent-message>

---

<agent-message from="arch-review-sol">
Review verdict: the frozen-port diagnosis is correct, but MUST-2 is not safe or authoritative as written.

BLOCKER 1: recursive marker deletion needs a path boundary the blueprint omits. The current reader rejects unsafe terminal ids before building the path at C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\trpc\router\notifications\agent-status-snapshot.ts:127-138. The existing mutation accepts any string at C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\trpc\router\terminal-agents\terminal-agents.ts:277-285. Extracting only the path builder and calling rm({recursive:true}) on terminalId? permits ../ traversal into directories ending .askq. Put SAFE_MARKER_SEGMENT validation inside askqMarkerDir/clearPendingQuestionMarkers, validate at the zod boundary, and derive the workspace-wide terminal set from store bindings rather than raw input. Add traversal/refusal tests.

BLOCKER 2: manual clear is not durable across resync. Host clear changes lastEventType to Stop but preserves lastEventAt at C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\terminal-agents\store.ts:179-199. Resync replays that Stop at C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\renderer\routes\_authenticated\components\V2NotificationController\lib\resyncAgentStatus.ts:473-480, which recreates review/green, and removes it only when terminalSeenAt already covers the event at :515-575. The proposed renderer clear never records a seen watermark. Therefore reconnect, another window, and SHOULD-B’s timer reintroduce a dot. The dismiss result should return affected terminal ids plus host lastEventAt, and the renderer should mark each seen through that generation, or the host must expose/broadcast a durable dismissal generation. A broad local delete alone cannot be called authoritative.

MAJOR: do not delete outstandingReadyAt in clearWorkspaceStatuses. This field is deliberately the only evidence needed to retract a ready alert after the green dot has already gone, per C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\renderer\stores\v2-notifications\store.ts:149-177. It is cleared only after the host accepts the seen report at C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\renderer\routes\_authenticated\components\V2NotificationController\lib\companionAlertSync.ts:381-410. Unconditional removal strands phone/watch ready notifications. Manual clear should use markTerminalSeenAndReportRead for affected terminals, then compare-and-clear on acknowledgement.

MAJOR: CompanionQuestionSink.dismissByTerminal -> questionStore.markStale retracts push through settle/cancelPending, but it does not publish question.stale or advance gseq. Existing hook resolution publishes a frame in createNotifyingCaptureSink at C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\companion\index.ts:1779-1806. Reconcile explicitly publishes settled questions because tree freshness depends on it at :970-997 and :1019-1041. The new dismiss path needs the same event publication with a manual-dismiss reason, or the paired UI can keep stale question state.

MAJOR: _deliver’s success predicate should parse the tRPC envelope and require result.data.json.ignored === false, not accept every response lacking the text 'ignored: true'. The host’s only accepted result is explicit ignored:false at C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\trpc\router\notifications\notifications.ts:312-357; ignored responses cover unknown terminal and wrong org at :263-297. Absence-based acceptance silently treats malformed/HTML/old responses as delivery, unlike notify.sh’s explicit ignored:false check at C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\agent-setup\templates\notify-hook.template.sh:203-209. Also, 'raise last exception' is undefined when every candidate returns ignored:true and none throws. Define a delivery error carrying the last response and add an all-ignored test.

MAJOR: SHOULD-B cannot call runResync periodically without changing its epoch/key guard. runResync returns immediately when syncedKeyRef equals the key at C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\renderer\routes\_authenticated\components\V2NotificationController\components\HostNotificationSubscriber\HostNotificationSubscriber.tsx:139-146. Current triggers are socket-open and workspace-key change at :265-289 and :428-430. A timer must have an explicit force path, reset the guard safely, prevent overlaps, and clean up. Also, if cross-window convergence is the chosen propagation mechanism, B is MUST, not optional. There is no terminal-store change listener wired to the event bus; clearWorkspaceStatuses only mutates/emit internally.

MAJOR: SHOULD-C must preserve permission red. The existing dead helper clears the entire source, including permission, at C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\renderer\stores\v2-notifications\store.ts:808-826. Interrupt intentionally uses the non-marker-deleting host mutation at C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\renderer\routes\_authenticated\_dashboard\v2-workspace\$workspaceId\hooks\usePaneRegistry\components\TerminalPane\hooks\useTerminalInterruptClear\useTerminalInterruptClear.ts:21-57. If C means clearing all renderer axes, it creates the live-red drop the plan says it avoids. Clear working/review/background only, or first prove the askq marker is gone.

MAJOR: the accepted marker-race statement is false for a newly raised question. The Python hook writes the askq owner before returning PermissionRequest at C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\main\lib\agent-jsonl-watcher\pane-map-hook.ts:2137-2141. rm can race that write, delete the new marker, and the blocked AskUserQuestion emits no next hook event. User dismissal may intentionally drop the question that existed when they clicked, but it should not silently swallow a later generation. Fence dismissal against each binding/question generation or document this as a real live-red race, not self-healing.

MINOR: candidate cost is unbounded by manifest count and can double on companion strip-and-retry. Terminals already carry SUPERSET_ORGANIZATION_ID at C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\terminal\env.ts:251-275, so prefer that org’s manifest before scanning the rest and impose a total budget. One stale manifest costing 0.75s is not the worst case.

MINOR: tests are under-specified. Add rotation (>1 MB -> .1), all-ignored delivery, malformed response, unsafe terminal id, ENOENT vs non-ENOENT marker errors, selected-terminal/workspace isolation, partial failure behavior, markStale + push cancellation + question.stale frame, renderer seen/outstanding handling, resync after dismiss, cross-window/periodic convergence, and mutation failure leaving local dots unchanged. Current real-Python harness exists at C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\main\lib\agent-jsonl-watcher\pane-map-hook.test.ts:7-97.

Confirmed claims: _post_hook is env-URL-only with 1.5s timeout at pane-map-hook.ts:2238-2258; the only call and companion strip retry are :2606-2648; the live debug log is 486 MB and _log has no rotation at :401-412; host ignored semantics are as diagnosed; clearWorkspaceStatuses currently omits shellRunningTerminals while plain-shell blue is independently rendered at store.ts:1166-1190; the menu handler indeed calls host clear plus review-only local clear and never the renderer’s clearWorkspaceStatuses at useDashboardSidebarWorkspaceItemActions.ts:288-307; clearV2TerminalRunStatus has no callers beyond its barrel export.

SHOULD-A gate is sound under current ordering. The host snapshots bindings before async marker reads at agent-status-snapshot.ts:201 onward; the hook writes the marker before posting PermissionRequest; and a PermissionRequest arriving after the captured row updates the local source timestamp, so the preEntry.occurredAt > row.lastEventAt fence at resyncAgentStatus.ts:460-463 protects it. Implement pendingPermission === false as an explicit permission-axis clear only, with a race test where a newer live PermissionRequest lands during resync.

FEATURES.md needs both human-readable feature-table rows and duplicate machine marker lines for each required root. The machine format is one token plus one root per line at C:\Users\khair\Code Projects\fork-superset-windows-arm64\FEATURES.md:110-120.
</agent-message>

---

<agent-message from="arch-review-fable">
FABLE REVIEW — verdict: no BLOCKERs. Diagnosis-to-fix mapping correct and verified against code. 5 MAJORs (all fixable in-blueprint), several MINORs. SHOULD-A gate: I pre-verified it — it holds.

DIAGNOSIS VERIFIED
- Python hook has NO failover: single env URL read (pane-map-hook.ts:2430), _post_hook posts one URL, timeout 1.5s (:2238-2258), call sites :2607/:2629; zero "manifest" occurrences in the file. notify.sh failover confirmed at packages/agent-setup/templates/notify-hook.template.sh:170-214. Live manifest on disk confirmed (~/.superset/host/<org>/manifest.json with "endpoint"; format matches cli manifest.ts:17-31).
- Bug 2 name-collision confirmed: useDashboardSidebarWorkspaceItemActions.ts:292-307 calls clearWorkspaceAttention (review-only, store.ts:691) + host terminalAgents.clearWorkspaceStatuses (terminal-agents.ts:277-286, force-Stop only, store.ts:187-200) — never renderer store clearWorkspaceStatuses (store.ts:648). No code path deletes .askq dirs; snapshot re-asserts red from marker (agent-status-snapshot.ts:127-167; no-reap policy :103-122 as blueprint cites). Log claims exact: agent-notify-hook.log is 509,127,651 bytes; _log (:401-412) has no rotation; _decision_log (:423-438) already has the 1MB pattern to copy. Test harness exists as claimed (pane-map-hook.test.ts: real python + Bun.serve, lines 12-27).

MAJOR-1 (MUST-1 predicate). "First response NOT containing ignored:true" is the wrong polarity. notify.sh keys SUCCESS on positive `"ignored":false` (template :204-206), with any-2xx as fallback (:207-214). A reused port answering 200 with a foreign body contains no ignored:true → negative predicate declares delivered → event silently lost — exactly the failure class this feature exists to fix. Python isn't constrained to substring matching like sh: json.loads the body and require ignored===false. Also handles both spacings for free.

MAJOR-2 (MUST-1 all-ignored path undefined). A ghost terminal (real precedent: agent-status-snapshot.ts:41-48 — EVERY hook POST returned ignored:true) gets ignored:true from every candidate on every event. "Raise last exception if all fail" has no exception then. As written it falls through undefined; worse, if treated as failure it triggers the companion strip-and-retry loop + post-error log on a healthy path, every event. Define: all candidates probed, ≥1 2xx, none accepted → delivered no-op (notify.sh parity), log once.

MAJOR-3 (MUST-1 timeout halving). Drop the 1.5s→0.75s change. A dead loopback port fails with instant ECONNREFUSED — failover latency does not need a shorter timeout. Halving only bites a slow-but-live host (this is an emulated-msys2 ARM64 box under load): spurious failover, duplicate lifecycle/companion delivery, strip-and-retry churn. Keep 1.5s per candidate.

MAJOR-4 (MUST-2 marker enumeration unspecified). The mutation must say WHERE the terminal list for marker deletion comes from. terminalAgentStore bindings are persistence-backed (store.ts:40-57) so they survive restart, but a binding-less terminal with a leaked marker re-asserts red the moment its binding revives — after the user explicitly dismissed. Enumerate terminal_sessions rows for the workspace (DB as source of truth) and rm each <terminalId>.askq. Carry the SAFE_MARKER_SEGMENT guard (agent-status-snapshot.ts:80,132) into the extracted askqMarkerDir — it's the traversal guard.

MAJOR-5 (MUST-2 green resurrection after dismiss). Resync re-applies row.lastEventType (Stop) per row; review-raise is only suppressed when terminalSeenAt >= lastEventAt (resyncAgentStatus.ts:515-575). Dismiss forces Stop keeping lastEventAt, and renderer clear wipes the source entry, so any terminal whose pane wasn't visible comes back GREEN on next resync — within 60s if SHOULD-B ships. Clear Status would trade stuck-red for returning-green. Fix inside the design: on dismiss success also markTerminalSeen(terminalId, row.lastEventAt) for the affected terminals (renderer has the primitive; resync itself calls it at :509-511).

SHOULD-A GATE: verified sound. Single loop 431-686 (brace-scanned), fence `continue` at :460-463 skips the pendingPermission block at :660-685; loop is synchronous so no mid-loop event interleave. Ship A — recommend mirroring the null-branch's workspaceId check (:664-666) in the else-branch. Note: WITHOUT A, the blueprint's cross-window "convergence" claim is weaker than stated — nothing clears the permission axis on pendingPermission=false except A itself; other windows rely on the Stop apply passing the strict-> fence (equal timestamps pass, so it likely converges, but that's the one convergence claim I could not fully prove). One more reason to ship A.

VERIFIED CORRECT (no action)
- Keeping existing clearWorkspaceStatuses mutation unchanged: right — useTerminalInterruptClear.ts:44-46 uses it; host mutation never touches markers so live reds survive interrupt.
- Companion chain exact as claimed: sink module-global (companion-question-sink.ts:78-111), question-store byTerminal :1918 + markStale :1976-1991 → settle :1592-1609 → onSettled → push.cancelPending (companion/index.ts:664-678). markStale-not-resolve is correct (read-api.ts:439). Extension point: the wrapper at companion/index.ts:1161-1163 also needs dismissByTerminal; sink may be null (bridge not running) — mutation must treat that as questionsDismissed=0, not throw.
- Renderer widening: store.ts:648-690 indeed misses shellRunningTerminals + outstandingReadyAt; blue fallback at :1183-1190 confirmed. clearV2TerminalRunStatus :812-827 confirmed dead (barrel re-export index.ts:2 only, zero callers, stale doc).
- FEATURES.md gate: marker lines are token<TAB>root, grep -F per line (check-feature-markers.mjs); two lines for (MANUAL-DISMISS) with different roots is legal. Deliberate local FEATURES.md edits are allowed (only repairs are frozen).
- MUST-1 latency: live env URL → single POST, unchanged. Codex is covered too — the same embedded Python scripts are registered into Codex's hooks.json (pane-map-hook.ts:2660-2668).

MINOR
- handleClearStatus reorder: today clearWorkspaceAttention runs even with host down; blueprint's all-after-mutation means host-down = zero visible effect + toast. Acceptable fail-loud, but it's a small behavior regression — decide explicitly.
- Ghost/ignored terminals now probe every manifest on every event (notify.sh already does this); with 1-2 orgs it's one extra local POST — fine, worth a comment.
- SHOULD-B must reset syncedKeyRef (HostNotificationSubscriber.tsx:139-143 dedupes on key) or the periodic tick no-ops; and B amplifies MAJOR-5 60x if that isn't fixed.
- pane-map-hook.log (the OTHER _log, :93) is only 1.6MB — rotation there optional, not required.

Bottom line: approve with the five MAJOR amendments — predicate polarity + all-ignored semantics, keep 1.5s, DB-sourced marker enumeration + guard, seen-stamp on dismiss. Ship SHOULD-A (gate verified) and SHOULD-C; SHOULD-B only after MAJOR-5 is addressed.
</agent-message>

---

<agent-message from="arch-design">
# FINAL IMPLEMENTATION SPEC — stuck red dot + manual "Clear Status"

Scope approved: MUST-1, MUST-2, SHOULD-A, SHOULD-B, SHOULD-C. All consensus amendments adopted. One reviewer premise is factually wrong and I have the measurement; the resulting decision still goes the reviewers' way.

---

## 0. Disagreement resolved: keep 1.5 s. Fable's conclusion is right, its premise is wrong.

Fable argued a dead loopback port gives instant ECONNREFUSED, so failover needs no shorter timeout. **Measured on this box just now:**

```
55331 (dead, was the host)  2043.6 ms  ConnectionRefusedError [WinError 10061]
59999 (dead, never used)    2039.5 ms  ConnectionRefusedError [WinError 10061]
48056 (live host)               0.6 ms  connected
```

A dead loopback port takes **~2040 ms** to refuse here, not microseconds. That is precisely why 5,099 of 5,103 logged failures read `<urlopen error timed out>` and only 3 read anything else — the 1.5 s deadline expires ~540 ms *before* Windows surfaces the RST. The hook has never once seen a refusal.

**Decision: keep `timeout=1.5`.** My 0.75 s proposal is withdrawn, for reasons the measurement supplies rather than Fable's:

- 1.5 s already caps the dead-port cost *below* the 2040 ms OS refusal, so shortening buys 750 ms, not 2 s. Small prize.
- Against that: 0.6 ms live vs 750 ms budget looks like a huge margin, but I have **no p99 measurement of live-host response latency** on this emulated ARM64 box, and the healthy host's 8 errors in 19,459 posts prove slow responses exist. Trading an unmeasured correctness risk for 750 ms is a bad trade.
- "Never worse than today" is the right bar for a fix. 1.5 s meets it exactly.

Two bounds adopted from Sol instead:

- **Total probe budget 4000 ms** across all candidates, checked before each attempt. Worst realistic case (2 candidates) = 3 s, inside budget; a pathological manifest set cannot turn one hook event into a 10 s stall. On budget exhaustion: stop, log, treat as undelivered.
- **Org-first candidate ordering.** `SUPERSET_ORGANIZATION_ID` is set on the terminal env at `C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\terminal\env.ts:256-257`. Order = env URL, then that org's manifest, then remaining manifests. Keeps the common list at 2 and avoids foreign-org round trips.

---

## 1. Files to modify

### MUST-1 — endpoint healing in the Python hook

**`C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\main\lib\agent-jsonl-watcher\pane-map-hook.ts`**, embedded `NOTIFY_SCRIPT` (`:312`+). Marker `(HOOK-ENDPOINT-HEAL)` on each new function.

`_hook_candidate_urls(env_url)` — new, near `_post_hook` (`:2238`):
- Root = `os.environ.get("SUPERSET_HOME_DIR")` or `Path.home()/".superset"`. **Manifest glob only** — marker paths keep `Path.home()`, because the host reads them at a hardcoded `homedir()` (`...\packages\host-service\src\trpc\router\notifications\agent-status-snapshot.ts:133-138`). Comment this; do not unify.
- Order: `env_url`, then `<home>/host/<SUPERSET_ORGANIZATION_ID>/manifest.json`, then `<home>/host/*/manifest.json`. Dedupe preserving first position.
- Endpoint → `<endpoint>/trpc/notifications.hook`. Per-manifest failure (missing, unreadable, bad JSON, no `endpoint`) skips that entry and logs; never raises.

`_deliver(candidates, body)` — new. Per candidate, while `elapsed < 4.0`:
- `_post_hook(url, body)` (timeout stays 1.5).
- **Positive acceptance only.** `json.loads(response_body)`, then require `parsed["result"]["data"]["json"]["ignored"] is False`. Anything else — a `True`, a missing key, a non-dict, unparseable text, HTML from a foreign server on a recycled port — is **not** delivery. This matches notify.sh's positive check at `...\packages\agent-setup\templates\notify-hook.template.sh:204-206` and the envelope the route returns (`notifications.ts:312-357`; the test sink at `pane-map-hook.test.ts:43` returns exactly this shape).
- Track `saw_2xx` separately for the all-ignored path.
- Returns one of three outcomes: `("delivered", url, status, body)`, `("ignored-everywhere", None, None, None)` when every candidate was probed, at least one answered 2xx, and none accepted, or raises the last transport exception when nothing answered.

**All-ignored is a delivered no-op** (amendment 2, notify.sh parity at `notify-hook.template.sh:212-214`). This is the ghost-terminal case the snapshot documents at `agent-status-snapshot.ts:41-48`. Emit exactly one `_log` line with `action: "ignored-everywhere"` and **return**. It must not trigger the companion strip-and-retry and must not log `post-error` — otherwise a wedged pane writes two error records per tool call forever.

Call site `:2606-2649`:
- `_post_hook` → `_deliver`; the `posted` record gains the delivering `url`.
- Companion strip-and-retry (`:2623-2642`) fires **only** on the raise path, and re-posts to **the candidate list already computed**, not a fresh probe. If `_deliver` raised because nothing answered, a stripped body will not answer either — skip the retry and go straight to the failure log.
- Failure path `:2643-2648`: keep the `post-error` line, and add one `_decision_log` line naming every candidate tried. `_decision_log` (`:415`) is always-on and already rotates at 1 MB.

`_log` rotation (`:401-412`) — MUST. Copy the rotate-at-1 MB idiom from `_decision_log:428-438`. The live file is **508,757,788 bytes**.

Codex is covered by the same script: `hookCommand()` / `notifyHookCommand()` at `pane-map-hook.ts:2660-2668` install one Python file for both agents. No separate Codex change.

### MUST-2 — authoritative manual dismissal

**`...\packages\host-service\src\trpc\router\notifications\agent-status-snapshot.ts`** — marker `(MANUAL-DISMISS)`.

Extract `askqMarkerDir(terminalId): string | null` from the inline construction at `:133-138`. **The `SAFE_MARKER_SEGMENT` test (`:80`) moves inside it** and returns `null` on failure, so no caller can build a path from an unvalidated id. `hasPendingQuestionMarker` uses it (a `null` means "definitively absent", its current `:132` behaviour).

New `clearPendingQuestionMarkers(terminalId, dismissStartedAtMs): Promise<{removed: string[], survivors: string[]}>`:
- `askqMarkerDir` → `null` ⇒ return empty/empty. Never `rm` from an id that failed the guard.
- `readdir`; `ENOENT` ⇒ empty/empty (success). Any other read error ⇒ **throw** (loud; an unknown filesystem is not a successful dismissal).
- **Per-owner mtime fence (amendment 8).** `stat` each owner; unlink only those with `mtimeMs <= dismissStartedAtMs`. A `stat` failure means the owner is **not** removed — unknown is not evidence, matching the safe direction the reader already takes at `:174-191`.
- Unlink the owner files, not the directory, then `rmdir` only if empty.

The fence is the honest answer to amendment 8, and it is better than documenting the race. `pane-map-hook.ts:2137-2141` writes the marker synchronously *before* returning `PermissionRequest`, so a question raised after the click has an mtime strictly greater than `dismissStartedAtMs` and **survives**. The semantics land exactly where they should: a dismissal clears what the user was looking at when they clicked, and nothing raised afterwards. Same wall clock, same process, NTFS mtime granularity ~100 ns.

**`...\packages\host-service\src\trpc\router\terminal-agents\terminal-agents.ts`** — new `dismissWorkspaceStatuses`, marker `(MANUAL-DISMISS)`.

- Input `z.object({ workspaceId: z.string().min(1), terminalId: z.string().min(1).optional() })`. Boundary validation per amendment 3; `SAFE_MARKER_SEGMENT` is enforced again inside the path builder.
- **Terminal set derived server-side from the DB** (amendment 3): `terminalSessions` rows where `originWorkspaceId = workspaceId`, intersected with `terminalId` when supplied. Same DB-as-truth read the snapshot already performs at `agent-status-snapshot.ts:249-259`. This sweeps leaked markers of terminals with no live binding, which a bindings-derived set would miss.
- `dismissStartedAtMs = Date.now()` captured once, before any deletion, and used as the fence for every terminal.
- Per terminal, in order: `clearPendingQuestionMarkers` → existing `ctx.terminalAgentStore.clearWorkspaceStatuses(workspaceId, terminalId)` → companion dismissal.
- Returns `{ dismissStartedAtMs, terminals: Array<{ terminalId, lastEventAt, markersRemoved: number, pendingAfter: boolean, questionDismissed: boolean }> }`. `lastEventAt` is the host-clock instant from the binding after the clear (amendment 4). `pendingAfter` is `survivors.length > 0`.
- First marker error aborts the whole mutation loud, reporting which terminals completed. Silent partial success is the bug class being fixed.

`clearWorkspaceStatuses` stays **unchanged** — `useTerminalInterruptClear.ts:46` depends on its non-red-dropping semantics.

**`...\packages\host-service\src\trpc\router\notifications\companion-question-sink.ts`** — add to `CompanionQuestionSink` (`:78-87`):

```ts
dismissByTerminal(input: { hostTerminalId: string; reason: string }): boolean;
```

Plus `forwardCompanionDismissal(...)` mirroring `forwardCompanionCapture` (`:264-311`). **Sink null ⇒ return `false`, never throw** (amendment 5) — that is every build where the bridge is not running.

**`...\packages\host-service\src\companion\index.ts`** — implement it on the `createNotifyingCaptureSink` wrapper registered at `:1161-1168`, beside the existing `capture` and `resolve` (`:1770-1806`):
- `deps.questions.byHostTerminal(hostTerminalId)`; `null` ⇒ return `false`.
- `deps.inner`/`questions.markStale(question.questionId, reason)` — terminal and provenance-free (`question-store.ts:1976-1991`). Not `resolve`: that stamps answered-by provenance (`:757-761`) and nobody answered. `markStale` routes through `settle()` → `onSettled` → `push.cancelPending` (`:663-676`), so the phone notification retracts.
- **Publish the frame** (amendment 5): `events.publish({ t: "question.stale", d: { questionId, reason: QUESTION_STALE_MANUAL_DISMISS_REASON } })`, matching the reconcile path at `:985-990`. `QuestionStaleEventData` is `{questionId, reason: string}` (`types.ts:1352-1355`), so a new reason constant is a drop-in; add it beside `QUESTION_STALE_TERMINAL_GONE_REASON` (`question-store.ts:1483`). `events.publish` advances `gseq`, so paired phones refetch instead of holding stale question state.
- Wrap the publish in the same never-throw guard the reconcile publisher uses (`index.ts:970-996`): a frame nobody can receive must not break the retraction beside it.

**`...\apps\desktop\src\renderer\routes\_authenticated\_dashboard\components\DashboardSidebar\components\DashboardSidebarWorkspaceItem\hooks\useDashboardSidebarWorkspaceItemActions\useDashboardSidebarWorkspaceItemActions.ts`** — rewrite `handleClearStatus` (`:292-307`). Ordering decided explicitly (Fable MINOR):

1. `clearWorkspaceAttention()` **first, unchanged**. Today this runs before the try block, so a host-down click still clears green and reports read. Moving it behind the mutation would regress that. Keep today's behaviour intact.
2. `await dismissWorkspaceStatuses`.
3. On success: `useV2NotificationStore.getState().clearWorkspaceStatuses(workspaceId)`, then per returned terminal `markTerminalSeenAndReportRead({ workspaceId, terminalId, lastEventAt })`.
4. On failure: existing `toast.error` (`:302-306`). Green is already cleared; red and yellow stay. **This is the correct outcome, not a regression** — the marker survived, so a locally-dropped red would be re-raised by the next resync. Showing the user a red that is still真 beats a green that lies for 60 s.

**Amendment 4 — durability.** Step 3's `markTerminalSeenAndReportRead` is what makes convergence provable. Forcing the binding to `Stop` while preserving `lastEventAt` means a later resync replays `Stop` → `review` for any terminal whose seen watermark does not cover `lastEventAt` (`...\resyncAgentStatus.ts:515-575`), and SHOULD-B would re-raise that green within 60 s. Marking seen through the host's own `lastEventAt` closes it, and it is the same primitive resync itself uses at `:509-511`.

`markTerminalSeenAndReportRead` (`...\V2NotificationController\lib\companionAlertSync.ts:390-412`) is also the answer to **Sol's MAJOR on `outstandingReadyAt`**: it reports read and compare-and-clears the record **only on host ack**, exactly as the type doc requires (`store.ts:150-177`). One call satisfies both.

**`...\apps\desktop\src\renderer\stores\v2-notifications\store.ts`** — `clearWorkspaceStatuses` (`:648-690`):
- **Add** `shellRunningTerminals` purge for the workspace's terminals. The agent-terminal gate at `:971-974` suppresses shell-blue on agent terminals, but `selectV2WorkspaceDisplayStatus:1183-1190` yields a status for open terminals with no source entry, so a plain shell's blue survives a full source purge.
- **Do NOT touch `outstandingReadyAt`** — reversing my earlier spec, per Sol. It is the retraction evidence for ready alerts and is cleared only by `clearOutstandingReady` on host ack (`:163-172`). Unconditional deletion strands a phone notification until expiry.
- Add the `ndots` mutation log line the sibling `clearWorkspaceAttention` writes at `:704-713`.

**Dead code, same PR.** Delete `clearV2TerminalRunStatus` (`store.ts:812-826`) and its barrel re-export (`...\stores\v2-notifications\index.ts:2`). Those are its only two references. Its doc comment at `:808-811` and `:827-829` claims interrupt and pane close route through it — false, and actively misleading anyone debugging this exact bug. Delete rather than correct: the behaviour it describes now lives in the dismiss path and in SHOULD-C.

### SHOULD-A — resync can retract red

**`...\V2NotificationController\lib\resyncAgentStatus.ts:660-685`.** Fable verified the gate: the `preEntry.occurredAt > row.lastEventAt` fence at `:460-463` covers a mid-flight `PermissionRequest` and the loop is synchronous. **Ship it.**

Add the `else` branch for `pendingPermission === false`, mirroring the null-branch's `workspaceId` check at `:664-666`:

```
else if (preEntry?.axes.permission !== undefined
      && preEntry.workspaceId === row.originWorkspaceId) {
  applySourceAxes(source, row.originWorkspaceId,
                  { set: [], clear: ["permission"] }, row.lastEventAt);
  result.retractedPermission++;
}
```

A also supplies the cross-window convergence proof for the permission axis that MUST-2 otherwise lacks: after a dismissal the marker is gone, so every other window's next snapshot reads `pendingPermission: false` and actively retracts. Without A, other windows converge on green/yellow but a stale red persists until reconnect.

### SHOULD-B — periodic resync

**`...\HostNotificationSubscriber\HostNotificationSubscriber.tsx:135-172`.** Ships only together with amendment 4 (which is in MUST-2), else it re-raises dismissed greens on a 60 s cadence.

`runResync` early-returns on `syncedKeyRef.current === key` (`:141-143`), so a tick must clear that guard. Requirements (amendment 6):
- `resyncInFlightRef` set before the call, cleared in both `.then` paths, so a slow snapshot cannot overlap the next tick.
- The tick sets `syncedKeyRef.current = null` immediately before invoking `runResync`, and only when connected and not in flight.
- 60_000 ms interval, created on connect and **cleared on disconnect and on unmount**, alongside the existing `retryTimerRef` cleanup.
- The existing generation guard (`resyncGenerationRef`, `:145`) already discards a superseded reply; do not add a second mechanism.

### SHOULD-C — interrupt clears the renderer too

**`...\TerminalPane\hooks\useTerminalInterruptClear\useTerminalInterruptClear.ts:40-58`.** After the existing mutation resolves, clear that terminal's own axes locally.

**Clear `working` and `review` and the background-running entry. Never `permission`** (amendment 7). An interrupt is an automatic path; dropping a live red there is the one thing forbidden. Ctrl+C does not answer a pending AskUserQuestion, and the watcher's `_main` clear on a detected interrupt (`...\agent-jsonl-watcher.ts:1043-1044`) is the only sanctioned red release on that path.

Do **not** route this at `dismissWorkspaceStatuses`. Keep `clearWorkspaceStatuses`.

### FEATURES.md

Two human-readable rows in the feature table (`:15`+), and four machine lines in the fenced ```markers block (`:110-120`, format `<marker-token>\t<path-glob-root>`; two lines with different roots is legal):

```
(HOOK-ENDPOINT-HEAL)	apps/desktop/src/main
(HOOK-ENDPOINT-HEAL)	apps/desktop/src/main/lib/agent-jsonl-watcher/pane-map-hook.test.ts
(MANUAL-DISMISS)	packages/host-service/src
(MANUAL-DISMISS)	apps/desktop/src/renderer
```

The test-pinned line matters: without it a merge could take upstream's router and leave the dispatch untested while every gate stays green — the reasoning already written at `notifications.ts:16-22` for `(COMPANION-CAPTURE-HOOK)`.

---

## 2. Data flow

**MUST-1, after.** Answer → `PostToolUse` → `_remove(askq/<owner>)` → `"Start"` → candidates `[55331/…, 48056/…]` → candidate 1 transport-fails at 1.5 s → candidate 2 → `200`, envelope parsed, `ignored === false` → accepted → `notifications.ts:312` broadcast, `:320` binding persisted → `statusTransitions.ts:76-87` sets working, clears permission.

**MUST-2, after.** Right-click → Clear Status → local `clearWorkspaceAttention()` (green + read report) → `dismissWorkspaceStatuses`: `dismissStartedAtMs` captured; terminal set read from `terminalSessions`; per terminal, owners with `mtime <= dismissStartedAtMs` unlinked (newer ones survive) → binding forced `Stop` → `byHostTerminal` + `markStale` → `settle()` → `push.cancelPending` + `question.stale` frame (gseq advances, phone refetches) → returns per-terminal `{lastEventAt, pendingAfter}`. Renderer: `clearWorkspaceStatuses(workspaceId)` purges sources, background, shell, manual; `markTerminalSeenAndReportRead` per terminal through `lastEventAt`. Next resync reads `pendingPermission: false`, `lastEventType: "Stop"` covered by the seen watermark, and asserts nothing. The hook's central guard sees an empty dir and stops holding `SubagentActive` (`pane-map-hook.ts:2214-2216`).

Validation at the tRPC boundary plus inside the path builder; ownership from the DB read; the `.askq` directory remains the single source of truth for red.

---

## 3. Build order

1. `_log` rotation. Verify: suite green, shipped script rotates at 1 MB.
2. `_hook_candidate_urls` (unused). Verify: 0/1/2 manifests, one malformed, org-first ordering, dedupe.
3. `_deliver` with positive envelope parsing, all-ignored outcome, 4 s budget. Wire the call site and the constrained companion retry. Verify: full test list §5.
4. Mutation check per `plans\agent-hook-url-frozen-port.md:116`: break the manifest glob, confirm only the failover tests fail, restore.
5. **Live heal, no rebuild needed.** Start the desktop so `writeNotifyScriptIfChanged` (`pane-map-hook.ts:2736`) lands the script; answer a question in `72d85ab1` or `d3ce7c25`; expect `permission → working` in `~/.superset/agent-dot-render.log` within a second and a `posted` line naming `48056`.
6. `askqMarkerDir` + `clearPendingQuestionMarkers` with the mtime fence.
7. `dismissByTerminal` on sink and wrapper, with `question.stale` publish.
8. `dismissWorkspaceStatuses`.
9. Renderer: store widening, `handleClearStatus`, dead-code deletion.
10. SHOULD-A.
11. SHOULD-B (after A, so a tick converges rather than oscillates).
12. SHOULD-C.
13. FEATURES.md; `node scripts/check-feature-markers.mjs`.
14. `bun run typecheck`, `bun run lint`, `/simplify`.

---

## 4. Risks

- **Duplicate delivery** if a slow live host processes a request we abandoned and the next candidate also accepts. Two identical broadcasts: harmless on the dot axes, and the companion path is already protected — the retry re-sends the *same* `companionLifecycleEventId`, which the sink matches against its applied-id window (`pane-map-hook.ts:2543-2555`). Bounded further by keeping 1.5 s.
- **Foreign server on a recycled port** answering 200 with arbitrary text: rejected by positive envelope parsing. This is the concrete reason the polarity flip matters.
- **All-ignored** on a genuinely wedged pane: one log line per event, no error spam, no retry. Matches notify.sh.
- **Marker fence residual**: a question whose marker write and our `stat` interleave inside one mtime tick. NTFS granularity ~100 ns against a multi-millisecond mutation; not a practical window. A `stat` failure never deletes.
- **Dismissal is still allowed to drop a live red** — the reds that existed at click time. That is the user's explicit decision. The fence narrows it to exactly that set; it is no longer an unbounded race, and I am not claiming it self-heals.
- **`pendingAfter: true`** means a newer question survived the dismissal. The renderer must not present that terminal as cleared; surface it rather than hide it.
- **Total probe budget** could in principle truncate a legitimate multi-org probe. 4 s against two 1.5 s candidates leaves headroom; log on exhaustion.
- **Host-down click**: green clears, red/yellow stay, toast fires. Deliberate, stated above.
- **Three similarly-named functions** (host `clearWorkspaceStatuses`, renderer `clearWorkspaceStatuses`, new `dismissWorkspaceStatuses`). The collision already caused this bug — cross-reference all three in comments.
- No schema change, no migration, no new persisted key (`partialize`, `store.ts:745-753`, already covers the maps).

---

## 5. Test list

**Hook (`pane-map-hook.test.ts`, real Python + real sink, harness `:12-97`)**
1. `_log` rotates past 1 MB, single `.log.1` backup.
2. Dead env URL → delivered via manifest.
3. Live env URL → manifests never probed.
4. Wrong-org sink returns `ignored:true`, right-org returns `ignored:false` → probing continues, delivers to the right one.
5. All candidates return `ignored:true` → `action: "ignored-everywhere"`, exactly one log line, no `post-error`, no companion retry.
6. Malformed body (HTML, truncated JSON, `{}`, `{"result":{}}`) → **not** delivered; probing continues.
7. All candidates transport-fail → `post-error` plus one `dot-decisions.log` line naming every candidate.
8. Probe budget exhausted → stops, logs, no further attempts.
9. Org-first ordering with two manifests present.
10. Existing suite unchanged — note the sink at `:43` returns no `ignored` field, so it now counts as **not** delivered; update it to `{success:true, ignored:false}` or the existing tests will break. Flagging this because it is easy to miss.

**Host**
11. Unsafe terminal id (`../`, empty, unicode) → `askqMarkerDir` returns null, nothing unlinked, mutation still succeeds for valid siblings.
12. `readdir` ENOENT → success; non-ENOENT → throws.
13. mtime fence: owner older than `dismissStartedAtMs` removed; owner newer survives; `pendingAfter: true` reported.
14. `stat` failure → owner retained.
15. Workspace isolation: terminals of another workspace untouched.
16. Terminal with a `terminal_sessions` row but no live binding → its leaked marker is still swept.
17. Partial failure: first terminal throws → mutation fails, response names completed terminals.
18. `markStale` fires, `push.cancelPending` called, `question.stale` frame published with the manual reason, gseq advanced.
19. Sink null → `questionDismissed: false`, no throw.

**Renderer**
20. Store `clearWorkspaceStatuses` purges sources, background, shell, manual; leaves `outstandingReadyAt` intact.
21. `handleClearStatus` on host failure → green cleared, red/yellow retained, toast shown, no store purge.
22. `handleClearStatus` on success → axes gone, `markTerminalSeenAndReportRead` called per returned terminal with the host `lastEventAt`.
23. Resync-after-dismiss: snapshot with `pendingPermission: false` and `lastEventType: "Stop"` raises neither red nor green.
24. SHOULD-A: `pendingPermission: false` retracts a latched red; wrong-workspace `preEntry` does not; the `:460-463` fence still wins for a newer local `PermissionRequest`.
25. SHOULD-B: tick resets the guard and runs; overlapping tick suppressed while in flight; interval cleared on disconnect and unmount.
26. SHOULD-C: interrupt clears working/review/background and **leaves permission set**.
27. Cross-window convergence: window B's next resync clears after window A dismisses.

**End-to-end (CDP, `.agents/skills/cdp-verification/SKILL.md`)**
28. Stick a red, click Clear Status, confirm the dot dies and **survives a forced resync**.

---

## 6. Unchanged from review consensus

Existing `clearWorkspaceStatuses` mutation untouched; `markStale` not `resolve`; no `Detached` broadcast; no subagent run-dir deletion; no leaked-marker age reap (`agent-status-snapshot.ts:104-122`); log rotation as MUST; Codex covered by the same script.

## 7. Out of scope

Events lost between 03:00 and the fix. Port pinning / stable socket (rejected at `plans\agent-hook-url-frozen-port.md:90-91`). The `SubagentActive` volume. The three other shell templates. A tab-level Clear Status (none exists; the mutation takes an optional `terminalId` so it is cheap later). The v1 `screens/main` menu and tabs store. Unifying the four `.askq` path implementations across Python, Electron main and host. "Agent status unavailable" UI. `SUPERSET_AGENT_HOOK_PORT`.
</agent-message>

---

<agent-message from="impl-hook">
MUST-1 done. Only the two allowed files touched. Nothing committed.

FILES
- C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\main\lib\agent-jsonl-watcher\pane-map-hook.ts
- C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\main\lib\agent-jsonl-watcher\pane-map-hook.test.ts

WHAT CHANGED (all inside NOTIFY_SCRIPT, marker `(HOOK-ENDPOINT-HEAL)`)
1. `_log` rotates at 1MB, single `.log.1` backup — same idiom as `_decision_log`.
2. `import time` added.
3. `_hook_candidate_urls(env_url)`: env URL, then `<root>/host/<SUPERSET_ORGANIZATION_ID>/manifest.json`, then `<root>/host/*/manifest.json` sorted; deduped first-position-wins. Root = `SUPERSET_HOME_DIR` or `Path.home()/".superset"`, glob only, with the comment that marker paths stay on `Path.home()` because the host reads them at a hardcoded `homedir()`. Per-manifest failure → skip + log line, never raises.
4. `_hook_accepted(raw)`: positive-only `parsed["result"]["data"]["json"]["ignored"] is False`.
5. `_deliver(candidates, body)`: per-request timeout unchanged at 1.5s, 4.0s total budget checked before each attempt. Returns `("delivered", url, status, body)` or `("ignored-everywhere", None, None, None)`; otherwise raises.
6. Call site: `candidates` built after `_decide_event_type` (markers still mutate first), `posted` log gains `deliveredUrl`, ignored-everywhere emits exactly one log line and returns, failure path logs post-error plus one always-on `_decision_log` line naming every candidate.

TESTS: 54 pass / 0 fail / 329 expect() in 25s (`bun test apps/desktop/src/main/lib/agent-jsonl-watcher/`). 45 pre-existing + 9 new covering (a) rotation, (b) dead env URL → manifest delivery, (c) live env URL → manifests unprobed, (d) ignored:true → keep probing → deliver to owner, (e) ignored-everywhere no-op, (f) HTML/truncated/{}/{"result":{}} rejected, (g) all-dead → post-error + one dot-decisions line, (h) budget exhaustion, (i) org-first ordering.

MUTATION CHECK: broke the manifest glob → 5 failover tests failed, 49 passed, zero pre-existing failures. Restored. Second mutation (dropped the org-id direct path) → only the org-first test failed. Restored; suite green again.

DEVIATIONS (all deliberate)
- Trap (j) handled, plus one more the spec did not flag: the EXISTING `hook()` helper inherits `process.env`, and this suite runs inside Superset, so the real `SUPERSET_ORGANIZATION_ID` / `SUPERSET_HOME_DIR` would have pointed the new failover at the developer's LIVE hosts. Both are now blanked in that helper.
- Logic split into `_hook_accepted` and `_undelivered` helpers rather than inline in `_deliver`. Structural only.
- The raise still carries the last transport exception, with `superset_saw_2xx` / `superset_tried_urls` set on it (synthetic `RuntimeError` when nothing threw). That attribute is how the call site skips the companion retry when nothing answered.
- `posted` keeps `"url"` = env URL and ADDS `"deliveredUrl"`, so the existing field does not silently change meaning.
- `endpoint.rstrip("/")` before appending `/trpc/notifications.hook`. Hosts write no trailing slash, so behaviour is identical; it just cannot produce `//` or a near-duplicate candidate.
- Spec did not define what happens if the companion strip-and-retry itself returns ignored-everywhere. Falling through to post-error would be wrong (the dot WAS delivered), so it logs `companion-rejected-dot-ignored-everywhere` and returns.
- Budget test uses sinks that sleep 3s rather than dead ports, so the three 1.5s timeouts are deterministic on any OS instead of depending on how fast it refuses a connection.
- Ran `bunx biome format --write` on the test file ONLY (biome rejected my multi-line `it(name, fn, timeout)` formatting). No repo-wide run.

TWO THINGS FOR INTEGRATION
- If `SUPERSET_HOST_AGENT_HOOK_URL` is empty/unset, `main()` still returns early at `skip-no-url` BEFORE candidates are built, so failover never runs for an env-less terminal. Left as-is (spec scoped the change to :2606-2649); say the word if you want the early return moved below the candidate build.
- `_post_hook` truncates the response body at 500 chars before the acceptance parse. The real envelope is ~60 chars, so no false negative today, but a much larger success body would read as undelivered (cost: a duplicate POST plus a spurious post-error line, not a lost dot).
</agent-message>

---

<agent-message from="impl-renderer">
Renderer half of MUST-2 + SHOULD-A/B/C is done. No commits, no repo-wide formatter, FEATURES.md untouched. Only my listed files plus 3 justified extras (below).

WHAT CHANGED

1. C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\renderer\stores\v2-notifications\store.ts
- clearWorkspaceStatuses now also purges shellRunningTerminals for the workspace (plain-shell blue rendered with no source entry), keeps outstandingReadyAt untouched, writes the ndots mutation line per source like clearWorkspaceAttention, marker (MANUAL-DISMISS).
- Deleted dead clearV2TerminalRunStatus and its barrel re-export in stores\v2-notifications\index.ts. Grep confirmed zero callers before deleting (only the barrel).

2. ...\DashboardSidebarWorkspaceItem\hooks\useDashboardSidebarWorkspaceItemActions\useDashboardSidebarWorkspaceItemActions.ts
- handleClearStatus is now a 3-line binding over an exported, React-free `runClearWorkspaceStatuses({workspaceId, workspaceHostUrl, clearWorkspaceAttention, invalidateBindings})`. Order: clearWorkspaceAttention() first → await terminalAgents.dismissWorkspaceStatuses({workspaceId}) → renderer store clearWorkspaceStatuses(workspaceId) → markTerminalSeenAndReportRead per returned terminal → invalidateQueries. Failure keeps the existing toast and leaves red/yellow latched. pendingAfter terminals are skipped for the seen mark. Doc block cross-references the three similarly-named functions.

3. ...\V2NotificationController\lib\resyncAgentStatus.ts (SHOULD-A)
- Added the missing `else` for pendingPermission === false, with the same workspaceId fence as the null branch, clearing ["permission"] at row.lastEventAt. New ResyncResult counter `retractedPermission`. The :460-463 occurredAt fence is untouched.

4. ...\HostNotificationSubscriber\HostNotificationSubscriber.tsx (SHOULD-B)
- PERIODIC_RESYNC_MS = 60_000. resyncInFlightRef set before the request, cleared in a `.finally` (covers rejection too; finally re-throws so nothing gets quieter). runPeriodicResync effect event: returns early unless connected and not in flight, nulls syncedKeyRef, then runResync(workspacesKey). Interval armed on connect (cleared first so a duplicate open leaves one), cleared on disconnect and on unmount next to retryTimerRef. No second generation mechanism.

5. ...\TerminalPane\hooks\useTerminalInterruptClear\useTerminalInterruptClear.ts (SHOULD-C)
- Still calls the existing clearWorkspaceStatuses HOST mutation; after it resolves, exported `clearInterruptedTerminalAxes` clears that terminal's working + review axes and its background-running entry. Never permission. Passes the entry's own occurredAt into applySourceAxes instead of Date.now() so a surviving permission axis keeps its host-clock instant (the resync fence compares against it).

TESTS (all new tests colocated, bun test)
- store.test.ts +3: purge covers sources/background/shell/manual; outstandingReadyAt survives; other workspaces untouched. 25 pass.
- useDashboardSidebarWorkspaceItemActions.test.ts (new) 5 pass: host failure → green cleared + toast + red/yellow latched + no purge + no invalidate; no-host path; success → axes gone, seen watermark = host lastEventAt per terminal, companion read reported and outstanding record retired; pendingAfter → not marked seen; null lastEventAt → not marked seen.
- resyncAgentStatus.test.ts +5, 22 pass: retracts the latched red; wrong-workspace preEntry does not; occurredAt fence still wins for a newer local PermissionRequest; cross-window red convergence; dismissed workspace not resurrected.
- HostNotificationSubscriber.test.tsx (new) 4 pass: tick re-asks every interval, overlap suppressed, stops on close and resumes on reconnect, stops on unmount. happy-dom + bun fake timers.
- useTerminalInterruptClear.test.ts (new) 3 pass: clears working/review/background, leaves permission (and its occurredAt), leaves outstandingReadyAt.
- My five files together: 58 pass / 0 fail.
- Full `bun test src/renderer`: 1703 pass, 13 fail, 0 errors. Those 13 are the exact pre-existing failures I measured on the same tree with my new test files removed (launch-command, persisted-keys, V2ProjectSettings, formatDayLabel, etc.). Zero new failures.
- `tsc --noEmit` in apps/desktop: 38 errors, none in any file I touched (pre-existing type debt). Biome check clean on everything I touched.

DEVIATIONS / ASSUMPTIONS
a) I exported `runClearWorkspaceStatuses` and `clearInterruptedTerminalAxes` from their hook files rather than testing the hooks through renderHook. There is no renderHook precedent anywhere in apps/desktop; the established pattern is testing named exports out of the hook file (e.g. useGithubStarAction.test.ts). Behaviour is unchanged, the hooks are thin bindings now.
b) Your contract said lastEventAt matched the binding type; the host half has since landed with `lastEventAt: number | null` (null = marker swept off a terminal with no binding). I skip the seen mark for null and cover it with a test. tsc caught this, so the two halves now agree.
c) Three files outside my list. resyncAgentStatus.test.ts: added the sibling exports (hostServiceQueryRetryDelay etc.) to its host-service-client mock — without them that file cannot run standalone at all, on HEAD too. useFileContent.test.ts and useFolderFirstImport.test.ts: both did `mock.module("react", () => ({ useMemo }))`, replacing React process-wide with no unmock, which killed every later file that touches React.createContext via posthog-js/react — including both of my new test files. Fixed by spreading the real module and overriding the one hook. Test-only, no product code; this is also why V2ProjectSettings and friends fail today, though those 13 still fail for their own reasons.
d) Cross-window convergence (27) is red-only. A dismiss in window A takes down window B's red on B's next resync (tested). B's GREEN does not converge: the seen watermark is per-renderer sessionStorage and agentStatusSnapshot carries no read state. Pre-existing shape, out of scope, documented in the test.
e) `applySourceAxes` marks a terminal as an agent terminal as a side effect. Both new call sites are already gated to agent terminals, so it is a no-op in practice.
</agent-message>

---

<agent-message from="impl-host">
MUST-2 host-service half complete. All 28 new tests pass; package typecheck clean (no new diagnostics).

FILES CHANGED (all absolute, all additive except 9 deleted lines listed below)

1. C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\trpc\router\notifications\agent-status-snapshot.ts
   - New exported `askqMarkerDir(terminalId): string | null` with SAFE_MARKER_SEGMENT inside it.
   - New exported `clearPendingQuestionMarkers(terminalId, dismissStartedAtMs)` → `{removed, survivors}`; `ClearedQuestionMarkers` interface exported.
   - `hasPendingQuestionMarker` now calls `askqMarkerDir`; null → false (behaviour at old :132 preserved).
   - Deletions: only the old inline `join(...)` + inline guard, and the `readdir, stat` import line (now `readdir, rmdir, stat, unlink`).

2. C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\trpc\router\terminal-agents\terminal-agents.ts
   - New exported `dismissWorkspaceStatuses(deps, input)` + `dismissWorkspaceStatusesRouter` procedure `dismissWorkspaceStatuses`.
   - Existing `clearWorkspaceStatuses` mutation byte-identical (whole-file diff has zero `-` lines).

3. C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\trpc\router\notifications\companion-question-sink.ts
   - `dismissByTerminal` added to `CompanionQuestionSink`; new `forwardCompanionDismissal({terminalId, reason}): boolean`.

4. C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\companion\index.ts
   - `dismissByTerminal` implemented on `createNotifyingCaptureSink`.

5. C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\companion\question-store.ts
   - `QUESTION_STALE_MANUAL_DISMISS_REASON` constant only.

NEW TESTS
- C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\trpc\router\notifications\agent-status-dismiss.test.ts (18 pass)
- C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\trpc\router\terminal-agents\dismiss-workspace-statuses.test.ts (10 pass)
Covers spec items 11-19.

TEST OUTPUT
- Touched areas (`bun test src/trpc/router/notifications src/trpc/router/terminal-agents src/terminal-agents src/companion`): 562 pass, 1 fail. The one failure is `src/companion/remote-pairing.test.ts` "is a remote-only code" — it reads `pairing.ts` (unmodified by me, `git status` clean) and searches for `"\n}\n"`; the file has CRLF terminators so the search can never match on this checkout. Pre-existing, unrelated.
- Full `bun test`: 1613 pass / 106 fail. All 106 are git-worktree / daemon / worker-pool / network integration suites plus the CRLF one; none mention dismiss, agent-status, companion, terminal-agents or question. Verified by baseline: I copied my 5 source files aside, `git checkout --` them, re-ran `test/integration/workspace-cleanup.integration.test.ts` → identical 6 pass / 11 fail, then restored (diffstat identical before and after). No other agent's files were touched.
- `tsc --noEmit`: 16 diagnostic lines, same set as before my change; the only companion one is the pre-existing `curation-gates.test.ts` `remoteAnswer` fixture drift.
- `biome check --write` run on my 7 files only, never repo-wide.

DEVIATIONS (all deliberate)
1. Core logic extracted as exported `dismissWorkspaceStatuses(deps, input)` with the router procedure as a thin wrapper — mirrors `resumeTerminalAgentSession` in the same file and is how the existing tests there drive it without a tRPC context.
2. Touched a 5th file, `companion/question-store.ts`, for the constant. Spec D required it; it was not in the "touch only" list, so flagging. One added constant, nothing else.
3. `createNotifyingCaptureSink`'s return type widened to `QuestionCaptureSink & Pick<CompanionQuestionSink, "dismissByTerminal">`. Forced: `CompanionQuestionSink` now requires the method, and the wrapper is what gets registered at index.ts:1161. `QuestionCaptureSink` itself is unchanged, so `asCaptureSink()` is unaffected.
4. `forwardCompanionDismissal` never throws for ANY reason, not just a null sink: a throwing sink is caught, logged loud via console.error, and reported as `questionDismissed: false`. By the time it runs the markers are already deleted and the binding already cleared, so a companion fault must not turn a completed dismissal into a reported failure.
5. `markStale` receives `input.reason`; the wire frame names `QUESTION_STALE_MANUAL_DISMISS_REASON`. The only caller passes that same constant, so log and wire agree — no drift against the constant's doc comment.
6. `unlink` returning ENOENT counts as `removed`, not an error: something else reached the dismissal's intended end state first.
7. `rmdir` failure is swallowed (best effort). The directory is a container; an empty one and an absent one both read as "no question", and a racing hook may legitimately have just recreated an owner.
8. Test 18 asserts gseq with a REAL `createEventStreamServer` (its constructor touches none of its deps, so stubs are safe) and `currentGseq()`, rather than the repo's usual `{publish}` fake — gseq is asserted nowhere else in the codebase and I wanted the real counter.
9. Tests redirect `homedir()` via `USERPROFILE`/`HOME` (verified bun honours both at call time) and restore in `afterEach`. The stat-failure case uses a dangling NTFS junction — readdir lists it, stat ENOENTs — which needs no elevation and is deterministic.

NOT DONE (out of scope, as instructed): renderer wiring, FEATURES.md marker entry, no commit.
</agent-message>

---

<agent-message from="cr-fable">
Adversarial review of uncommitted diff. Verified every finding against code; ran all new/touched suites (all green: host-service dismiss 28/28, renderer 90, hook failover+subscriber 35, react-mock fixes 5/5). Findings:

[BLOCKER] C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\companion\index.ts:1830 (dismissByTerminal) + terminal-agents.ts:229
Defect: Companion dismissal is NOT fenced by dismissStartedAtMs — `dismissByTerminal` marks stale whatever question is pending at call time, including one raised AFTER the click.
Failure: User clicks Clear Status at T0. Hook lands a new AskUserQuestion at T1>T0 (window = tRPC round trip + readdir/stat/unlink loop). `clearPendingQuestionMarkers` correctly keeps the T1 marker (survivor) — but `forwardCompanionDismissal` runs unconditionally afterward, `byHostTerminal` returns the NEW pending question, `markStale` settles it permanently. Phone alert retracted, `/v1/answer` → 410, question unanswerable from companion forever, while desk red legitimately re-asserts. Directly violates the "question raised after dismiss must survive" invariant and FEATURES.md's own claim ("a question raised after the click survives").
Fix: Pass dismissStartedAtMs into dismissByTerminal; skip when `question.askedAtMs > dismissStartedAtMs` (PendingQuestion already carries askedAtMs). Also gate the call on `cleared.survivors.length === 0`.

[MAJOR] apps\desktop\src\renderer\...\useDashboardSidebarWorkspaceItemActions.ts:138 (applyWorkspaceStatusDismissal)
Defect: Renderer purge is workspace-wide and unfenced — `clearWorkspaceStatuses(workspaceId)` drops a red latched from a post-click question even when the host reports it `pendingAfter: true`; the flag only gates seen-marking, not the purge.
Failure: New PermissionRequest broadcast latches red between click and mutation reply. Reply applies → purge wipes that red. The blocked agent fires no further hook, so the red stays hidden until the 60s periodic resync re-derives it from the surviving marker. Up to a minute of green-looking dot over a blocked question. Compounding hole server-side (terminal-agents.ts:198): an owner created between `readdir` and return is never listed → `pendingAfter: false` → terminal also marked seen.
Fix: For terminals with `pendingAfter: true`, clear per-terminal axes except permission (reuse the axis-clear used by clearInterruptedTerminalAxes) instead of relying on the workspace-wide purge; or trigger an immediate resync after dismissal instead of waiting for the periodic tick.

[MAJOR] apps\desktop\src\main\lib\agent-jsonl-watcher\pane-map-hook.ts:2837 (retry gate) + _deliver
Defect: The companion strip-and-retry is dead code for its designed trigger: an HTTP 400 from the owning host raises urllib HTTPError inside `_deliver` (exception path — status never inspected), so `superset_saw_2xx` stays False and `has_companion and getattr(exc, "superset_saw_2xx", False)` skips the retry in the single-host case.
Failure: Companion capture shape vs route schema drift (the exact scenario the retry was built for, per the comment at :2830) → every Stop carries companionLifecycleEventId → every Stop 400s → strip-retry never fires → binding never updated → yellow stuck; resync replays the stale binding so it never heals. Worse with a second org host: owner 400s, foreign host answers 200 `ignored:true` → `_deliver` returns "ignored-everywhere" → main() logs a benign delivered-no-op and returns, no retry, no post-error. Breaks the documented guarantee that a payload change "can degrade the companion feature but can NEVER move a dot". No test covers 400→strip-retry (existing assertions only check the retry does NOT fire).
Fix: In `_deliver`, catch urllib.error.HTTPError separately and record a `saw_http_response` flag (and don't let an owner's 4xx be masked by a foreign host's 200-ignored); gate the strip-retry on "any candidate returned an HTTP response" rather than 2xx-with-unaccepted-body.

Checked and clean: SAFE_MARKER_SEGMENT guard centralized inside askqMarkerDir, no client-derived paths (terminal set is DB-sourced, terminalId only intersects it); unlink-per-owner, no recursive rm; ENOENT-tolerant, other errors abort loudly with completed-list; host `clearWorkspaceStatuses` mutation + store method byte-unchanged; marker mutations strictly before delivery (candidates resolved at pane-map-hook.ts:2797, after _decide_event_type); positive acceptance (`ignored is False`) matches sh-template parity incl. SUPERSET_HOME_DIR/org-first ordering; malformed/foreign bodies never count as delivered; interrupt path clears working/review/background only, never permission; resync retract branch is positive-evidence (`pendingPermission === false`) + workspace + occurredAt fenced; outstandingReadyAt untouched by renderer purge (test-asserted); periodic-resync interval cleaned on disconnect/unmount with in-flight + generation guards; clearV2TerminalRunStatus had zero callers at HEAD; react-mock spreads strengthen (not weaken) the two test files; no subprocesses in hook Python, no sync fs on Electron main; lifecycle-id dedupe safe under multi-candidate probing (first acceptance stops the loop, retry strips the id); FEATURES.md markers parse — `scripts/check-feature-markers.mjs` passes all 241. Note: `packages/host-service/src/companion/remote-pairing.test.ts` "(PAIR-EVICTION-HONEST)" fails on this machine, but it is a source-scan test broken by CRLF checkout of unmodified pairing.ts — pre-existing, unrelated to this diff.

Verdict: FIX-FIRST
</agent-message>

---

<agent-message from="fix-renderer">
All three fixes applied. Only my three files + their two colocated test files touched.

FIX-1 (BLOCKER) — `apps/desktop/src/renderer/routes/_authenticated/components/V2NotificationController/lib/resyncAgentStatus.ts`
Retraction branch gained a third conjunct: `row.lastEventType !== "PermissionRequest"`. While the host's own binding still says PermissionRequest, the absence of a marker is not evidence — a generic tool-permission red never had one. Both intended uses still fire (dismissal forces the binding to Stop; a delivered answer moves it to Start/Stop). Long comment records why the occurredAt fence can't cover it (T > T is false).

FIX-2 (BLOCKER) — `.../DashboardSidebarWorkspaceItem/hooks/useDashboardSidebarWorkspaceItemActions/useDashboardSidebarWorkspaceItemActions.ts`
`applyWorkspaceStatusDismissal` now re-latches the permission axis for every returned terminal with `pendingAfter: true`, via `applySourceAxes(set:["permission"])` at the host-returned `lastEventAt`, right after the workspace purge. Loop restructured: the `lastEventAt === null` skip moved to the top and now guards both branches — a pendingAfter with no binding is skipped rather than re-latched, because `applySourceAxes` would otherwise default to renderer `Date.now()` and plant a local-clock timestamp that the next resync's host-clock fence compares against. Documented. Added import of `getV2TerminalNotificationSource`.

FIX-3 (MINOR) — `.../V2NotificationController/components/HostNotificationSubscriber/HostNotificationSubscriber.tsx`
Serialization moved into `runResync`. `resyncInFlightRef` (boolean) replaced by `resyncInFlightTokenRef: number | null` (holds the generation) plus `queuedResyncKeyRef: string | null`. Mid-flight callers queue the latest key and return; the `.finally` releases only if it still owns the token, then drains the queued key. `runPeriodicResync` keeps its check as pure suppression (a tick carries the same key, nothing to add). Disconnect and unmount clear both refs so an abandoned request can't release the gate under the epoch that replaced it, and so a reconnect resync doesn't wait out a dead socket's timeout. Existing generation guard and the 10s retry path unchanged.

Tests
- `resyncAgentStatus.test.ts`: +1 test — `pendingPermission:false` + `lastEventType:"PermissionRequest"` at the same instant → no retraction, red survives at 5_000. Existing `pendingPermission:false` + Stop retraction test unchanged and still passing. 23 pass / 0 fail.
- `useDashboardSidebarWorkspaceItemActions.test.ts`: replaced the seen-mark-skip test with one asserting the red survives the whole dismissal flow (permission axis back at the host instant 6_000, status "permission", review axis and every other terminal's dot still purged, no seen mark, no companion report, outstandingReadyAt intact). +1 test for pendingAfter with null lastEventAt → no re-latch. 6 pass / 0 fail.
- `HostNotificationSubscriber.test.tsx`: +2 tests — key change mid-flight defers to after settle (no overlap), and several mid-flight key changes collapse to one follow-up. Existing periodic-suppression test kept. 6 pass / 0 fail.

Verification
- Combined run over V2NotificationController + DashboardSidebarWorkspaceItem + stores/v2-notifications (12 files): 154 pass / 0 fail. Ran together deliberately, since `mock.module` is process-global here.
- `tsc --noEmit` over apps/desktop: zero diagnostics naming any of the three files or their tests.
- `biome check` on the six files: clean, no fixes applied.
- No commits, no repo-wide formatter.

Invariants held: interrupt path and the store's `clearWorkspaceStatuses` untouched; `outstandingReadyAt` untouched; the occurredAt fence at the top of the resync loop unchanged.

Deviation: the "five files the previous implementer listed" weren't named in my brief, so instead of guessing I ran the three whole directories above. If that list included anything outside them, name it and I'll run it.
</agent-message>

---

<agent-message from="clean-hook">
All 11 items applied to the two hook files only. 60/60 tests pass, unmodified. Biome clean. Embedded script py_compiles. Feature-marker gate: 242/242 OK.

Files:
C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\main\lib\agent-jsonl-watcher\pane-map-hook.ts
C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\main\lib\agent-jsonl-watcher\pane-map-hook.test.ts

APPLIED

P1 (behavior restoration). New manifest probe renamed `_manifest_pid_alive` (~:2342); sole caller in `_manifest_candidate_urls` updated. The (BF) codex `_pid_alive` (~:736) is no longer shadowed, so `_codex_job_active` gets its int() coercion, pid<=0 guard and GetExitCodeProcess/STILL_ACTIVE check back. Cross-reference comment on each naming the other and why semantics differ (manifest fails OPEN, codex leans "still active").

P2. One `_rotate(log_path, backup_path)` (~:407) used by `_log` and `_decision_log`; `Path.replace` killed the exists/unlink dance. Rotation is checked once per process PER PATH via a module-level `_ROTATED` set — one mechanism covers both callers, so `_decision_log` adopted the same flag rather than keeping per-call (it writes at most a handful of lines per process). 14 duplicated lines -> 1 helper.

P3-P7 (`_deliver` + main tail).
- Promotion moved into `_deliver`: a disown sweep carrying an http_error now raises. Both duplicated blocks in main are gone, including the dead `retry_after or outcome[4]`.
- `_HookOutcome = collections.namedtuple(..., kind/url/status/body/http_error)`; added `import collections`.
- `_HookUndelivered(RuntimeError)` with real `saw_response` field and `__cause__`. A `from_sweep` classmethod keeps the old message rule (the transport exception's own str() when there is one) so the post-error / retry log fields are byte-identical.
- `_hook_accepted`/`_hook_ignored` merged into tri-state `_hook_ignored_flag(raw)` -> True/False/None. One parse per response instead of two. No wrappers kept — `_deliver` compares `is False` / `is True` inline (only call site).
- `saw_ignored` dropped, derived from `last_ignored_body is not None`.
- `superset_tried_urls` dropped.

Invariant care worth flagging: I ordered the promotion check INSIDE the `last_ignored_body is not None` branch, not before it. Putting it first would have changed the message on the no-disown-plus-400 path from "HTTP Error 400: Bad Request" to "HTTP 400 from <url>" — a real log-field change the tests would not have caught.

P8-P11 (fast path).
- `_HookCandidates` is a lazy, re-iterable candidate source: env URL yielded first, manifest tail enumerated only when a second candidate is pulled. Verified directly: after pulling candidate 1 the tail cache is still None, so an accepted first POST touches no disk at all.
- Tail memoized at module level (`_MANIFEST_CANDIDATES`), so the companion retry sweep re-iterates for free.
- Path-level `seen_paths` set: own-org manifest no longer parsed and probed twice.
- Candidate URL built and deduped BEFORE the pid probe; verified env-URL==own-org yields one probe-free entry.
- `_kernel32()` lazy module singleton replaces per-call `ctypes.WinDLL` + prototype declaration.
- `tried` is now an in/out list owned by main and passed to both `_deliver` sweeps (deduped across them). It replaces `candidates` in all four log sites, since a lazy source has no full list to name. Budget-exhausted log drops `candidateUrls` and its message counts attempts rather than "X of Y" — the accepted logging change. The all-dead post-error and dot-decisions lines still name every candidate tried; that test passes unchanged.

Test file: `hook()` gained an optional `envOverride` param (spread last, wins over defaults); `runHook` is now a 1-line delegate passing `SUPERSET_AGENT_WATCHER_DEBUG: "1"` plus the caller's env. ~37 duplicated lines removed. Safe because every runHook call site already sets its own SUPERSET_HOST_AGENT_HOOK_URL.

SKIPPED / DEVIATIONS
- No test assertions changed. Zero test edits were needed — the only test-file change is the harness merge above.
- `_HookOutcome.http_error` is kept as specified but is now read by nobody: with the promotion inside `_deliver`, a returned ignored-everywhere always carries None, and main never reads it on the delivered path. It is a dead field. Flagging rather than removing, since the 5-field shape was specified explicitly. Say the word and I will drop it to 4 fields.
- `_deliver`'s outer catch in main stays `except Exception` (not `except _HookUndelivered`) so an unexpected error still cannot crash the hook; the retry gate uses `isinstance(exc, _HookUndelivered) and exc.saw_response`.

Final: 60 pass / 0 fail / 376 expect() calls across 2 files.
</agent-message>

---

<agent-message from="mb-adopt">
Part A complete. Not committed. Only the 4 permitted files touched (+2 new tests). FEATURES.md untouched.

CHANGES

A1 `C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\runtime\main-workspace-sweep.ts`
- `runMainWorkspaceSweep(ctx, probeIsGitRepo = isGitRepo)` — second param injected, same idiom as archived-workspace-reconcile's `exists`.
- Loop now: `const isRepo = await probeIsGitRepo(row.repoPath); await ensureMainWorkspace(ctx, row.id, row.repoPath, { nonGit: !isRepo });` under a (MASTER-ALWAYS-ACTIVE) comment naming the swallowed PRECONDITION_FAILED.
- Skip guard widened to `!row.repoPath || !existsSync(row.repoPath)` so an empty/absent path can't reach `existsSync`.
- Chose `{ nonGit: false }` for the git case rather than omitting opts (spec allowed either); `ensureMainWorkspaceStrict` reads `opts?.nonGit` truthily, so identical.

A2 `...\v2-workspace\$workspaceId\components\V2WorkspaceView\V2WorkspaceView.tsx:132-138`
- `useAutoAdoptBackgroundSessions({ store, workspaceId, isLayoutReady });` immediately after `useV2WorkspacePaneLayout()`, with a (MASTER-PLUS-LAUNCH) comment naming 066f6f2f77 and upstream ebe0144bfb and stating it is the ONLY mount.

A3 `...\hooks\useAutoAdoptBackgroundSessions\useAutoAdoptBackgroundSessions.ts`
- 6 marker-tagged lines after `isFetchingSessions`: `workspaceTrpc.useUtils()`, a `useCallback` invalidate, `useWorkspaceEvent("terminal:lifecycle", workspaceId, invalidate, isLayoutReady)`. Signature verified against the real overloads (type, workspaceId, callback, enabled) and matches useTerminalAgentBindings:58-66. Import added: `renderer/hooks/host-service/useWorkspaceEvent`.

TESTS (10 new, all passing)

`packages\host-service\src\runtime\main-workspace-sweep.test.ts` — 6 tests. Real in-memory drizzle DB + migrations (createTestDb pattern from project-import.test.ts), so these assert OUTCOMES not call shapes: git project → HEAD read, branch `feature-x`; non-git → branch `__superset_non_git__`, name = folder basename, zero git calls; mixed projects judged per-path; idempotent across two sweeps; missing and empty repoPath both skipped before the probe runs. Deviation from spec: no mock of `ensureMainWorkspace` — the behavioural assertion is strictly stronger and avoids a process-global `mock.module` in host-service.

`...\hooks\useAutoAdoptBackgroundSessions\useAutoAdoptBackgroundSessions.test.tsx` — 4 tests. Real component harness (happy-dom + @testing-library/react, the HostNotificationSubscriber.test.tsx pattern; no renderHook). Main test is exactly A4: settled empty list → no panes → fire `terminal:lifecycle` → invalidate called with `{workspaceId}` → refetched list lands → pane for `terminal-1` exists. Plus: subscription is disabled until `isLayoutReady`; no adopt while `isFetching`; re-adopt adds no second pane.

DEVIATIONS / LIMITS
- The renderer test mocks two modules via `mock.module` (`@superset/workspace-client`, `renderer/hooks/host-service/useWorkspaceEvent`), each spreading the real module and overriding one export. Leakage checked, not assumed: full `bun test` in apps/desktop with and without the new file gives the IDENTICAL 40-failure set (same test names, all pre-existing/env-specific — resolvePath, homebrew PATH, real-git worktree suites). Nothing new.
- That test asserts the hook subscribes with the right type/workspaceId/enabled and that invalidate→refetch→pane works. It does NOT drive the real event bus — the real `useWorkspaceEvent` resolves the host through the auth/cloud provider tree, which no unit harness has.
- Markers `(MASTER-ALWAYS-ACTIVE)` and `(MASTER-PLUS-LAUNCH)` now exist in code but are NOT in FEATURES.md (you told me not to touch it). Someone must add them or the manifest and the code disagree.

VERIFICATION RUN
- `bun test src/runtime/main-workspace-sweep.test.ts` → 6 pass / 0 fail.
- `bun test <auto-adopt test>` → 4 pass / 0 fail.
- Full `bun test` packages/host-service: 105 fail with my changes vs 109 on the HEAD version of the two files (restored by hand, no stash). Diff is removals only — zero new failures. These suites are flaky/env-dependent on Windows.
- `bunx tsc --noEmit` both packages: only pre-existing debt. The single V2WorkspaceView diagnostic (`trailing` prop on V2PresetsBar, now line 337) exists at line 330 in HEAD — my 7 added lines just shifted it. Zero diagnostics on the hook or either test file.
- `node scripts/check-dangerous-diagnostics.mjs` → (REFERR-GATE) clean, all 7 packages.
- Biome check --write applied to all touched files, clean.

MANUAL VERIFICATION STILL REQUIRED
1. A2/A3 end-to-end: desktop running, a v2 workspace open and idle (list settled). From outside, `superset agents create --workspace <id> --agent claude --prompt "..."`. A terminal pane for that session must appear in the OPEN workspace within a second, without closing/reopening it. Repeat with `superset ws create --agent claude` then opening that workspace (mount-time path).
2. A1 end-to-end: a non-git project (plain folder) whose main workspace row has been deleted; restart host-service. The boot sweep must recreate a `type='main'` row with branch `__superset_non_git__` and the folder name as its name, and log no warning. Before this fix that row could never come back.
</agent-message>

---

<task-notification>
<task-id>afaad4b1421c7e124</task-id>
<tool-use-id>toolu_01TA2jxkc8v6qZM3osUqkouA</tool-use-id>
<output-file>C:\Users\khair\AppData\Local\Temp\claude\C--Users-khair-Code-Projects-fork-superset-windows-arm64\1965f49a-6bb1-4b13-95f6-52f27fa5ae35\tasks\afaad4b1421c7e124.output</output-file>
<status>completed</status>
<summary>Agent "Integrate master feature" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Integration complete — report:

**Task: integrate the master-workspace feature (FEATURES.md, stale test, gates).**

1. **FEATURES.md** — two feature rows added after the `(MASTER-ARCHIVE-ONLY)` row, `(REMOVE-STICKY)` row amended with the mains exemption, three marker lines added. Marker gate: **All 249 feature markers present**, exit 0.
2. **Stale test fixed** — `sidebarMutations.test.ts` "clears the pin on a kept main-workspace row" rewritten as "tombstones a main-workspace row with its pin cleared": now asserts `isHidden === true` (tombstoned) + `pinnedAt` null, comments reference `(REMOVE-STICKY)` tightening and `(MASTER-ALWAYS-ACTIVE)` as the return path. Passes.
3. **REFERR gate** — clean, 0 dangerous diagnostics across all shipped packages.
4. **Tests** — host-service `src/runtime`: sweep suite 6/6 green; 19 failures in the directory are all pre-existing env-dependent suites (git identity, PR runtime, gh CLI, path config), proven by a stashed HEAD run: **22 fail at HEAD vs 19 with the changes** — the 3 recovered are exactly the sweep tests the `{nonGit}` fix makes pass; zero new failures. Renderer: all six touched areas **139 pass / 0 fail** (AgentHooks + reconciler, useDashboardSidebarState incl. the repaired test, dashboardSidebarLocal, DashboardNewWorkspaceModal incl. PromptGroup + resolveMasterMode, useAutoAdoptBackgroundSessions, useProjectGitState).
5. **Typecheck** — host-service: zero diagnostics in touched files. Desktop: the single V2WorkspaceView diagnostic (`V2PresetsBar` props, now line 337) is the documented pre-existing debt from HEAD line 330, shifted by the 7 added lines; nothing new.
6. **git status** — change set is exactly the three builders' files + FEATURES.md + sidebarMutations.test.ts (11 modified, 5 new). Nothing unexpected.

Nothing committed. All gates pass; tree is ready for code review.</result>
<usage><subagent_tokens>468345</subagent_tokens><tool_uses>17</tool_uses><duration_ms>278106</duration_ms></usage>
</task-notification>

---

<agent-message from="cr3-sol">
[BLOCKER] C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\renderer\routes\_authenticated\_dashboard\v2-workspace\$workspaceId\hooks\useAutoAdoptBackgroundSessions\useAutoAdoptBackgroundSessions.ts:61
Defect: The new invalidation subscribes to `terminal:lifecycle`, but the host emits that event only for exit and OSC 133 command-start/command-end, never when a terminal session is created.
Failure: With the supported cmd.exe fallback, `agents.run` creates and starts the agent terminal but cmd has no OSC 133 scanner (`SHELLS_WITH_READY_MARKER` excludes cmd), so no lifecycle event fires until exit. If the workspace was already open with a settled empty terminal query, the query never refetches, no pane appears, and the agent runs invisibly. A bare externally-created `terminal.createSession` has the same failure on every shell because creation emits no event. The new test fabricates a lifecycle event and therefore misses the producer gap. Host evidence: `packages/host-service/src/terminal/terminal.ts:3365-3395` records creation without broadcasting; broadcasts exist only at 3627/3640/3654 and 3724.
Fix: Add and consume a real terminal-created lifecycle event after the active session row is committed, or explicitly invalidate the terminal list from every successful external creation path; test the real host producer through the client instead of manually invoking the callback.

[BLOCKER] C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\runtime\main-workspace-sweep.ts:38
Defect: A transient git-probe failure is converted to `nonGit: true`, so the sweep rewrites existing git main workspaces as non-git.
Failure: If Git cannot be launched during startup, `isGitRepo` catches the error and returns false (`runtime/git/non-git.ts:62-67`). The sweep passes `{nonGit:true}`; `ensureMainWorkspaceStrict` computes `__superset_non_git__` before querying the existing row and then updates its branch and possibly its name (`ensure-main-workspace.ts:89-92,114-131`). A normal git project is persisted as non-git and git paths stop treating it as a repo, violating the stated unchanged-git-project invariant.
Fix: Do not use a boolean that conflates confirmed non-repo with probe failure to update an existing row. Preserve the old git path for existing mains and probe only when rebuilding a missing main, or make the probe tri-state and skip on unknown.

[MAJOR] C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\renderer\routes\_authenticated\_dashboard\kanban\hooks\useProjectGitState\useProjectGitState.ts:91
Defect: `isError` ORs the git and multi-repo query errors even after the multi-repo query becomes irrelevant and disabled, while TanStack Query retains the disabled query's prior error state.
Failure: On first modal render the host workspace list can be empty, enabling `getMultiRepoInfo`; if that probe exhausts retries before hydration and errors, then the main row arrives and the git probe succeeds as non-git. `multiRepoEnabled` becomes false but `multiRepoProbeErrored` stays true (verified with QueryObserver: disabling an errored query leaves status `error`/`isError=true`), so resolver rung 7 permanently returns branch mode. Submit then calls `workspaces.create`, which rejects non-git projects with `Cannot create a branch/worktree in a non-git workspace.`
Fix: Report only the currently relevant probe error, e.g. `mainWorkspaceId ? gitProbeErrored : multiRepoProbeErrored`, and add a hydration-after-multi-probe-error test.

Checked clean: hidden-main bucket gating and host/org/type scope, row-less-main exclusion, sidebar type-resolution retry, resolver rung order, master submit zod shape/order/double-submit guard, feature markers, and sidebar/mode/host unit tests.
</agent-message>

---

Can you double-check for me that, with any of the notifications around the questions, if I answer the question on the desktop, the notifications go automatically? Same with if we get notified that the chat has ended or the turn has finished for the agent. If I then interact with that terminal (So what would normally clear the green dot) It also gets rid of the notification.

---

<agent-message from="notif-verify">
# Notification retraction verification

Fork @ `060a4971f6`, companion @ `4e1bf82`. Read-only. Renderer/host uncommitted work checked — companion chain is clean except two files noted at the end.

Terminology correction up front: companion has only TWO lifecycle kinds — `g` READY and `e` ERROR (`core/.../protocol/AlertNotificationText.kt:103-106`). "Turn finished / ready for review" IS `g`. No third kind.

---

## Q1 — QUESTION notification, answered on the DESKTOP

### Structured AskUserQuestion answered at the desk → **WORKS, phone AND watch**

Chain, every hop verified:

1. `PostToolUse:AskUserQuestion` → `_companion_resolved` → `{toolUseId}` — `apps/desktop/src/main/lib/agent-jsonl-watcher/pane-map-hook.ts:2787-2800`, attached at `:3002-3004`.
2. Hook POST → `forwardCompanionCapture` → `sink.resolve({hostTerminalId, toolUseId, resolvedAtMs})` — `packages/host-service/src/trpc/router/notifications/companion-question-sink.ts:333-339`.
3. Bridge sink → `deps.inner.resolve(input)` — `packages/host-service/src/companion/index.ts:1804`.
4. Store `resolve()` → `settle()` → `deps.onSettled(question)` — `packages/host-service/src/companion/question-store.ts:1618`. This is `(SETTLE-CHOKE-POINT)`: the ONLY place a terminal state is written, so no settle route can forget.
5. `onSettled` → `sender.cancelPending(questionId)` — `packages/host-service/src/companion/index.ts:669-683`.
6. `cancelPending` → `sendRetraction` → `buildRetractPushData` → FCM broadcast — `packages/host-service/src/companion/push.ts:2773-2782`, `:2475-2507`. Cancels an in-flight original (`:2489-2492`) and queues behind it on the same per-question chain, so retraction can't be overtaken.
7. Phone: v1 `k="r"` → `PushRouting.kt:59-60` → `CompanionNotifications.retract()` → `manager.cancel(questionId, ID_QUESTION)` — `app-phone/.../notifications/CompanionNotifications.kt:384-386`. Matches on `i` alone.
8. Watch: same route line `:60` → `tellWatchResolved` → `WatchBridge.pushResolved` (`WatchBridge.kt:250-266`) → `/companion/v1/question/resolved` → `app-watch/.../datalayer/CompanionWearListenerService.kt:75-80` → `QuestionNotifier.cancel`.

Desk answers are the ORIGIN of this path, not an afterthought — `_companion_resolved`'s own comment says so (`pane-map-hook.ts:2788-2792`).

### Plain typed answers (prose, not the tool) → **no notification exists to retract**

`_companion_question` returns `None` unless `PreToolUse` + `AskUserQuestion` (`pane-map-hook.ts:2740-2742`). Free-text prompts and permission prompts never mint a companion question record, so they never buzz. Not a gap — out of scope by construction.

### Dead hook / interrupted / aborted question → **WORKS, but only while the phone is polling**

`reconcile()` re-reads the transcript, finds the `tool_result`, and settles `resolved` with `surface: "desktop"` — `question-store.ts:2116-2121`. Same settle seam → same retraction. Terminal killed with no answer: settles `stale` after TWO observations `QUESTION_EXPIRY_CORROBORATION_MS` = 5 min apart (`question-store.ts:1470`, `:2096-2110`), also retracts.

**Caveat:** `reconcile()` is called from exactly one place — the `/v1/heartbeat` handler, `packages/host-service/src/companion/read-api.ts:1675`. It is phone-driven, not a host timer. A phone that has stopped heartbeating never triggers the backstop. That is the case where the backstop is needed AND the case where it doesn't run.

### Manual dismiss (right-click Clear Status) → **WORKS**

`forwardCompanionDismissal` → `dismissByTerminal` → `markStale` → settle → retract — `companion-question-sink.ts:362-372`, `index.ts:1862-1890`. Correctly `markStale` not `resolve` (no provenance to fabricate). Fenced on `dismissStartedAtMs`: a question raised at-or-after the click is left alone (`index.ts:1863-1875`), so a fresh question isn't retracted out from under a blocked agent.

### Partial keystroke write from the phone → **WORKS**

`answer.ts:1267` `markStale` on unconfirmed write → settle → retract.

---

## Q2 — READY (green dot) notification, interacted with on the DESKTOP

### **WORKS** for the interactions that actually report

Chain:

1. Desktop → `markTerminalSeenAndReportRead` — `apps/desktop/src/renderer/routes/_authenticated/components/V2NotificationController/lib/companionAlertSync.ts:371-413`. Clears green locally (`markTerminalSeen`, `stores/v2-notifications/store.ts:358-388`) and reports `seenThroughAt`.
2. → `companion.markLifecycleSeen.mutate` (`companionAlertSync.ts:449-454`) → `packages/host-service/src/trpc/router/companion/companion.ts:398-408` → `packages/host-service/src/companion/index.ts:917-956`.
3. → `lifecycle-alerts.ts:1176-1275`: `findReadAlerts` → `retireAlert(alert, {kind:"seen"})` (`:1261`) → `notifiesPhone` true for `seen` (`:122-124`) → `fireRetraction` → `push.sendLifecycleRetraction` (`:653-680`).
4. Blind fallback if no held alert (survives a host restart): `retractById` with a recomputed id, `:1264-1274`.
5. Phone: v3 `k="c"` → `LifecycleRouting.kt:278-289` → `SeenLifecycleEvents.suppress` + `CompanionNotifications.cancelNotification` (`:297-311`) + `dismissOnWatch`.
6. Watch: `WatchBridge.pushLifecycleDismiss` → `/companion/v1/lifecycle/dismiss` → `CompanionWearListenerService.kt:121-128` → `LifecycleNotifier.cancel`.

**Which desktop interactions report — exactly three:**

| trigger | file:line |
|---|---|
| Active pane changes in the open workspace (open workspace, switch tab, click a different pane) | `.../v2-workspace/$workspaceId/hooks/useClearActivePaneAttention/useClearActivePaneAttention.ts:58` |
| Sidebar "Mark read" | `.../useDashboardSidebarWorkspaceItemActions.ts:226` |
| Right-click → Clear Status | `.../useDashboardSidebarWorkspaceItemActions.ts:176` |

**Do NOT report:** window focus, typing in the terminal, scrolling, reading an already-active pane without a pane change. If the pane was already active when the green arrived, the effect does re-fire on the status flip (`useClearActivePaneAttention.ts:64` deps) — even with the app backgrounded, which is a false-positive retraction, not a linger.

### Green cleared by NEW work (agent starts again) → **WORKS as designed, alert stays up on purpose**

Renderer: `statusTransitions.ts:76-85` deletes the review entry but leaves `outstandingReadyAt` standing (`store.ts:586-599`, `:750-755`). Host: `outcome:"progress"` → `supersede({kind:"new-cycle"})` (`lifecycle-alerts.ts:1145-1147`) → `notifiesPhone` returns **false** for `g`+`new-cycle` (`:122-124`) → the card is left `standing` for the next `g` to replace in place. That is the `(ONE-BUZZ-UNTIL-READ)` contract. The later pane-open reports via the surviving `outstandingAt` and retracts then.

### GAPS on the ready path

**GAP R1 — terminal exit / pane close. Unrecoverable.**
`lifecycleEvents.ts:194-202` calls `pruneTerminalSeen` (deletes `outstandingReadyAt`, `store.ts:402-406`) and `clearSources`, with no report. The host retracts only on a hook-delivered `outcome:"session-end"` (`lifecycle-alerts.ts:1149-1150`), whose only producer is the agent's own notify hook. A pty killed/crashed/closed with no SessionEnd hook → phone keeps the ready card to TTL, and the local evidence needed for the resync repair is destroyed.

**GAP R2 — app restart.**
Store is sessionStorage-backed (`store.ts:835`), so a real restart starts empty. Resync mints then immediately deletes the outstanding record (`resyncAgentStatus.ts:485-487`) and cold-start-seeds the green away (`:509-520`). Nothing reported, and the repair block can't fire (`:558`, `:631-635` both false). A later pane-open bails at `companionAlertSync.ts:395`. An unread green older than the host session boundary is orphaned on the phone for its TTL. Documented `(DOT-PERSIST)` tradeoff, but it is a clear-without-report.

**GAP R3 — workspace deletion (soft-delete to Recycle Bin).**
No renderer path reports on delete; the row just leaves the sidebar. Terminals then die → GAP R1.

**Latent:** `useMarkSidebarWorkspaceTerminalsSeen` (`DashboardSidebarWorkspaceStatusProvider.tsx:374-387`) clears green with the non-reporting variant. Currently has no callers.

---

## Third alert type — terminal-agent FAILURE (`e`)

**GAPPED.** `findReadAlerts` filters `alert.kind !== "g"` (`lifecycle-alerts.ts:1044`) and the blind fallback hard-codes `kind: "g"` (`:1268`). Desktop "seen" can never retract a failure alert. Only `session-end` or TTL takes it down. Explicitly intentional per the comment at `:118-120` ("an error the user has not seen is exactly the thing that must not be quietly folded away") — but the lived consequence is that opening the failed chat on the desktop leaves the phone and watch failure alert standing.

Note the asymmetry at `:1160-1173`: a `failed` on top of a PermissionRequest DOES mint an `e`, so a blocked agent that dies produces a failure alert with no seen-retraction.

---

## Lingering edge cases

**Watch, question resolution — no retry queue.** `pushResolved` is one-shot (`WatchBridge.kt:250-266`). `NoWatchPaired` (`WatchLink.kt:127`) or `Failed` (`:150`) → wrist card survives. `QuestionNotifier.post` sets **no** `setTimeoutAfter` (`QuestionNotifier.kt:123-131`), and `onHello` replays lifecycle dismissals but not resolutions (`WatchBridge.kt:546-562`). Card clears only on user tap or swipe — indefinitely otherwise. Sharpest asymmetry in the design: the lifecycle dismissal has a 24 h durable queue, the safety-critical question resolution has none.

**Watch, lifecycle — bounded.** `PendingLifecycleDismissals` (SharedPreferences, `commit()`, cap 256, TTL 24 h) queues before sending and clears only on clean delivery (`WatchBridge.kt:341-342`, `:381-385`). Replayed from `onHello` (`:553`) — i.e. when the watch app is FOREGROUNDED, not merely reconnected. A watch that is connected but whose app is never opened gets nothing until `setTimeoutAfter` expires the card.

**Phone offline at retraction time — questions OK.** Retraction shares the question's `collapse_key` (`PROTOCOL.md:2635-2636`), so a waking phone receives only the retraction; the cancel is a harmless no-op. I did **not** verify collapse behaviour for the lifecycle `c` frame — treat that as unchecked.

**Phone re-buzz after retraction.** `retract()` writes no negative record (contrast lifecycle's `SeenLifecycleEvents.suppress`). If a duplicate/late question push arrives after the retraction and the bridge is unreachable, `CompanionMessagingService.kt:344-364` posts a contentless card with no state check — a fresh buzz for an already-answered question. Cleared only by the foreground sweep.

**Foreground sweep covers questions ONLY.** `CompanionNotifications.sweep` filters `it.id == ID_QUESTION` (`CompanionNotifications.kt:396-406`), driven from `SessionViewModel.kt:183,216`. Lifecycle cards live under a different id precisely so the sweep can't see them (`:494-495`); their only backstop is `setTimeoutAfter` (`:271`).

**Lifecycle cancel has a structural hole.** A READY card is posted under tag `t:<terminalId>` but the two direct cancels address `e:<eventId>` and the raw eventId (`CompanionNotifications.kt:298,303`). The terminal-keyed card is reachable only via `forgetRetracted`'s return value; if the shown-alert record was evicted (cap 64, `ShownLifecycleAlerts.kt:196`) or aged out, it returns `null` (`:185`) and the ready card is not cancelled. Mirrored on the watch (`LifecycleNotifier.kt:187-202`). Also `forgetRetracted` checks terminalId before eventId (`:173`), so an ERROR retraction carrying a terminal that holds a live READY collaterally cancels the READY.

**Lost-contact notification** (`CompanionNotifications.kt:349-379`) has no FCM retraction at all, no sweep coverage, no `setTimeoutAfter`, no watch relay. By design — raised locally by the phone.

---

## Uncommitted-work flags

`lifecycleEvents.ts` and `V2WorkspaceView.tsx` are modified in the working tree, but the specific lines GAP R1 and the pane-change trigger rest on are committed and unchanged. Everything else cited (`store.ts`, `companionAlertSync.ts`, `resyncAgentStatus.ts`, both sidebar hooks, all of `packages/host-service/src/companion/*`) is clean.

</agent-message>

---

<agent-message from="arch-notif">
# Blueprint — companion notification-gap fixes (FIX-1/2/3)

All line refs verified by reading. Two repos:
FORK = `C:\Users\khair\Code Projects\fork-superset-windows-arm64`
COMP = `C:\Users\khair\Code Projects\superset-companion`

## 0. Corrections to the brief (read first)

**R3 does NOT collapse into R1.** Soft-delete leaves terminals alive — `COMP`… no, FORK: `C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\renderer\routes\_authenticated\_dashboard\components\DashboardSidebar\hooks\useBulkWorkspaceSoftDelete\useBulkWorkspaceSoftDelete.ts:11-13` states the bin move is "worktree, branch and **terminals untouched**". So no exit fires, R1 never triggers, and the phone keeps a ready card for a binned thread. R3 needs its own retirement path (curation-driven, below). Hard delete does dispose (`apps\desktop\src\renderer\react-query\workspaces\useDeleteWorkspace.ts:96-99` → `terminal.disposeWorkspaceSessions` → `packages\host-service\src\terminal\terminal.ts:2939`), so hard delete → R1.

**No new key may be added to the `c` push frame.** `COMP\core\src\main\java\family\khaira\supersetcompanion\core\protocol\LifecyclePush.kt:234-241` validates the `c` key set with `requireExactKeySet` — an extra key is a hard `ProtocolException` on every shipped phone, i.e. the retraction is dropped and the card is stranded. FEATURES.md restates this ("exact-equality in both directions"). Every wire-level fix below therefore uses existing keys only.

**Pre-existing bug found, fixed as part of FIX-3's addressing work.** `e` retractions today carry a real `t` (`packages\host-service\src\companion\lifecycle-alerts.ts:665`), and the phone's `forgetRetracted` (`COMP\core\...\notify\ShownLifecycleAlerts.kt:173-181`) treats a non-empty `t` as "retire the terminal-collapsed READY row up to `gx`". `notifiesPhone` returns true for `e` + `new-cycle` (`lifecycle-alerts.ts:122-124`), so today: agent errors → agent starts new work → the `e` retraction silently cancels the user's unread standing READY card. Live now, independent of FIX-3.

**Ambiguity + assumption (FIX-3 blind fallback).** The user asked for `e` to retract on desktop-seen. I widen the live-map path (covers everything the host still holds) but keep the blind restart fallback `g`-only. Hashing both kinds doubles blind broadcasts on every warm reconnect, which is the exact harm documented at `lifecycle-alerts.ts:1225-1233` (the phone's 64-slot `SeenLifecycleEvents` window evicting real tombstones). Residual stated in §5. Say the word if the user wants both.

---

## 1. Recommended approach

### FIX-1 — durable watch question-retraction (COMP only, no wire change)

Clone the `PendingLifecycleDismissals` shape for resolutions, and give the wrist card a self-timeout.

* Durable queue `PendingQuestionResolutions` (SharedPreferences, `commit()`, cap 256, TTL 6 h), enqueue-before-send, clear on clean delivery, replay on `onHello` — byte-for-byte the same discipline as `COMP\app-phone\...\wear\PendingLifecycleDismissals.kt` and `WatchBridge.kt:335-387`.
* Persist only `questionId | enqueuedAtMs | resolvedAtMs | resolvedBySurface | outcome`. **`resolvedByLabel` is deliberately not persisted**: all three `pushResolved` callers pass `null` (`ConversationViewModel.kt:518-524`, `SessionViewModel.kt:231-237`, `CompanionMessagingService.kt:209-215`) and nothing on the wrist renders it (only `resolvedBySurface`, `app-watch\...\ui\AnswerStateScreen.kt:283`). Keeps the `:`/`,` delimiter encoding safe — every persisted token is base64url or a fixed enum word.
* TTL 6 h, not the dismissals' 24 h: it matches the question staleness horizon and the new wrist timeout, so a queued entry can never outlive the card it cancels. (24 h for symmetry is the alternative; 6 h is self-consistent, pick it.)
* Belt: `setTimeoutAfter` on the wrist question card, computed from `askedAtMs + QUESTION_STALE_AFTER_MS`. `QUESTION_STALE_AFTER_MS = 21_600_000` mirrors the bridge's `PUSH_QUESTION_EXPIRY_MS` (`packages\host-service\src\companion\config.ts:377`). Rejected alternative: carry the push's `x` to the watch on `WatchQuestion` — two of the three post paths (`pushQuestionById`, `onHello`) have no push in hand, so it would need the constant anyway; one mechanism beats two.

Why not "replay by re-fetching at hello" (call `pushQuestionById` for queued ids): it needs the desktop reachable at exactly the moment the watch reconnects, which is the case the queue exists for.

### FIX-2 — stuck ready alerts (host-side authority, FORK; one shared Kotlin fix, COMP)

Three producers, three answers, all authored where the fact is known:

* **R1 (terminal died)** — one choke point. Every exit broadcast in the tree funnels through `EventBus.broadcastTerminalLifecycle` (`packages\host-service\src\events\event-bus.ts:224`); the nine call sites are all in `terminal\terminal.ts` and `terminal\reaper\reaper.ts`. Hook a fork-only companion sink there, gated on `eventType === "exit" && confirmed !== false`, and have it call a new `LifecycleAlertManager.retireTerminal()` which is just `supersede(id, { kind: "session-end" })` — the existing, already-correct retirement. **No renderer change**, so nothing collides with the fixer's in-flight `lifecycleEvents.ts` edit.
  Rejected: calling from each of the 9 broadcast sites (silently incomplete the first time someone adds a tenth). Rejected: an `EventBus.onTerminalLifecycle` in-process listener like `onWorkspaceChanged` (`event-bus.ts:258-266`) — better dependency direction, but it needs the `EventBus` threaded into the bridge through both mounts (`serve.ts:134`, `apps\desktop\src\main\host-service\index.ts:154`) for no behavioural gain. The registry sink slot is what `registry.ts:253` exists for and answers "nobody consumed it" when the bridge is off.
  Rejected: polling `liveness.isProvablyGone` from the alert sweep — up to ~90 s late, silent when the daemon is unreachable, and the sweep only runs while a `held` alert exists (`lifecycle-alerts.ts:601-615`).

* **R2 (app restart)** — renderer supplies the one fact only it has, host decides what to retract. New one-shot mutation carrying the cold-start boundary the resync already computes in **host clock** terms (`resyncAgentStatus.ts:393-407`); the host retires `g` alerts with `outcomeAtMs < boundary`. One call per host per launch, zero blind broadcasts, no per-terminal `reportTerminalSeen` storm — which is what `companionAlertSync.ts:419-423` and `lifecycle-alerts.ts:1225-1233` explicitly forbid.
  Rejected: inferring the cold start host-side from `sidebar_mirror_meta.appLaunchId`. `APP_LAUNCH_ID` is `crypto.randomUUID()` at module scope (`...CollectionsProvider\dashboardSidebarLocal\sidebarVisibility.ts:73`) so it changes on a renderer **reload**, where sessionStorage survives and the desktop keeps its green dot — the host would retract a card the user has not read. Wrong proxy.
  Rejected: renderer reporting seen for every cold-seeded terminal — the documented mass-retraction the design forbids.

* **R3 (soft delete / any curation-off)** — the mirror is the authority and already reaches the bridge. On a mirror change, retire live alerts whose workspace is now curated off. The hold rule already exists (`lifecycle-alerts.ts:639`, `createLifecycleCurationProbe`); this applies the same rule to the states the hold cannot reach (`sent`/`standing`). Hook: the existing `mirrorChangeSink` at `packages\host-service\src\companion\index.ts:1222-1239`.

* **Phone addressing (shared with FIX-3, MUST)** — `forgetRetracted` returns a key or `null`, and `null` conflates "no record" with "stale retraction" (`ShownLifecycleAlerts.kt:171-189`). Cap-64 eviction (`:142`) or an expired row therefore leaves the terminal-keyed card uncancellable. Make the verdict three-valued and cancel the terminal key on **Unknown** only — an unknown record is ignorance, and a stuck card is worse than one extra cancel (the next finish reposts and buzzes). Keep the generation guard on **Stale**, which is the whole point of `gx`.

### FIX-3 — error alerts retract on desktop-seen (FORK + COMP + PROTOCOL)

* Drop the `kind !== "g"` filter in `findReadAlerts` (`lifecycle-alerts.ts:1044`). `notifiesPhone` already returns true for `e` + `seen`, so nothing else in the state machine changes.
* Keep the blind fallback `g`-only (`:1264-1274`), documented (see §0).
* **`fireRetraction` sends `t: ""` for `kind === "e"`.** This is the precise-addressing fix, and it is wire-legal today: PROTOCOL §13.5.1 already says `t` "may be `""` on `q`, `e` and `c`", and both `sanitizeTerminalHandle` (`push.ts:771-775`) and the phone parser (`LifecyclePush.kt:241,247`) accept empty. An `e` card is always event-keyed on both devices (`LifecycleAlertIdentity.key`, `ShownLifecycleAlerts.kt:30-35`), so an `e` retraction has no terminal-collapsed row to name — and sending one is the pre-existing collateral bug in §0.
* Record the user's override in the `notifiesPhone` doc (`:118-120`) and in `markLifecycleSeen`'s "ONLY `g`" note (`:1197-1201`), dated 2026-08-20.

---

## 2. Files to create / modify

### FORK — host-service

| Path | Change |
|---|---|
| `packages\host-service\src\companion\lifecycle-alerts.ts` | **MUST.** Add to `LifecycleAlertManager`: `retireTerminal(hostTerminalId)` (→ `supersede(id,{kind:"session-end"})`), `retireReadyBefore(boundaryMs)` (walk alerts, `kind==="g" && outcomeAtMs < boundaryMs` → `retireAlert(alert,{kind:"seen"})`), `retireCuratedOffAlerts()` (walk alerts, `deps.isCuratedOff(alert.hostWorkspaceId)` → `retireAlert(alert,{kind:"curated-off"})`). New `RetireReason` member `{kind:"curated-off"}`, rank 1 in `retireReasonRank` (`:79-81`) so it is loud and can upgrade a `new-cycle`. `findReadAlerts:1044` — drop the kind filter. `fireRetraction:653-680` — `terminalHandle: alert.kind === "g" ? deps.terminalHandle(...) : ""`. Comments at `:118-120` and `:1197-1201` updated with the user's decision + date. |
| `packages\host-service\src\companion\registry.ts` | **MUST.** Three new `createSinkSlot` slots beside the existing two (`:311-327`): `terminal-gone` (`recordCompanionTerminalGone({hostTerminalId}): boolean`), `cold-start-boundary` (`recordCompanionColdStartBoundary({boundaryMs}): boolean`), `curation-changed` — or fold the last one into the existing mirror sink (preferred: it is already called from `publishCompanionMirrorChanged:203`, so no new slot). Net: **two** new slots. |
| `packages\host-service\src\events\event-bus.ts` | **MUST.** In `broadcastTerminalLifecycle:224-232`, before/after the fan-out: `if (message.eventType === "exit" && message.confirmed !== false) recordCompanionTerminalGone({ hostTerminalId: message.terminalId })`. Fork-only lines → carry a marker token (see FEATURES.md row below). `confirmed !== false` mirrors the renderer's own rule at `lifecycleEvents.ts:187-193`; the reaper emits the confirming exit later (`reaper.ts:472-479`). |
| `packages\host-service\src\companion\index.ts` | **MUST.** Register the two new sinks next to the lifecycle-seen sink (`:917-960`), each with an `unwind.push` clear. Extend `mirrorChangeSink` (`:1222-1239`) with `lifecycleAlerts.retireCuratedOffAlerts()` inside its own try/catch — a curation write must never fail on a retraction. |
| `packages\host-service\src\trpc\router\companion\companion.ts` | **MUST.** New `retireStaleReadyAlerts` mutation beside `markLifecycleSeen:398-407`. Input `z.object({ boundaryMs: z.number().int().positive() })`. Returns `{ accepted: boolean }`. Never throws for the off states, same as its neighbour. |
| `FEATURES.md` | **MUST.** One markers row: `(ALERT-RETIRE-ON-EXIT)\tpackages/host-service/src/events/event-bus.ts`. Rationale identical to `(COMPANION-CAPTURE-HOOK)`: the hook lives in an upstream-shaped file and an upstream merge could delete it with every other gate green. Extend the companion-alerts manifest paragraph with the three new retirement triggers. |

Tests to add (bun): `lifecycle-alerts.test.ts` (exists, 1598 lines — extend, do not create). `packages\host-service\src\companion\push-retract.test.ts` for the empty-`t`-on-`e` frame.

### FORK — renderer

| Path | Change |
|---|---|
| `...\V2NotificationController\lib\companionAlertSync.ts` | **MUST.** New export `reportColdStartBoundary({ hostUrl, boundaryMs }): Promise<boolean>`, same silent-on-failure contract as `reportTerminalSeen:435-468`, but keyed by `hostUrl` directly (the caller has it) rather than through `hostUrlByWorkspaceId`. |
| `...\V2NotificationController\lib\resyncAgentStatus.ts` | **MUST.** At `:398-401`, where `hostSessionBoundaries.set(hostUrl, hostNow - elapsedMs)` runs — fire-and-forget `reportColdStartBoundary`. That map already dedupes per host (`:393`), so it fires exactly once per host per cold launch. Add a `coldStartBoundaryReported` counter to `ResyncResult` for the existing diagnostic log. |
| `...\V2NotificationController\lib\lifecycleEvents.ts` | **NO CHANGE.** R1 is host-side. Avoids the fixer's in-flight edit. |
| `...\stores\v2-notifications\store.ts` | **NO CHANGE.** |

Tests: extend `companionAlertSync.test.ts` (its transport double at `:19-45` already fakes `getHostServiceClientByUrl`) and `resyncAgentStatus.test.ts`.

### COMP — phone

| Path | Change |
|---|---|
| `app-phone\...\phone\wear\PendingQuestionResolutions.kt` | **NEW, MUST.** `PendingQuestionResolution` data class + `PendingResolutionStore` interface + `PendingQuestionResolutions(store, capacity=256, ttlMs=21_600_000)` + `SharedPreferencesPendingResolutionStore` (own file `companion_question_resolutions`, `commit()`). Structural clone of `PendingLifecycleDismissals.kt` — same `enqueue`/`pending`/`clear`/`isLive`, same `@Synchronized`, same "full queue refuses loudly and drops nothing" rule. |
| `app-phone\...\phone\wear\WatchBridge.kt` | **MUST.** Constructor gains `pendingResolutions` (`:104-109`). `pushResolved:250-266` → enqueue, `deliverResolution(...)`, return `Sent`/`SentUnrecorded` exactly as `pushLifecycleDismiss:335-348`. New private `deliverResolution` mirroring `deliverDismissal:369-387` (clear only on `Delivered && failedNodeCount == 0`). New `flushQuestionResolutions(nowMs)`. `onHello:546-562` calls it **before** the oldest-pending-question push, so a queued resolution can never cancel the card hello just posted. |
| `app-phone\...\phone\di\CompanionContainer.kt` | **MUST.** New lazy `pendingQuestionResolutions` beside `:213-214`; pass it at `:222`. |
| `app-phone\...\phone\notifications\CompanionNotifications.kt` | **MUST.** `cancelLifecycle:297-311` consumes the new three-valued verdict; cancel `terminalKey(terminalId)` when the verdict is Unknown and `terminalId` is non-empty. |

### COMP — watch

| Path | Change |
|---|---|
| `app-watch\...\wear\notify\QuestionNotifier.kt` | **MUST.** `post(...)` gains `timeoutAtMs: Long`; `.setTimeoutAfter((timeoutAtMs - now).coerceAtLeast(1))` at `:123-131`, same idiom as `LifecycleNotifier.kt:151`. `notifyQuestion` → `question.askedAtMs + QUESTION_STALE_AFTER_MS`. `notifyOversize`/`notifyContentless` → `now + QUESTION_STALE_AFTER_MS` (neither payload carries `askedAtMs`; arrival is a lower bound on the ask, so the card never times out early). |
| `app-watch\...\wear\notify\LifecycleNotifier.kt` | **MUST.** `cancel:...` consumes the same verdict as the phone, same Unknown rule. |

### COMP — core / protocol

| Path | Change |
|---|---|
| `core\...\core\notify\ShownLifecycleAlerts.kt` | **MUST.** `forgetRetracted` returns a sealed `RetractionVerdict` — `Retired(key)` / `Stale` / `Unknown`. Behaviour otherwise unchanged: terminal branch first, generation guard intact, event branch as today. One rule, both surfaces. |
| `core\...\core\CompanionConstants.kt` | **MUST.** `const val QUESTION_STALE_AFTER_MS = 21_600_000L` in a new "Question freshness" block. Must equal `PUSH_QUESTION_EXPIRY_MS` in the fork; cross-repo, so no test can bind them — call that out in the comment on both sides. |
| `PROTOCOL.md` | **MUST.** §15 constants table: `Question staleness horizon | 21 600 000 ms`. §13.5.6: one paragraph — an error retraction carries `t: ""` because an `e` card is event-keyed and has no terminal-collapsed row, and a non-empty `t` on an `e` retraction retires a READY row it was never about. §13.5.5/§11: the wrist question card carries its own `setTimeoutAfter` from `askedAtMs` + the horizon, because the watch has no equivalent of the phone's foreground `sweep`. §13.5.5: question resolutions are eventual, not one-shot — same durable-queue wording as the watch dismissal, TTL 6 h. |

Tests: `PendingQuestionResolutionsTest.kt` (clone `PendingLifecycleDismissalsTest.kt`), extend `ShownLifecycleAlertsTest.kt`, update `WatchBridgeIntentTest.kt` construction, `WatchRelayedTextWireTest.kt` for the resolution-queue contract.

---

## 3. Data flow

**FIX-1, resolution:** phone resolves (answer confirmed / sweep / FCM retract) → `WatchBridge.pushResolved` → **enqueue first** (durable, `commit()`) → `WatchLink.sendResolved` → `Delivered && failedNodeCount==0` → `clear(questionId)`. `NoWatchPaired`/`Failed`/partial → row stays. Watch reconnects → `HELLO` → `onHello` → `flushLifecycleDismissals` → `flushQuestionResolutions` → oldest pending question. Watch: `QUESTION_RESOLVED` → `QuestionNotifier.cancel` + `QuestionStore.onResolved` (idempotent for unknown ids, `QuestionStore.kt:327-346`). Independently, every wrist question card self-cancels at `askedAtMs + 6 h`. Validation: at the queue boundary (`enqueue` refuses over cap and reports `SentUnrecorded`, never silently drops) and at `WatchLink.send` (`MAX_MESSAGE_BYTES` require).

**FIX-2 R1:** pty exit / dispose / reaper correction → `EventBus.broadcastTerminalLifecycle({eventType:"exit", confirmed?})` → fork hook, `confirmed !== false` → `recordCompanionTerminalGone` → (bridge off ⇒ `false`, no-op) → `lifecycleAlerts.retireTerminal` → `supersede(terminalId, {kind:"session-end"})` → per alert: `held` deleted silently, `sending` flagged (retracted on `markDelivered`), `sent`/`standing` → one `c` each, `retracted` inert. Phone: `routeLifecycleRetract` → suppress tombstone first, then cancel, then watch dismissal (`LifecycleRouting.kt:278-289`). State persisted: none new on the host (alert table stays process-local, per `lifecycle-alerts.ts:416-421`); durable state is the phone's `ShownLifecycleAlerts` + suppression gate.

**FIX-2 R2:** desktop cold start → renderer resync computes `hostSessionBoundary = hostNow - elapsedMonotonic` → `companion.retireStaleReadyAlerts({boundaryMs})` (validated at the tRPC boundary: positive int; the sink additionally refuses `boundaryMs > now()` — a future boundary would retire everything) → `retireReadyBefore` → `retireAlert(..., {kind:"seen"})` per covered `g`. Same instant the renderer seeds those terminals as seen locally (`resyncAgentStatus.ts:509-520`), so both surfaces agree.

**FIX-2 R3:** user bins/archives/snoozes/completes a thread → `useSidebarMirrorSync` full-replace → `sidebarMirror.sync` → `replaceMirror` (`sidebar-mirror.ts:186-260`, content hash decides `changed`) → `publishCompanionMirrorChanged` → `mirrorChangeSink` → `tree.curation` event **and** `retireCuratedOffAlerts()` → `workspaceSidebarVerdict !== "show"` → one `c` per live alert. DB (`sidebar_workspace_state`) is the source of truth for the verdict; the probe's 30 s positive cache is fine here because a cached `true` is the state we act on.

**FIX-3:** user opens the chat on the desktop → `markTerminalSeenAndReportRead` (`companionAlertSync.ts:371-413`) → `companion.markLifecycleSeen` → bridge revalidates terminal↔workspace against host.db (`index.ts:917-953`) → `findReadAlerts` now returns `g` **and** `e` rows with `outcomeAtMs <= seenThroughAt` → `retireAlert(..., {kind:"seen"})` → `fireRetraction` with `t = handle` for `g`, `t = ""` for `e` → phone: `e` cancels its event-keyed row only; `g` cancels event key + terminal key under the generation guard.

---

## 4. Build order

Each step independently verifiable. **Fork work lands as its own commit after the master-workspace feature; companion work is independent and can land first.**

**COMP (independent, ship first — it makes the fork's retractions land correctly on shipped phones):**
1. `ShownLifecycleAlerts` verdict + both cancel sites + tests. Verify: `gradlew :core:test :app-phone:test :app-watch:test`.
2. `QUESTION_STALE_AFTER_MS` + wrist question `setTimeoutAfter` + PROTOCOL §15 row. Verify: unit test on the computed timeout; manual on-wrist check that a card self-clears.
3. `PendingQuestionResolutions` + store + tests (pure JVM, no device).
4. `WatchBridge.pushResolved`/`flushQuestionResolutions`/`onHello` + container wiring + `WatchBridgeIntentTest` update.
5. PROTOCOL §13.5.5/§13.5.6 paragraphs.

**FORK (after the master-workspace commit is in):**
6. `lifecycle-alerts.ts` alone: `findReadAlerts` widening, `fireRetraction` empty-`t`-for-`e`, the three new manager methods, the `curated-off` reason, comment updates. Verify: `bun test packages/host-service/src/companion/lifecycle-alerts.test.ts` + `push-retract.test.ts`. Nothing is wired yet, so this step cannot change runtime behaviour beyond FIX-3.
7. `registry.ts` slots + `index.ts` registration + `mirrorChangeSink` call (R3 live). Verify: bun tests; manual — bin a thread with a ready card on the phone, card disappears.
8. `event-bus.ts` hook + FEATURES.md marker (R1 live). Verify: `bun run scripts/check-feature-markers.mjs`; manual — kill a terminal with a ready card, card disappears.
9. tRPC mutation + renderer `reportColdStartBoundary` + resync call (R2 live). Verify: renderer bun tests; manual — leave a ready card, quit and relaunch the desktop, card disappears.
10. FEATURES.md manifest prose. `/simplify` before commit (production code touched).

---

## 5. Risks and edge cases

* **Marker survival.** The `event-bus.ts` hook is fork-only code in an upstream-shaped file. Without the FEATURES.md marker row a nightly merge can take upstream's version and every gate stays green while R1 silently dies. Same failure class the fork already wrote `(COMPANION-CAPTURE-HOOK)` for.
* **`confirmed: false` exits.** Deliberately not retired — the row stays `active` for the reaper, which emits the confirming exit (`terminal.ts:2905-2915`, `reaper.ts:472-479`). Cost: a card lives until the reaper's pass (observed up to 21 min in the DISPOSE-LIMBO note). Retiring on the unconfirmed exit would retract a card for a terminal that may still be alive.
* **Host-service restart (residual, R2).** Alert state is process-local by design. Desktop restart + host restart ⇒ nothing holds the alert, nothing can name it, and the card lives to its 6 h TTL — the phone's `setTimeoutAfter` (`CompanionNotifications.kt:271`) takes it down, no manual swipe. The only cure is either durable alert state (schema change, needs explicit user approval) or a phone-side lifecycle sweep against the tree — both out of scope.
* **`e` retraction after a host restart (residual, FIX-3).** Blind fallback stays `g`-only, so a read does not retract a restart-orphaned `e`. Dies at TTL.
* **Snooze retracts too (R3).** `curated-off` covers snooze, not just the bin. Defensible ("not now" ⇒ take the buzz away), but it is a behaviour change beyond the literal ask — flag to the user. If unwanted, gate `retireCuratedOffAlerts` on the verdict being `deleted`/`completed`/`archived` only; `workspaceSidebarVerdict` already returns the discriminated reason (`sidebar-filter.ts:95-102`), so the narrowing is one condition.
* **Cancel-on-Unknown (phone).** Trades a possible extra cancel of an unrecorded newer card for never stranding one. The next finish reposts and buzzes, so the worst case is one missed buzz on an unread chat whose record was evicted at cap 64 — the same trade PROTOCOL §13.5.6 already documents ("one extra buzz, never a stuck notification").
* **Retraction storms.** `retireCuratedOffAlerts` runs on every *changed* mirror sync, `retireReadyBefore` once per host per launch. Both are bounded by the alert table (512) and every path goes through `retireAlert`, whose `retracted` rows are inert — no alert can fire two `c`s.
* **Clock.** `boundaryMs` is host clock by construction (`hostNow - elapsedMonotonic`); a renderer that could not read `performance.now()` or `hostNow` skips cold-start seeding entirely (`resyncAgentStatus.ts:394-405`) and must skip the report too — same guard, do not report a boundary the seeding did not use.
* **Concurrency (phone).** `PendingQuestionResolutions` must be `@Synchronized` on the instance and constructed **once** in the container, for the reason `LifecycleNotifier` spells out — the Data Layer dispatches on a pool and a per-call instance is no lock at all.
* **Migration.** The new SharedPreferences file starts empty; a resolution sent by the old build and lost stays lost (bounded by the new wrist timeout once the watch updates). No fork DB schema change anywhere in this design.
* **Rollout order (PROTOCOL §13.5.7 is a hard gate).** The empty-`t`-on-`e` change is safe in either order — every shipped phone already accepts `t: ""` on `c`. The phone's verdict change is a pure improvement. So there is no coupled rollout, which is why the two commits can land independently.
* **Overlap with the in-flight master-workspace work.** `lifecycleEvents.ts`, `events/types.ts`, `terminal/terminal.ts` and `eventBus.ts` (workspace-client) are all dirty right now. This design touches **none** of them except `packages/host-service/src/events/event-bus.ts` (host, not the workspace-client one) — check for a conflict there at rebase time, and re-read `broadcastTerminalLifecycle` after the `created` event lands, since the new `created` variant flows through the same method and must fall through the hook's `eventType === "exit"` guard.

---

## 6. Out of scope

* Durable host-side alert state (would need a `host.db` schema change and explicit user approval).
* Phone-side reconciliation of *lifecycle* cards against the tree on foreground (`CompanionNotifications.sweep` stays questions-only). This is the only thing that would close the double-restart residual.
* Question-path retraction (`v1 r`), the answer path, leases, pairing — untouched.
* Any change to the `q`/`g`/`e` push key sets, to `collapse_key`, or to the dedupe/suppression window.
* The `new-cycle` silent-retirement rule for `g` — unchanged.
* Watch UI beyond the notification timeout.
* Kanban / sidebar / recycle-bin behaviour: R3 reads the mirror, it never writes curation.

</agent-message>

---

<agent-message from="arch-notif">
# FINAL SPEC — companion notification-gap fixes (FIX-1/2/3)

Supersedes the blueprint. All 15 review points folded in. Every anchor below re-read this session.

FORK = `C:\Users\khair\Code Projects\fork-superset-windows-arm64`
COMP = `C:\Users\khair\Code Projects\superset-companion`

---

## 0. Decisions carried in from review, and one new finding

**Confirmed pre-existing bug (both reviewers).** `fireRetraction` passes a real terminal handle for every kind (`packages\host-service\src\companion\lifecycle-alerts.ts:665`); the phone reads a non-empty `t` on a `c` as "retire the terminal-collapsed READY row up to `gx`" (`COMP\core\...\notify\ShownLifecycleAlerts.kt:173-181`); `notifiesPhone` fires for `e` + `new-cycle` (`:122-124`). So an error followed by fresh work silently cancels the user's unread ready card, today, with no part of this feature enabled. Fixed by §FIX-3's empty-`t` rule.

**New finding — M4's hop is exact, not a hash guess.** `notifications.ts:307-318` computes `occurredAt = nextLifecycleInstantMs(...)` **once** and passes the same value to `broadcastAgentLifecycle` (`:317`) and to `forwardCompanionLifecycle` (`:341`). So the renderer's `payload.occurredAt` for a `Stop`/`Failed` event **is** the `outcomeAtMs` the alert id hashes. The visible-clear report can therefore use `seenThroughAt = payload.occurredAt` and land on the live map every time — no blind broadcast, no `canProveAbsence` gamble.

**M4 scope, pinned.** Only `eventType === "Stop"` and `"Failed"` can mint an alert: `apps\desktop\src\main\lib\agent-jsonl-watcher\pane-map-hook.ts:2981-2985` sets `lifecycle_outcome` to `"failed"` / `"ready"` for exactly those, and `lifecycle-alerts.ts:1153` ignores every other outcome. The report must be scoped to those two event types — **not** to "any event reaching the `statusTransitions.ts:98` tail", because `BackgroundRunning` reaches that tail too and mints nothing.

**Intentional asymmetry, stated so nobody "fixes" it.** R2 (desktop relaunch) retires `g` only. FIX-3 retracts `e` on a real read of that terminal; a cold relaunch is not a read of anything — the renderer's cold-start seeding is a heuristic about idle greens, and silently dropping an unseen agent-death on app restart is the failure this alert class exists to prevent.

**Fail-direction split, stated so nobody unifies it.** The curation HOLD fails open (uncertain ⇒ fire the alert, `lifecycle-alerts.ts:288-295`). The curation RETIRE fails closed (uncertain ⇒ retire nothing). Both fail toward the user being told.

---

## 1. FORK — host-service

### 1.1 `packages\host-service\src\companion\lifecycle-alerts.ts` (MUST)

**Reasons.** Add three `RetireReason` members: `{kind:"terminal-gone"}`, `{kind:"desktop-relaunch"}`, `{kind:"curated-off"}`. `retireReasonRank:79-81` needs **no edit** — it returns 1 for everything that is not `new-cycle`, so all three are loud and can upgrade a queued `new-cycle` on a `sending` row. Distinct names rather than reusing `session-end` so `fireRetraction`'s log line (`:654-659`) says which mechanism fired. **Test-pin the rank and the `notifiesPhone` verdict for all three** (review pt 9).

**`notifiesPhone:122-124`** — no code change. Update the doc block: `:118-120`'s "`e` ALERTS ARE UNCHANGED" paragraph now records the user's override — desktop-seen retracts an `e`, decided 2026-08-20 — while the silent case remains exactly `g` + `new-cycle`.

**`findReadAlerts:1037-1049`** — delete the `if (alert.kind !== "g") continue;` at `:1044`. Nothing else in the function changes; the `outcomeAtMs > seenThroughAt` boundary and the newest-first sort already do the right thing for both kinds.

**`markLifecycleSeen:1176-1275`** — the blind fallback at `:1264-1274` stays `kind: "g"`. Rewrite the "ONLY `g`" comment at `:1197-1201` to say what is now true: the live map covers both kinds; the blind restart hash stays ready-only because hashing both would double the blind broadcasts the `:1225-1233` note exists to avoid, and an `e`'s id is only recomputable when `seenThroughAt` coincides exactly with its outcome instant. Residual in §6.

**`fireRetraction:653-680`** — `terminalHandle: alert.kind === "g" ? deps.terminalHandle(alert.hostTerminalId) : ""`. `gx` is **unchanged and still required** — `buildLifecycleRetractPushData:984-988` refuses a non-positive `outcomeAtMs`, and `e` rows carry a real one from `mint:1068`. **Test-pin that an `e` retraction has `t === "" && gx === String(outcomeAtMs)`** (review pt 15).

**Three new manager methods** on `LifecycleAlertManager` (`:203-212`):

```ts
retireTerminal(hostTerminalId: string): void;
retireReadyBefore(boundaryMs: number): number;   // returns alerts retired
retireCuratedOffAlerts(isCuratedOffFresh: (hostWorkspaceId: string) => boolean): number;
```

* `retireTerminal` → `supersede(hostTerminalId, { kind: "terminal-gone" })`. Nothing else; `supersede:878-883` + `retireAlert:895-938` already handle all five states correctly.
* `retireReadyBefore` → walk `alerts`, take `kind === "g" && outcomeAtMs < boundaryMs`, `retireAlert(alert, { kind: "desktop-relaunch" })`. **Includes `held`** (deleted silently, no `c`) — the evidence is "the desktop has seeded these as read", which retires a never-sent alert too.
* `retireCuratedOffAlerts` — **takes the fresh probe as a parameter** (review pt 1). It must never reach for `deps.isCuratedOff`, whose 30-second positive cache (`:277-279`) would retire alerts for a workspace whose snooze expired inside the window. Signature makes that impossible to get wrong and is test-injectable.

**`retireCuratedOffAlerts` algorithm** (review pt 2 — flip-scoped, held-preserving):

1. Collect the distinct `hostWorkspaceId`s of alerts in state `sending | sent | standing` only. **`held` is excluded**: it is still under the existing fire-at-expiry hold at `claim:639`, and deleting it would drop a mid-snooze finish the user has never seen.
2. For each, call `isCuratedOffFresh`. Compare against a manager-owned `Map<string, boolean> curationVerdictByWorkspace`.
3. Retire that workspace's `sending|sent|standing` alerts **only on a false→true flip**, with `{kind:"curated-off"}`. An unchanged `true` retires nothing — otherwise every unrelated mirror write (the 5-minute heartbeat included) would re-walk and re-log.
4. Record the new verdict either way. Prune map entries for workspaces with no live alerts, so it stays bounded by the alert table.
5. A workspace absent from the map on its first sighting seeds as `false` **before** the flip test, so a workspace that was already curated off when its alert was minted does not get retired by the first sync it appears in. (The mint path could not have produced it anyway — `claim:639` holds — but state it.)

Net effect, which is exactly what the user approved: snooze/bin/archive/complete takes the standing card off the phone now, and a finish that happens *during* the snooze still fires when the snooze lapses.

**Scope note.** `curated-off` covers snooze, not just the bin. If the user wants bin/archive/complete only, `workspaceSidebarVerdict` already returns the discriminated reason (`sidebar-filter.ts:95-102`) — one condition in the `index.ts` closure narrows it, no change here.

### 1.2 `packages\host-service\src\companion\registry.ts` (MUST)

Two new `createSinkSlot` slots beside the existing pair at `:311-327`:

| Slot | `what` | Call | `whenAbsent` |
|---|---|---|---|
| terminal-gone | `"terminal-gone"` | `recordCompanionTerminalGone({ hostTerminalId }): boolean` | `false` |
| relaunch-boundary | `"relaunch-boundary"` | `recordCompanionRelaunchBoundary({ boundaryMs }): boolean` | `false` |

`onThrowMessage` in each names what the user loses ("a phone notification may outlive the terminal it names" / "…may outlive the desktop session that raised it"). **No third slot for R3** — it rides the existing `publishCompanionMirrorChanged:203-218`.

### 1.3 `packages\host-service\src\events\event-bus.ts` (MUST)

In `broadcastTerminalLifecycle:224-232`, after `this.broadcast(...)`:

```ts
// (ALERT-RETIRE-ON-EXIT) fork-only.
if (message.eventType === "exit" && message.confirmed !== false) {
    recordCompanionTerminalGone({ hostTerminalId: message.terminalId });
}
```

* `confirmed?: boolean` exists on the exit variant only (`events\types.ts:78-85`), so the `eventType` check narrows the union before the field is read.
* `confirmed !== false` mirrors the renderer's own rule (`lifecycleEvents.ts:187-193`); the reaper emits the confirming exit later (`reaper.ts:472-479`).
* **This is the choke point for all five exit sites**: `terminal.ts:2834` (dispose, session live), `:2862` (limbo confirming exit, sessionless row), `:2909` (unconfirmed — skipped by the guard), `:3725` (pty `onExit`), `reaper.ts:473`. Nine `broadcastTerminalLifecycle` calls total once the in-flight `created` lands (`terminal.ts:3760`, plus command-start `:3628`, command-end `:3641`, synthesized command-end `:3655`).
* **`created` falls through the guard** — it is not `"exit"`. Add a test asserting a `created` broadcast records no terminal-gone (review pt 11); it is the one event most likely to be "helpfully" folded in later.
* Import is `recordCompanionTerminalGone` from `../companion/registry.ts`. No cycle: `registry.ts` imports `./index` type-only (`:26`) and `./config` at runtime, which pulls no event code.

### 1.4 `packages\host-service\src\companion\index.ts` (MUST)

* Register both sinks beside the lifecycle-seen sink (`:917-960`), each with its own `unwind.push({ what, close })`.
  * terminal-gone sink → `lifecycleAlerts.retireTerminal(input.hostTerminalId)`, return `true`. No host.db revalidation: the caller *is* the host runtime announcing a death it performed, unlike the renderer-originated seen signal.
  * relaunch-boundary sink → validate `Number.isInteger(boundaryMs) && boundaryMs > 0 && boundaryMs <= now()`; a future boundary is refused with an error log (it would retire everything). Then `lifecycleAlerts.retireReadyBefore(boundaryMs)`, return `true` — "the bridge received it", the same meaning `accepted` has everywhere else (`:919-921`).
* `mirrorChangeSink:1222-1239` — add the retirement **in its own `try`/`catch`, after and independent of the `events.publish` block** (review pt 8), so a throwing publish cannot skip the retirement and a throwing retirement cannot swallow the freshness event:

```ts
try {
    lifecycleAlerts.retireCuratedOffAlerts((hostWorkspaceId) => {
        try {
            return isLifecycleWorkspaceCuratedOff({
                db: hostDb, organizationId: options.organizationId,
                hostWorkspaceId, nowMs: Date.now(),
            });
        } catch (error) {
            logger.error("could not read curation for a live alert; retiring nothing", { hostWorkspaceId, error });
            return false;   // fail CLOSED here — see §0
        }
    });
} catch (error) { logger.error("curated-off retirement threw", { error }); }
```

`isLifecycleWorkspaceCuratedOff` is already exported at `:214-228`. **Do not** substitute `createLifecycleCurationProbe`.

### 1.5 `packages\host-service\src\trpc\router\companion\companion.ts` (MUST)

New mutation beside `markLifecycleSeen:398-407`:

```ts
const relaunchBoundaryInput = z.object({
    boundaryMs: z.number().int().positive().finite(),
});
retireStaleReadyAlerts: protectedProcedure
    .input(relaunchBoundaryInput)
    .mutation(({ input }): { accepted: boolean } => ({
        accepted: recordCompanionRelaunchBoundary({ boundaryMs: input.boundaryMs }),
    })),
```

Never throws for the off states, same contract and same doc-comment shape as its neighbour. `.int()` is safe **only because the renderer floors** (§2.2, review pt 4); say so in the comment, because the raw value on the other side is fractional and a future refactor that drops the floor would 400 every call.

### 1.6 `FORK\FEATURES.md` (MUST)

Both halves (review pt 12):

1. A table row for the retirement behaviour, marker column `` `(ALERT-RETIRE-ON-EXIT)` ``.
2. A line in the fenced ` ```markers ` block. Format is documented at `FEATURES.md:115-119`: `<marker-token>\t<path-glob-root>`, **a real tab**. Line: `(ALERT-RETIRE-ON-EXIT)<TAB>packages/host-service/src/events` — narrow root, so the gate proves the token is in the events package and not merely somewhere under `packages/`.
3. Extend the existing companion-alerts manifest paragraph with the three new retirement triggers and the empty-`t`-on-`e` rule.

Verify with `bun run scripts/check-feature-markers.mjs`.

---

## 2. FORK — renderer

### 2.1 `...\V2NotificationController\lib\companionAlertSync.ts` (MUST)

New export, same silent-on-failure contract as `reportTerminalSeen:435-468`, keyed by `hostUrl` directly (the caller has it; `hostUrlByWorkspaceId` is workspace-keyed and irrelevant here):

```ts
export function reportRelaunchBoundary({ hostUrl, boundaryMs }): Promise<boolean>
```

Guard `Number.isSafeInteger(boundaryMs) && boundaryMs > 0` before sending; resolve `false` otherwise. Never rejects.

**Acknowledged-latch** (review pt 7): module-level `const relaunchBoundaryReportedHosts = new Set<string>()`, added **only** on `accepted === true`. A transport failure or `accepted: false` leaves the host eligible on the next resync — `retireReadyBefore` is idempotent (a `retracted` row is inert, `retireAlert:896`), so retries are free. Deliberately **not** cleared by `forgetAlertContextSyncsForHost:287-298`: a host reconnect is not a new desktop launch, and the set's lifetime is the renderer's, which is the launch.

### 2.2 `...\V2NotificationController\lib\resyncAgentStatus.ts` (MUST)

At `:398-401`, where `hostSessionBoundaries.set(hostUrl, hostNow - elapsedMs)` runs — that map already dedupes per host (`:393`), so this is once per host per cold start:

* Keep the **fractional** value in the map (local comparisons at `:512` are unchanged).
* Report `Math.floor(hostNow - elapsedMs)` (review pt 4). Flooring biases the boundary **earlier**, so it can only ever retire strictly fewer alerts — the safe direction, same bias the cold-start seeding already has.
* Fire-and-forget through `reportRelaunchBoundary`; on `accepted`, add to the latch set.
* Both existing bail-outs (`:394-397` no monotonic clock, `:401-405` no `hostNow`) must also skip the report — if the seeding did not happen, its boundary must not be published.
* Add `relaunchBoundaryReported: boolean` to `ResyncResult` (`:409-424`) for the existing diagnostic log.

### 2.3 `...\V2NotificationController\lib\lifecycleEvents.ts` (MUST — this is the M4 hop)

**Nothing at `:187-203`.** R1 is host-side; the terminal-exit block stays exactly as the in-flight master-workspace commit leaves it.

In `updatePaneStatus`, immediately after `clearSources(workspaceId, transition.clearSources)` at `:278`:

```ts
// (ALERT-CONTEXT-NAMES) The user was LOOKING at the pane when the turn ended.
if (
    targetVisible &&
    (payload.eventType === "Stop" || payload.eventType === "Failed") &&
    transition.axes === null &&
    target.terminalId.length > 0
) {
    useV2NotificationStore.getState().markTerminalSeen(target.terminalId, payload.occurredAt);
    void reportTerminalSeen({
        workspaceId,
        terminalId: target.terminalId,
        seenThroughAt: payload.occurredAt,
    });
}
```

Why each clause:

* `targetVisible` — the whole point. The `wasAwaitingPermission && !targetVisible` case takes the same `:101-102` clear path but the user is **not** looking, so it must not report.
* the two event types — only these mint alerts (§0). `transition.axes === null` asserts the clear path actually ran rather than re-deriving `statusTransitions`' logic.
* `payload.occurredAt` — exactly the `outcomeAtMs` the alert id hashes (§0). This is what makes the hop land on the live map.
* `markTerminalSeen` locally as well: reporting "seen" to the host while the renderer keeps no watermark is the divergence that makes the next resync re-raise a green for an event the user watched, and then re-report it. `markTerminalSeen` only bumps forward (`store.ts:365-366`) and instants are monotonic per terminal, so a **later** finish still raises its review normally.

**No debounce, deliberately.** Idempotence is structural: one report per event, keyed by that event's own instant; a duplicate WS delivery produces an identical `seenThroughAt`; `retireAlert` is idempotent and the tombstone path (`retractById:955-992`) dedupes repeats. `targetVisible` is per-target, so a burst across terminals cannot fan out. Adding a timer here would only delay the report past the alert it retracts.

**Not `markTerminalSeenAndReportRead`.** Its guard `if (!clearedReview && outstandingAt === null) return` (`companionAlertSync.ts:395`) refuses precisely this case — there is no review entry and no outstanding record, by construction. Call `reportTerminalSeen` directly.

**Ordering note for the implementer.** In the common case the alert never exists: a visible pane means the user is present, presence gating holds the alert at `claim:638`, and the read deletes the held row silently. The report matters when the window is visible but unfocused and presence has lapsed. The renderer can never beat the mint — the router mints synchronously at `:341`, before the renderer's round trip.

---

## 3. COMP — phone / watch / core

### 3.1 `app-phone\...\phone\wear\PendingQuestionResolutions.kt` (NEW, MUST)

Structural clone of `PendingLifecycleDismissals.kt`: `PendingQuestionResolution` data class, `PendingResolutionStore` interface, `PendingQuestionResolutions(store, capacity = 256, ttlMs = 21_600_000)`, `SharedPreferencesPendingResolutionStore` (own file `companion_question_resolutions`, `commit()`, same `,`/`:` encoding, same `@Synchronized`, same "a full queue refuses loudly and drops nothing" rule).

Persisted fields: `questionId | enqueuedAtMs | resolvedAtMs | resolvedBySurface | outcome`. `resolvedAtMs` is nullable — encode absent as an empty field, decode empty as `null`. Delimiter-safe: `questionId` is 22-char base64url, the other two are fixed enum tokens.

**`resolvedByLabel` is not persisted.** All three `pushResolved` callers pass `null` (`ConversationViewModel.kt:518-524`, `SessionViewModel.kt:231-237`, `CompanionMessagingService.kt:209-215`) and nothing on the wrist renders it (`app-watch\...\ui\AnswerStateScreen.kt:283` reads only `resolvedBySurface`). The two `resolvedMessage(...)` paths (§3.2) *can* carry a label: the **immediate send keeps it**, only a **replay** degrades it to `null`. Document that in the data class.

TTL 6 h, not the dismissals' 24 h: it equals the question staleness horizon and the new wrist timeout, so a queued entry can never outlive the card it would cancel.

### 3.2 `app-phone\...\phone\wear\WatchBridge.kt` (MUST)

* Constructor (`:104-109`) gains `pendingResolutions: PendingQuestionResolutions`.
* **One enqueue-before-send primitive, and every resolution goes through it** (review pt 3). Today there are three senders, not one: `pushResolved:250-266`, `pushQuestionById:239` (`link.sendResolved(resolvedMessage(detail))`) and `onAnswerIntent:446` (same). Add:

```kotlin
private suspend fun deliverResolution(message: WatchQuestionResolved, nowMs: Long): WatchSendResult
```

mirroring `deliverDismissal:369-387` — enqueue the label-less record first, send the full message, `clear(questionId)` only on `Delivered && failedNodeCount == 0`. Route all three call sites through it. `pushResolved` returns `Sent` / `SentUnrecorded` exactly as `pushLifecycleDismiss:335-348` does, with the same "sent anyway, nothing will retry it" error log.
* New `suspend fun flushQuestionResolutions(nowMs: Long)`, mirroring `flushLifecycleDismissals:356-361`.
* `onHello:546-562` — call it **after** `flushLifecycleDismissals` and **before** the oldest-pending-question push, so a queued resolution can never cancel the card hello just posted.

### 3.3 `app-phone\...\phone\di\CompanionContainer.kt` (MUST)

New `by lazy` for `pendingQuestionResolutions` beside `:213-214`; pass at `:222`. **One instance for the process** — `@Synchronized` locks the instance, and the Data Layer dispatches on a pool (the reasoning `LifecycleNotifier` already spells out for its own singleton).

### 3.4 `core\...\core\notify\ShownLifecycleAlerts.kt` (MUST — serves FIX-2 and FIX-3)

`forgetRetracted:165-189` returns a three-valued verdict instead of `String?`:

```kotlin
sealed interface RetractionVerdict {
    data class Retired(val key: String) : RetractionVerdict   // record removed
    data object Stale : RetractionVerdict                     // record outranks this retraction
    data object Unknown : RetractionVerdict                   // no record at all
}
```

Mapping from today's behaviour: the `held.generationAtMs > generationAtMs` early return at `:177` becomes `Stale`; a removal becomes `Retired(key)`; both "no terminal row" **and** "no event row" become `Unknown` (keeping the `:184` compaction write). Logic otherwise unchanged.

### 3.5 `app-phone\...\phone\notifications\CompanionNotifications.kt` + `app-watch\...\wear\notify\LifecycleNotifier.kt` (MUST)

`cancelLifecycle:297-311` and `LifecycleNotifier.cancel` consume the verdict identically — one rule, both surfaces, as PROTOCOL §13.5.6 requires:

* `Retired(key)` → cancel that key (today's behaviour).
* `Stale` → cancel nothing beyond the unconditional event-key cancels. The generation guard is the whole point of `gx`.
* `Unknown` **and** `terminalId` non-empty → cancel `terminalKey(terminalId)` anyway.

The `Unknown` branch is the cap-64 / expired-record hole: `record:142` keeps only the last 63 rows, so a busy phone loses the record while the card is still on screen and the terminal-keyed card becomes uncancellable. Trade: at worst one extra cancel of a card whose record was evicted, and the next finish reposts and buzzes — the same "one extra buzz, never a stuck notification" trade PROTOCOL §13.5.6 already documents. **Test both directions: `Stale` never cancels, `Unknown` cancels** (review pt 14).

### 3.6 `app-watch\...\wear\notify\QuestionNotifier.kt` (MUST)

`post:89-135` gains `timeoutAtMs: Long`; add to the builder, **as a relative duration** (review pt 6), matching `LifecycleNotifier.kt:151`:

```kotlin
.setTimeoutAfter((timeoutAtMs - System.currentTimeMillis()).coerceAtLeast(1))
```

* `notifyQuestion` → `question.askedAtMs + QUESTION_STALE_AFTER_MS`.
* `notifyOversize` / `notifyContentless` → `System.currentTimeMillis() + QUESTION_STALE_AFTER_MS`; neither payload carries `askedAtMs`, and arrival is a lower bound on the ask, so the card can never time out early.

### 3.7 `core\...\core\CompanionConstants.kt` (MUST)

`const val QUESTION_STALE_AFTER_MS = 21_600_000L` in a new "Question freshness" block. Must equal `PUSH_QUESTION_EXPIRY_MS` (`FORK\packages\host-service\src\companion\config.ts:377`). Cross-repo, so no test can bind the pair — put a comment naming the other file **on both sides**.

### 3.8 `COMP\PROTOCOL.md` (MUST)

* §15 constants table: `Question staleness horizon | 21 600 000 ms`.
* §13.5.6: an error retraction carries `t: ""`. An `e` card is event-keyed on both devices (`LifecycleAlertIdentity.key`), so it has no terminal-collapsed row to name, and a non-empty `t` retires a READY row the retraction was never about. Already legal — §13.5.1's table says `t` "may be `""` on `q`, `e` and `c`", and both `sanitizeTerminalHandle` (`push.ts:771-775`) and `LifecyclePush.kt:241,247` accept empty. **The `c` key set is unchanged**; `gx` stays required.
* §13.5.6: the three-valued retraction verdict and the cancel-on-`Unknown` rule.
* §13.5.5 / §11: question resolutions are eventual, not one-shot — same durable-queue wording as the watch dismissal, TTL 6 h, replay on `hello`, label degrades to absent on replay. And: the wrist question card carries its own `setTimeoutAfter` from `askedAtMs` + the horizon, because the watch has no equivalent of the phone's foreground `sweep`.

---

## 4. Data flow

**FIX-1.** Any resolution (answer confirmed / foreground sweep / FCM retract / `pushQuestionById` / `onAnswerIntent` stale-or-resolved reply) → `deliverResolution` → enqueue durably (`commit()`) → `WatchLink.sendResolved` → clean delivery clears the row; `NoWatchPaired`/`Failed`/partial leaves it. Watch reconnects → `HELLO` → `flushLifecycleDismissals` → `flushQuestionResolutions` → oldest pending question. Watch: `QUESTION_RESOLVED` → `QuestionNotifier.cancel` + `QuestionStore.onResolved` (idempotent for unknown ids, `QuestionStore.kt:327-346`). Independently, every wrist question card self-cancels at `askedAtMs + 6 h`.

**FIX-2 R1.** pty exit / dispose / reaper correction → `broadcastTerminalLifecycle` → guard → `recordCompanionTerminalGone` (bridge off ⇒ `false`, no-op) → `retireTerminal` → `supersede(..., terminal-gone)` → per alert: `held` deleted silently, `sending` flagged and retracted on `markDelivered:697-722`, `sent`/`standing` one `c` each, `retracted` inert → phone `routeLifecycleRetract` (`LifecycleRouting.kt:278-289`): suppress tombstone **first**, then cancel, then watch dismissal.

**FIX-2 R2.** Cold desktop start → resync computes `hostNow - elapsedMonotonic` → floor → `companion.retireStaleReadyAlerts` → tRPC validates positive finite int → sink refuses `> now()` → `retireReadyBefore` → `g` alerts with `outcomeAtMs < boundary` retired as `desktop-relaunch`. Latched per host on `accepted` only.

**FIX-2 R3.** Bin/archive/snooze/complete → `useSidebarMirrorSync` full replace → `sidebarMirror.sync` → `replaceMirror:186-260` (content hash decides `changed`; the hash covers `appLaunchId`, so a cold launch always counts as changed) → `publishCompanionMirrorChanged` → `mirrorChangeSink` → `tree.curation` publish, then, separately guarded, `retireCuratedOffAlerts(fresh probe)` → flip-scoped retirement of `sending|sent|standing` only. `sidebar_workspace_state` is the source of truth for the verdict.

**FIX-2 phone addressing.** Every `c` above → `cancelLifecycle` → unconditional event-key cancel + legacy raw-id cancel → verdict → `Retired`/`Unknown` cancel the terminal key, `Stale` does not.

**FIX-3, two entry points.** (a) User opens the chat → `markTerminalSeenAndReportRead:371-413` → `companion.markLifecycleSeen` → bridge revalidates terminal↔workspace against host.db (`index.ts:917-953`) → widened `findReadAlerts` returns `g` **and** `e` → `fireRetraction` with `t = handle` for `g`, `t = ""` for `e`. (b) Turn ends while the pane is visible → `updatePaneStatus` hop → `reportTerminalSeen(seenThroughAt = payload.occurredAt)` → same path, exact id match.

Validation boundaries: tRPC input schemas (both mutations); the bridge's host.db revalidation for the seen signal; the relaunch sink's `boundaryMs <= now`; `assertLifecycleIdentity` + `sanitizeTerminalHandle` when the frame is built; `requireExactKeySet` on the phone. Persisted state: no new host DB schema anywhere. New durable client state: one SharedPreferences file on the phone.

---

## 5. Build order

Each step independently verifiable. **COMP first and shippable on its own; FORK strictly after the master-workspace commit lands.**

**COMP (independent):**
1. `ShownLifecycleAlerts` verdict + both cancel sites + tests (`Stale` never cancels, `Unknown` cancels). `gradlew :core:test :app-phone:test :app-watch:test`.
2. `QUESTION_STALE_AFTER_MS` + wrist `setTimeoutAfter` (relative) + PROTOCOL §15 row.
3. `PendingQuestionResolutions` + store + `PendingQuestionResolutionsTest.kt` (clone `PendingLifecycleDismissalsTest.kt`: cap, TTL, restart survival, unwritable store, idempotent enqueue).
4. `deliverResolution` + all three call sites + `flushQuestionResolutions` + `onHello` order + container wiring + `WatchBridgeIntentTest.kt` construction update.
5. PROTOCOL §13.5.5 / §13.5.6 paragraphs.
   **Steps 2 and 3 ship in the same release, non-negotiable** (review pt 10): the 6 h queue TTL is only defensible because the wrist card times itself out at the same horizon. Shipping the TTL alone leaves a card with no expiry and a queue that stops retrying under it.

**FORK (after the master-workspace commit):**
6. `lifecycle-alerts.ts` only — widened `findReadAlerts`, empty-`t`-for-`e`, three reasons, three methods, doc updates. Nothing wired; the only live behaviour change is FIX-3(a). `bun test packages/host-service/src/companion/`.
7. `registry.ts` slots + `index.ts` registrations + `mirrorChangeSink` retirement → **R3 live**. Manual: bin a thread holding a ready card, card disappears; un-snooze inside 30 s and confirm no wrongly-retired alert (the fresh-read test).
8. `event-bus.ts` hook + FEATURES.md row + marker line → **R1 live**. `bun run scripts/check-feature-markers.mjs`. Manual: kill a terminal holding a ready card. Test: a `created` broadcast records nothing.
9. tRPC mutation + `reportRelaunchBoundary` + resync call → **R2 live**. Manual: leave a ready card, quit and relaunch the desktop.
10. `lifecycleEvents.ts` visible-clear hop → **M4 live**. Manual: with the window visible but unfocused and presence lapsed, let a turn fail on the front pane; the phone's error card clears.
11. FEATURES.md manifest prose. `/simplify` before commit (production code touched).

**Tests to add**

*Host (bun, extend `lifecycle-alerts.test.ts` — 1598 lines, do not create a new file):* retireTerminal across all five states; retireReadyBefore boundary-exclusive and `g`-only and includes `held`; retireCuratedOffAlerts flip-only, `held`-preserving, fresh-probe-not-cached, throw ⇒ retires nothing; the three reasons rank 1 and notify the phone; `findReadAlerts` returns `e`; blind fallback still `g`. *`push-retract.test.ts`:* `e` retraction has `t === ""` and a real `gx`; `g` retraction unchanged.
*Host events:* exit records terminal-gone; `confirmed: false` does not; `created` does not; command-start/end do not.
*Renderer (bun):* `companionAlertSync.test.ts` — boundary latch only on `accepted`, retry after failure, non-integer refused. `resyncAgentStatus.test.ts` — reported once per host, floored, skipped when seeding is skipped. New `lifecycleEvents.test.ts` — report fires for visible `Stop`/`Failed`, not for `BackgroundRunning`, not when `wasAwaitingPermission && !targetVisible`, carries `payload.occurredAt`, marks seen locally.
*Kotlin (JVM):* as listed per step.
*Manual only:* every real FCM/Data-Layer delivery, doze behaviour, the wrist timeout expiring, and cross-repo constant equality.

---

## 6. Documented residuals

1. **Unconfirmed dispose delay.** `confirmed: false` exits are deliberately not retired; the reaper's confirming pass is what fires the retraction — observed up to 21 minutes in the DISPOSE-LIMBO note at `terminal.ts:2884-2892`. Retiring on the unconfirmed exit would retract a card for a terminal that may still be running.
2. **Double restart.** Alert state is process-local by design (`lifecycle-alerts.ts:416-421`). Desktop **and** host-service both restarting leaves no row to name, so nothing retracts and the card lives to its 6 h TTL — taken down by the phone's own `setTimeoutAfter` (`CompanionNotifications.kt:271`), never needing a manual swipe. Cures: durable alert state (host DB schema change, explicit user approval required) or a phone-side lifecycle sweep. Both out of scope.
3. **`e` after a host restart.** The blind fallback stays `g`-only, so a read does not retract a restart-orphaned error alert; it dies at TTL. Reversible in one line if the user prefers the doubled blind broadcasts.
4. **Cap-64 eviction.** Cancel-on-`Unknown` closes the stuck-card case at the cost of a possible extra cancel of an unrecorded newer card; bounded to one missed buzz, re-raised by the next finish.
5. **Snooze retracts.** `curated-off` includes snooze. Behaviour change beyond the literal ask, one condition to narrow.
6. **Cross-repo constant.** `QUESTION_STALE_AFTER_MS` and `PUSH_QUESTION_EXPIRY_MS` cannot be bound by a test. Comments on both sides are the only guard.
7. **Merge exposure.** The `event-bus.ts` hook is fork-only code in an upstream-shaped file; the marker line is what makes its removal a red nightly rather than a silent regression.

## 7. Out of scope

Durable host-side alert state; phone-side lifecycle sweep against the tree (`CompanionNotifications.sweep` stays questions-only); the `v1 r` question-retraction path; the answer path, leases, pairing; any change to the `q`/`g`/`e`/`c` key sets, `collapse_key`, or the dedupe/suppression window; the `new-cycle` silent-retirement rule; watch UI beyond the notification timeout; the `BackgroundRunning`-sets-review oddity at `statusTransitions.ts:105-112` (noticed, pre-existing, unrelated).

</agent-message>

---

<agent-message from="fix-master">
All 7 fixes applied. No commits. Zero new test failures, zero new tsc diagnostics.

## Per-fix status

**F1 (BLOCKER) DONE.** New `isGitRepoStrict` in `C:\Users\khair\Code Projects\fork-superset-windows-arm64\packages\host-service\src\runtime\git\non-git.ts` — rethrows instead of collapsing to `false`, and deliberately does NOT read the shared TTL cache (a concurrent lenient `isGitRepo` could have written a post-failure `false` into it). It is the sweep's injected default; the loop catch-and-classifies: repo → git path, provably-not → nonGit, throw → `console.warn` + `continue`, writing nothing.
Tests `packages\host-service\src\runtime\main-workspace-sweep.test.ts`: 6 → **9 pass**. Added probe-throws-with-existing-git-row (row byte-identical: branch/id/name), probe-throws-with-no-row (nothing created), and one-project's-failure-doesn't-stop-the-next.

**F2 (BLOCKER) DONE.** `created` eventType added to `TerminalLifecycleMessage` (carries `adopted: boolean`), broadcast at the very end of `createTerminalSessionInternal` — after the row commit, the map insert AND the subscribe rollback window, so an announced session can't be un-made.

Two consumer changes beyond the brief, both required:
- `packages\workspace-client\src\lib\eventBus.ts` re-narrows per eventType and its final arm is `command-start`. Without an explicit `created` arm every create would have been delivered as a command-start and lit the shell-running blue dot.
- `handleV2TerminalLifecycleEvent` (`...\V2NotificationController\lib\lifecycleEvents.ts`) reaches the `exit` teardown by fallthrough. `created` needed an early return or a new terminal would have had its agent source cleared and its registry entry pruned. (tsc enforces this one: the exit branch reads `payload.confirmed`.)

Other four consumers checked and safe unmodified: `useTerminalAgentBindings`, `useTerminalResumeCandidate`, `DashboardSidebarWorkspaceStatusProvider` (all invalidate-only), `useV2WorkspaceRun` (guards `eventType !== "exit"`).

**Deviation:** host-side test is a new bun test, `packages\host-service\src\terminal\terminal.created-event.test.ts` (**4 pass**), stubbing only `getDaemonClient` — real host DB + migrations, real env/shell resolution, real session bookkeeping. I did not add it to the `*.node-test.ts` files: they cannot execute on this box (better-sqlite3 native binding missing — I confirmed by running one) and this fork's CI doesn't run them, so the assertion would have been unverifiable. Asserts fresh create, adopted create (`adopted: true`), reuse-of-live-session is silent, refused create is silent. Wire narrowing covered separately in `eventBus.test.ts` (**6 pass**, +1: all four eventTypes asserted by name). Renderer test premise + hook comment corrected.

**F3 (MAJOR) DONE.** `isError` now reports only the enabled probe. Used the actual enablement flags rather than `mainWorkspaceId ? …` — the git probe is *also* disabled when `hostUrl` is null, so `gitProbeEnabled ? gitProbeErrored : multiRepoEnabled ? multiRepoProbeErrored : false` matches reality. Renamed the local `enabled` → `gitProbeEnabled`.
New `useProjectGitState.test.tsx`: **5 pass**, including hydration-after-multi-probe-error and the mirror case (stale git error not leaking into a multi-repo answer). The `useQuery` stub models TanStack's retention deliberately.

**F4 (MINOR) DONE.** Rung 2 is now `machineId == null || selectedHostId == null || selectedHostId === CLOUD_HOST_ID || selectedHostId !== machineId`. Kept the CLOUD_HOST_ID clause (redundant, documents intent). `resolveMasterMode.test.ts`: 17 → **20 pass** (both-null, machineId-null, selectedHostId-null). Rungs not reordered.

**F5 (MINOR) DONE — the restore-then-ensure branch.** Verified `restoreWorkspace` (`useDashboardSidebarState.ts:927-936`) no-ops for row-less rows and clears deletedAt/isHidden/archivedAt/completedAt/snoozeUntil/snoozeLaunchId, so it is a strict superset of unarchive+unsnooze. Master submit is now `restoreWorkspace` → `ensureWorkspaceInSidebar`.

**F6 (MINOR) DONE, blocked at two levels.** `submitBlocker` in `PromptGroup.tsx` (the gate all three submit triggers share) returns `Pick an agent to run in <masterName>`, and the master path in `useSubmitWorkspace` re-checks before anything mutates so a future caller can't bypass it. Branch mode untouched — a test asserts a no-agent prompt still becomes `namingPrompt` there.
`PromptGroup.test.tsx`: 15 → **20 pass** (binned-master, no-agent+prompt, no-agent+attachments, branch-mode-unchanged, plus the reworked restore assertion).

**F7 (MINOR) DONE.** `(MASTER-PLUS-LAUNCH)` row now states both git-path changes explicitly — the transient "Checking project…" on a cold probe, and folder-missing blocking before submit instead of failing server-side after — under a plain "Git projects are NOT byte-identical to before, and deliberately so". Also updated for restore→ensure, the no-agent refusal, and the `created` event. `(MASTER-ALWAYS-ACTIVE)` row gained the tri-state probe sentence. Rungs not reordered. Marker gate: all 249 present. Override-consistency: OK.

## One regression I caused and fixed

My first `useProjectGitState.test.tsx` mocked `../useProjectMainWorkspaceId`. `mock.module` is process-global with no unmock, and in a full-suite run it broke **7 PromptGroup tests** — the stub was still installed when that file ran. Re-mocking the real module in `afterAll` did NOT undo it. Rewrote the file to mock exactly PromptGroup's module set (each reinstalled in `beforeEach`, so whichever runs second takes its stubs back) and to drive the master through the host workspace list via the *real* `useProjectMainWorkspaceId` / `useWorkspaceHostUrl`. Green in both file orders and in the full suite.

## Verification

- **Desktop full suite:** baseline at HEAD (stashed, measured, restored) 3000 pass / **40 fail**; with fixes 3074 pass / **39 fail**. Failure set is a strict subset — the one that disappeared is a feature-owned sidebarMutations test. Nothing new.
- **host-service `src/`:** 45 fail before and after, identical sets; 1402 → 1415 pass.
- **workspace-client:** 14 pass / 0 fail.
- **tsc:** host-service, workspace-client, desktop all run; grep over each output finds **zero** diagnostics in any touched file. Pre-existing debt (mermaid dupes, workspace-fs exports, filesystem router) unchanged.
- **biome check:** clean on all 21 touched files.

The 39/45 pre-existing failures are environment-bound on this box (`wmic` absent, POSIX `/bin/sh` and path assumptions, real-git and `gh` fixtures).

**Deviation worth flagging:** biome's `check --write` opportunistically reformatted two unrelated spots in `packages\host-service\src\terminal\terminal.ts` (upstream import order, a union signature rewrap at :693). I reverted both — that file conflicts on every upstream merge and the drift predates this work. My diff there is 20 added lines, nothing else. It means `scripts/lint.sh` still reports those 2 pre-existing diagnostics for that file.

## Files

Modified: `packages\host-service\src\runtime\git\non-git.ts`, `...\runtime\main-workspace-sweep.ts`, `...\events\types.ts`, `...\terminal\terminal.ts`, `packages\workspace-client\src\lib\eventBus.ts`, `...\eventBus.test.ts`, `apps\desktop\src\renderer\routes\_authenticated\_dashboard\kanban\hooks\useProjectGitState\useProjectGitState.ts`, `...\_authenticated\components\V2NotificationController\lib\lifecycleEvents.ts`, `...\_dashboard\v2-workspace\$workspaceId\hooks\useAutoAdoptBackgroundSessions\useAutoAdoptBackgroundSessions.ts` (+ its test), `...\DashboardNewWorkspaceModal\components\DashboardNewWorkspaceForm\PromptGroup\PromptGroup.tsx`, `...\PromptGroup\hooks\useSubmitWorkspace\useSubmitWorkspace.ts`, `...\PromptGroup\PromptGroup.test.tsx`, `...\hooks\useMasterWorkspaceTarget\resolveMasterMode.ts` (+ its test), `FEATURES.md`.

New: `packages\host-service\src\terminal\terminal.created-event.test.ts`, `apps\desktop\src\renderer\routes\_authenticated\_dashboard\kanban\hooks\useProjectGitState\useProjectGitState.test.tsx`.

All under `C:\Users\khair\Code Projects\fork-superset-windows-arm64\`.
</agent-message>

---

<agent-message from="impl-notif-k">
Companion half done. NOT committed. Working tree at 4e1bf82 + 9 modified / 4 new.

TESTS: `./gradlew :core:test :app-phone:testDebugUnitTest :app-watch:testDebugUnitTest --continue`
- core 260 tests, 2 failed — both the known env-gated `SC_*_VECTOR_FILE` ones (`CrossRuntimeDifferentialVectors20260729Test`, IllegalStateException at :354, "SC_..._VECTOR_FILE is required"). Nothing else.
- app-phone 287 / 0 failed. app-watch 17 / 0 failed.
- 40 new tests: PendingQuestionResolutionsTest 14, WatchBridgeResolutionTest 10, QuestionNotifierTimeoutTest 7, ShownLifecycleAlertsTest 21→30.

FILES (all absolute, C:\Users\khair\Code Projects\superset-companion\)
Modified:
- core\src\main\java\family\khaira\supersetcompanion\core\notify\ShownLifecycleAlerts.kt
- core\src\main\java\family\khaira\supersetcompanion\core\CompanionConstants.kt
- core\src\test\java\family\khaira\supersetcompanion\core\notify\ShownLifecycleAlertsTest.kt
- app-phone\src\main\java\family\khaira\supersetcompanion\phone\notifications\CompanionNotifications.kt
- app-phone\src\main\java\family\khaira\supersetcompanion\phone\wear\WatchBridge.kt
- app-phone\src\main\java\family\khaira\supersetcompanion\phone\di\CompanionContainer.kt
- app-watch\src\main\java\family\khaira\supersetcompanion\wear\notify\LifecycleNotifier.kt
- app-watch\src\main\java\family\khaira\supersetcompanion\wear\notify\QuestionNotifier.kt
- PROTOCOL.md
New:
- app-phone\src\main\java\family\khaira\supersetcompanion\phone\wear\PendingQuestionResolutions.kt
- app-phone\src\test\java\family\khaira\supersetcompanion\phone\wear\PendingQuestionResolutionsTest.kt
- app-phone\src\test\java\family\khaira\supersetcompanion\phone\wear\WatchBridgeResolutionTest.kt
- app-watch\src\test\java\family\khaira\supersetcompanion\wear\notify\QuestionNotifierTimeoutTest.kt

SPEC ITEM 3 CROSS-REPO CHECK: verified fork `PUSH_QUESTION_EXPIRY_MS = 21_600_000` at packages/host-service/src/companion/config.ts:377. Constant matches; comment names it as the guard.

EMPTY-t VERIFICATION (the "shipped phones must already handle it" ask): they do. `t: ""` skips the terminal branch in forgetRetracted entirely and falls through to exact-event identity — which is the rule an error was always retired on — and the unconditional `cancel(eventKey(eventId))` covers the card regardless. Confirmed wire-legal per §13.5.1 (`t` may be `""` on `c`). The bug would be the opposite: a NON-empty `t` on an error retraction hands the client a generation comparison against a READY row it was never about. Now stated as a bridge constraint in §13.5.6. Two tests pin it.

DEVIATIONS (5, all justified):

1. **Cancel rule extracted to :core.** Spec 2 wanted both cancel sites to consume the verdict identically and tested BOTH directions. Both sites are Android-only (`NotificationManager`), and this project has no Robolectric and no mockk — JUnit + coroutines-test only — so a `when` duplicated at each site is untestable at either. Added `fun retractionCancelKey(verdict, terminalId): String?` to ShownLifecycleAlerts.kt: literally one rule, both surfaces, each site now a one-liner addressing its own id space (string tag / hashed int). Exhaustive `when` still breaks the build on a new verdict, in one place. Both directions tested: `Stale never cancels the terminal key`, `Unknown cancels the terminal key anyway`, plus an end-to-end capacity-eviction test.

2. **QuestionNotifier arithmetic behind two internal helpers.** `post` is private, so the spec's inline `.setTimeoutAfter((timeoutAtMs - System.currentTimeMillis()).coerceAtLeast(1))` could not also be tested as spec'd. Same arithmetic, now in top-level `internal questionStaleAtMs(fromMs)` and `questionTimeoutAfterMs(timeoutAtMs, nowMs)` (the seam `intentShapeError`/`draftFor` already use). Builder calls `questionTimeoutAfterMs(timeoutAtMs, System.currentTimeMillis())` — still relative, still floored at 1.

3. **`deliverResolution` returns `ResolutionDelivery(send, recorded)`, not bare `WatchSendResult`.** Forced: spec put the enqueue INSIDE deliverResolution (right call — three senders, not one), but pushResolved must report SentUnrecorded from the enqueue verdict, and a bare WatchSendResult throws it away. Body delegates to top-level `internal suspend deliverResolutionThrough(message, nowMs, queue, send)` — `WatchLink` is a concrete class taking a `Context`, not an interface, so this is the only way to test the enqueue-before-send ordering off device.

4. **No "WatchBridgeIntentTest construction update".** That premise is wrong — WatchBridgeIntentTest never constructs a WatchBridge; it drives top-level internal functions precisely because WatchLink is un-fakeable. Nothing to update; resolution coverage went into the new WatchBridgeResolutionTest.

5. **"Replay order in onHello" covered indirectly.** Driving onHello needs a ~25-member CompanionRepository fake plus a WatchLink interface refactor — out of scope and a large change to a shipped class. Covered as: `pending()` returns oldest-first (queue test), replay does not duplicate or refresh a queued entry (delivery test), and the flush-before-push ordering in onHello is source-level with the reason commented.

MINOR, unflagged in spec but changed because it went stale: `WatchPushResult.SentUnrecorded`'s doc said "Only the dismissal path can produce this" — now false, updated.

ONE JUDGEMENT CALL: `PendingQuestionResolutions.TTL_MS = CompanionConstants.QUESTION_STALE_AFTER_MS` rather than a second literal `21_600_000`. Same value, and the coupling is real — past the horizon the wrist card has already self-timed-out, so the entry has nothing left to resolve. Asserted at 21_600_000 in the test so a change to the shared constant still trips a gate.

PROTOCOL.md: §15 row added; §13.5.6 gained the empty-`t` constraint and a three-verdict table + cancel-on-Unknown reasoning; §13.5.5 gained the eventual-resolution rule (TTL = horizon, label degrades on replay, flush order); new §11.8 for the watch's copy of a settlement.
</agent-message>

---

whats built so far? once its all coded up and reviewed stop and tell me before trying to build them for install

---

<agent-message from="cr-verify2">
Verification of 7-fix delta (uncommitted tree vs 060a4971f6 = HEAD). All fixes FIXED. 2 new MINOR findings. Verdict: SHIP.

1. FIXED — sweep tri-state. isGitRepoStrict at packages/host-service/src/runtime/git/non-git.ts:91-93; classify loop at packages/host-service/src/runtime/main-workspace-sweep.ts:33-62 (repo → git, false → {nonGit:true}, throw → warn+continue, no write). Verified simple-git 3.36.0 checkIsRepo semantics in bundled source: returns false ONLY on exit 128 + "Not a git repository|Kein Git-Repository"; everything else (git missing, spawn failure) rethrows — tri-state is real. Cache-skip reasoning HOLDS: isGitRepo's catch writes value=false into the shared TTL cache (non-git.ts:66-70), so reading that cache would return a failed probe as "false". 9 sweep tests pass; throw cases genuinely pinned (test.ts:203 row-untouched after failed probe, :228 nothing created, :241 continue-to-next). A probe error can no longer rewrite an existing git main.

2. FIXED — created lifecycle event. Union arm packages/host-service/src/events/types.ts:54-65; broadcast at END of createTerminalSessionInternal (packages/host-service/src/terminal/terminal.ts:3760-3766, after row commit at :3392, map insert, subscribe rollback window; isAdopted/createdAt in scope). Both adaptations present: packages/workspace-client/src/lib/eventBus.ts:296-301 explicit created arm (else-chain final arm is command-start — test pins all 4 arms by name); lifecycleEvents.ts:161 early-return before exit-teardown fallthrough. Four unmodified consumers verified safe: useTerminalAgentBindings.ts:66 + useTerminalResumeCandidate.ts:58 + DashboardSidebarWorkspaceStatusProvider.tsx:246 invalidate-only; useV2WorkspaceRun.ts:353 early-returns on !== "exit". Grepped every eventType switch/if chain renderer+main+host: no other terminal-lifecycle consumer (pull-requests.ts:267 "created" is workspace:changed, different channel). Auto-adopt invalidation fires: useAutoAdoptBackgroundSessions.ts:63-73 subscribes invalidate-only gated on isLayoutReady; test pins the post-settle CLI-session adoption. created-event tests: fresh, adopted-flag, re-request-silent, refused-create-silent — 4 pass.

3. FIXED — isError enablement flags exactly match query enabled conditions (useProjectGitState.ts:45 gitProbeEnabled == git query enabled :53; :70-71 multiRepoEnabled == multi query enabled :77). Probes mutually exclusive via !mainWorkspaceId.

4. FIXED — resolveMasterMode.ts:102-109 rung 2 exact: machineId==null || selectedHostId==null || CLOUD_HOST_ID || !== machineId. 20 tests pass incl. all four rung-2 cases + both-null.

5. FIXED — restoreWorkspace (useDashboardSidebarState.ts:927-940) clears deletedAt/isHidden/archivedAt/completedAt/snoozeUntil/snoozeLaunchId, no-ops row-less; ensureWorkspaceInSidebar then inserts row-less master, no-ops a just-restored active row (bucket !== "hidden" guard at :167-173). Strict superset of unarchive+unsnooze — archived and snoozed cases both covered, binned master now exits the bin (PromptGroup test "a Recycle-Binned master is restored, not launched into inside the bin" passes). No regression.

6. FIXED — refusal both layers: PromptGroup.tsx submitBlocker arm (master + no agent + prompt/attachments → "Pick an agent to run in <name>") and re-check in useSubmitWorkspace master path BEFORE restoreWorkspace/any mutation. All three submit paths (button :539, prompt :716, Cmd+Enter :423) route through handleSubmit → submitBlocker. Branch mode untouched — blocker gated on mode==="master"; test "branch mode still keeps a prompt with no agent, as the naming prompt" passes.

7. FIXED — FEATURES.md: (MASTER-PLUS-LAUNCH) row states both deliberate git-path changes (transient "Checking project…" + up-front folder-missing block); (MASTER-ALWAYS-ACTIVE) row has the tri-state sentence. node scripts/check-feature-markers.mjs: all 249 present.

Self-reported items confirmed: useProjectGitState.test.tsx green in BOTH file orders (ran A: gitState→PromptGroup→…, B: PromptGroup→autoAdopt→gitState; mocks re-installed per beforeEach, mocked set identical to PromptGroup's); terminal.ts diff is exactly +20 lines, zero deletions, no reformats; created-event test uses real DB+migrations with only daemon-client-singleton mocked (spread of real module, single override).

Test runs: sweep+created-event 13/13; eventBus 6/6; desktop targeted 62/62 + reverse-order 29/29; resolveMasterMode 20/20. Full desktop suite: 3074 pass / 39-40 fail — all pre-existing env-bound (scrutinized the persisted-key registry fail: unregistered writers are kanbanSplitLayout + recycleBinRetention, pre-existing fork stores, not this delta). Host-service src tree: 1415 pass / 45 fail — EXACTLY the documented 45 env-bound baseline (gh/wmic missing, Electron guard, unix shell paths), none in delta files. Caveat: full host-service incl. test/integration would not complete inside a 20-min window in this env (hangs on wmic-dependent integration tests); baseline comparison is against the src tree where the count matches exactly.

NEW findings:
[MINOR] packages/host-service/src/runtime/git/non-git.ts:92
Defect: checkIsRepo's not-a-repo detection is locale-dependent (English/German regex in simple-git); a non-English git locale makes a genuine non-repo THROW instead of returning false.
Failure: on such a machine the sweep logs "probe FAILED" every boot and never backfills a lost non-git master. Fails in the SAFE direction (skip, no write) — inherited simple-git behavior, not introduced here.
Fix: none required for this delta; could force LC_ALL=C on the strict probe's git env later.

[MINOR] apps/desktop/.../PromptGroup/hooks/useSubmitWorkspace/useSubmitWorkspace.ts:167
Defect: restoreWorkspace/ensureWorkspaceInSidebar run before await promptContext.build; a build throw lands in the catch after the master is already restored/surfaced.
Failure: master pops back into Active with no agent launched and toast "Could not open the project workspace" — workspace WAS restored. Cosmetic partial-effect; build has an internal 2s timeout so throw is unlikely.
Fix: move restore/ensure after the awaits, or word the toast as launch failure.

Checked clean: sweep call sites (app.ts only, default param), ensureMainWorkspace/Strict nonGit plumbing incl. multi-repo guard, host broadcast fn signature (type spread), mobile/shared have no TerminalLifecyclePayload consumers, NewWorkspaceScreen BRANCH_ONLY_TARGET, AgentHooks reconciler ordering, selectHiddenMainsToSurface bucket-precedence predicate vs the sidebarMutations tombstone change (archived/binned masters stay in their buckets on project re-add — recoverable, no resurrect-loop), workspace-client tsc errors are pre-existing workspace-fs type debt unrelated to delta.

Verdict: SHIP
</agent-message>

---

<agent-message from="fix-tiny">
Done, not committed.

Code: C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\renderer\routes\_authenticated\components\DashboardNewWorkspaceModal\components\DashboardNewWorkspaceForm\PromptGroup\hooks\useSubmitWorkspace\useSubmitWorkspace.ts
- `restoreWorkspace(mainWorkspaceId)` + `ensureWorkspaceInSidebar(...)` moved to AFTER `await promptContext.build(...)`, immediately before recordPrompt → closeAndResetDraft → navigate → agents.run. Build throws → catch clears `masterSubmitInFlight` and toasts with zero sidebar mutation.
- Order preserved everywhere else: restore/ensure still precede navigate (row Active before workspace screen opens) and precede agents.run. Double-submit guard untouched (set before all early returns, cleared on every refusal + in catch).
- Existing restore comment kept and extended with the new reason ("only once nothing above can still throw"); no comment now misdescribes the code.

Tests: C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\renderer\routes\_authenticated\components\DashboardNewWorkspaceModal\components\DashboardNewWorkspaceForm\PromptGroup\PromptGroup.test.tsx
- No existing test pinned the old ordering (`sidebarCalls` and `order` are separate arrays and the build mock pushed nothing), so nothing needed updating and nothing would have caught a regression. Added one test instead: "a failed prompt build leaves the master where it was" — new `promptBuildError` mutable stub makes `promptContext.build` throw; asserts toast text, `sidebarCalls` empty, no agents.run, `order` empty.
- 21 pass / 0 fail (was 20; +1 is the new test). Verified the new test FAILS under the old ordering (temporarily reverted, ran it, restored).

bunx tsc --noEmit: no diagnostics in either touched file. Pre-existing tree-wide type debt unchanged (mermaid/hast dup-version mismatches, host-service filesystem members, billing page TS7006).
</agent-message>

---

<agent-message from="s3-reuse">
REUSE-ONLY review of the uncommitted master-workspace diff. 4 real findings + 4 low. No correctness claims.

**1. `useSurfaceHiddenMainWorkspaces.ts:29-103` is a near-clone of `usePlaceLocalWorktreesInSidebar.ts:27-73`.**
Same 4 providers (useCollections/useLocalHostService/useDashboardSidebarState/useHostWorkspaces), a second `useLiveQuery` over the same `collections.v2WorkspaceLocalState`, a second `useMemo` mapping every host workspace, same effect shape ending in an `ensureWorkspaceInSidebar` loop.
Cost: a duplicate live-query subscription and a second full workspace-array rebuild per render, in the always-mounted `AgentHooks`; and the two must be hand-kept in step — they already diverge on where the host filter sits (placement filters `hostId === machineId` in the memo at :36; surfacing filters inside the selector at :72, so its memo churns on remote-host changes that can never produce output).
Existing helper: `usePlaceLocalWorktreesInSidebar` itself. One query selecting the 7 classifier leaves serves both (placement only needs row presence), one selector returns both lists.
Full paths: `C:\Users\khair\Code Projects\fork-superset-windows-arm64\apps\desktop\src\renderer\routes\_authenticated\components\AgentHooks\hooks\useSurfaceHiddenMainWorkspaces\useSurfaceHiddenMainWorkspaces.ts`, `...\AgentHooks\hooks\usePlaceLocalWorktreesInSidebar\usePlaceLocalWorktreesInSidebar.ts`

**2. `selectHiddenMainsToSurface.ts:3-9` duplicates `selectWorktreesToPlace.ts:1-6`.**
`LocalWorkspaceForSurfacing` is `LocalWorkspaceForPlacement` verbatim plus `hostId`. Cost: two names for one row shape in sibling folders; a new workspace `type` has to be added twice.
Existing helper: `LocalWorkspaceForPlacement` — add `hostId` and export one type.

**3. `selectHiddenMainsToSurface.ts:71-80` re-encodes the gate `isAutoIncludedLocalMainWorkspace` already owns.**
That helper (`...\CollectionsProvider\dashboardSidebarLocal\sidebarVisibility.ts:33-51`) is exactly `projectId !== null && !hasRow && hostId === machineId && sidebarProjectIds.has(projectId)`. The new selector rewrites three of those four conditions inline with the row-presence one inverted.
Cost: the two must stay exact complements. Anyone tightening the auto-include gate (adding `hostReachable`, say) will not find this copy, and drift puts a main under both paths or under neither — the orphaned-main bug this feature exists to fix.
Existing helper: extract the shared 3 conditions from `isAutoIncludedLocalMainWorkspace` and have both call it.

**4. `useSubmitWorkspace.ts:132-146` duplicates the `submitBlocker` rule at `PromptGroup.tsx:349-355`.**
`submitBlocker` already gates all three submit triggers — button, Enter, and the window Cmd/Ctrl+Enter listener all funnel through `handleSubmit` (PromptGroup.tsx:390-403) — and the only other `useSubmitWorkspace` caller (`NewWorkspaceScreen.tsx:507`) passes `BRANCH_ONLY_TARGET`, so the in-hook copy is unreachable in master mode today.
The two copies have already diverged: label is `masterName ?? "this project"` (useSubmitWorkspace.ts:138) vs `masterName ?? selectedProject?.name ?? "this project"` (PromptGroup.tsx:320-321), and the attachment predicate is `attachmentIds.length` vs `visibleFiles.length`. Same sentence shown to the user, two spellings of the project name.
Existing helper: put the display label on the `master` arm of `MasterWorkspaceTarget` in `resolveMasterMode.ts` so both read one field; then the hook copy is either deletable or a one-line reuse of the same string.
(The `linkedPR` guard at useSubmitWorkspace.ts:104-108 is NOT this — the draft can carry a linked PR from before the project switch, so it is reachable. No finding.)

LOW:
- `useSurfaceHiddenMainWorkspaces.ts:64-73` is the 9th verbatim `v2SidebarProjects → projectId` live query (`useVisibleSidebarWorkspaceIds.ts:27-35`, `useSidebarMirrorSync.ts:481-489`, 6 more). Pre-existing convention, so consistent — but a `useSidebarProjectIds()` would collapse ~4 of them and this is the moment the 9th arrived.
- `resolveMasterMode.ts:70-155` is now a second, richer answer to the question `PromoteCardDialog.tsx:101-108` answers as `isResolved && !isGitRepo && mainWorkspaceId`. Not harmful duplication (the resolver adds host identity, multi-repo, `worktreeExists`, `isError`), but PromoteCardDialog could now consume `resolveMasterMode` instead of its own `isNonGit`.
- `main-workspace-sweep.test.ts:35-41` and `terminal.created-event.test.ts:100-105` add copies 12 and 13 of the in-memory `drizzle + migrate + cast to HostDb` block across host-service tests. Precedent for extracting one exists: `packages\host-service\src\companion\test-fixtures.ts`.
- The three new renderer tests each re-hand-roll the happy-dom + `IS_REACT_ACT_ENVIRONMENT` + per-test `installMocks()` harness (`PromptGroup.test.tsx:24-32`, `useProjectGitState.test.tsx:23-29`, `useAutoAdoptBackgroundSessions.test.tsx:30-41`), and `useProjectGitState.test.tsx:64-72` documents that its mocked module SET must stay identical to PromptGroup.test.tsx's — a coupling enforced only by a comment, so adding a stub to one file silently breaks the other. Copy drift is already visible: `PromptGroup.test.tsx:493` drops the `await` on `GlobalRegistrator.unregister()` that all four pre-existing copies and the other new file have.

CLEAN on the rest of the angle, checked explicitly:
- `isGitRepoStrict` (non-git.ts:91-93) is a 1-line wrapper over the existing `createUserSimpleGit`, not a copy of `isGitRepo`'s body.
- `useProjectGitState`'s `isError` is additive to the existing hook rather than a parallel hook.
- The created-event subscription (useAutoAdoptBackgroundSessions.ts:63-74) reuses `useWorkspaceEvent` in exactly the shape `useTerminalAgentBindings.ts:66` and `useTerminalResumeCandidate.ts:58` use.
- `eventBus.test.ts` extends the existing `makeHostServer` harness rather than a new one.
- The sweep's injected `probeIsGitRepo` follows the existing `runArchivedWorkspaceReconcile` injected-`exists` idiom.
- `selectHiddenMainsToSurface` reads state only through `getWorkspaceSidebarBucket` rather than re-deriving buckets — correct reuse.
</agent-message>

---

<agent-message from="s3-eff">
EFFICIENCY REVIEW (wasted work only, no correctness findings). 7 findings, ranked.

**1. MEDIUM — every terminal:lifecycle event refetches terminal.list**
`apps/desktop/src/renderer/routes/_authenticated/_dashboard/v2-workspace/$workspaceId/hooks/useAutoAdoptBackgroundSessions/useAutoAdoptBackgroundSessions.ts:65-74`
Subscribed invalidate-only, no eventType filter, but only `created` is needed. `command-start` / `command-end` / synthesized prompt-redraw ends broadcast per foreground command on any OSC 133 shell (`packages/host-service/src/terminal/terminal.ts:3627,3640,3654`). Cost per event: `terminal.list` is not a cheap read — renderer→host RPC → `getDaemonClient()` + `daemon.list()` IPC round-trip + DB read (`terminal.ts:1060-1100`); then `isFetching` flips true→false = 2 re-renders of `V2WorkspaceCenter` (the query is tracked in this hook, mounted there); then the adopt effect re-runs (`sessions` is a new array) doing a localStorage parse, 2 Set builds and a tab walk. So a shell running 20 commands = 40 daemon round-trips + 80 center re-renders that can adopt nothing.
Also: grep shows no other lifecycle→`terminal.list.invalidate` in the tree, so the comment's "same shape `useTerminalAgentBindings` uses" isn't load-bearing cost precedent.
Cheaper: first line of the callback `if (payload.eventType !== "created") return;`. Keeps the created→pane invariant exactly; drops ~all volume. (`exit` already has its own consumers.)

**2. MEDIUM — sweep spawns a git process per project per boot, serially, unconditionally**
`packages/host-service/src/runtime/main-workspace-sweep.ts:31-62` (probe at :53)
`isGitRepoStrict` = `checkIsRepo()` = one git spawn. `ensureMainWorkspaceStrict` then spawns git AGAIN for the git path (`symbolic-ref --short HEAD`, ensure-main-workspace.ts:93-94). Boot git spawns for this sweep went 1×N → 2×N, serial, N = every project row ever. On Windows ARM64 a git spawn is ~50-150ms, so 30 projects ≈ +2-5s. Non-blocking for server start (background IIFE, `app.ts:219`) but it is `await`ed ahead of `runArchivedWorkspaceReconcile` (`app.ts:230` then `:238`), so the crash-recovery reconcile is delayed by the whole added time.
Cheaper, invariant-preserving:
 (a) bounded concurrency (pool of 4-8) over `rows` — loop bodies are independent per project; tri-state handling unchanged.
 (b) skip the probe entirely for projects that already have a `type='main'` row whose `branch !== NON_GIT_BRANCH` — one indexed SQL read replaces N spawns. Those rows take the git path today and, if the folder stopped being a repo, `ensureMainWorkspaceStrict` throws → wrapper swallows → row untouched: identical to pre-diff behaviour, no rewrite risk, so the fail-closed guarantee survives.
Do NOT reuse the TTL-cached `isGitRepo` — the diff's comment is right that it reintroduces the ambiguity.

**3. LOW-MEDIUM — modal fires host RPCs for probes master mode will never read**
`apps/desktop/.../DashboardNewWorkspaceModal/hooks/useMasterWorkspaceTarget/useMasterWorkspaceTarget.ts:28` → `useProjectGitState`
`resolveMasterMode` rungs 1-3 return `branch` for a cloud host, a remote host, or a multi-repo project — but `useProjectGitState`'s `enabled` doesn't know that. Selecting a REMOTE host with a main workspace issues a `git.isRepo` RPC across the relay to that host; `multi-repo-info` fires against the local host for any project with no local main (incl. while cloud is selected). `staleTime: Infinity` so it's once per key, not per render, but each is a network round-trip for a value rung 2 discards.
Cheaper: add an `enabled` param to `useProjectGitState` (default true, kanban unaffected) and pass `selectedHostId === machineId && selectedHostId !== CLOUD_HOST_ID` from `useMasterWorkspaceTarget`. Predicate order and fail-closed rungs untouched.

**4. LOW — three full scans of the workspace list per modal keystroke**
`useMasterWorkspaceTarget.ts:27, 28 (inside useProjectGitState:43), 36`
`useProjectMainWorkspaceId` is an unmemoized `workspaces.find` (`kanban/hooks/useProjectMainWorkspaceId/useProjectMainWorkspaceId.ts:22`) and is called twice — directly and again inside `useProjectGitState` — plus the `workspaces.find` at :36. `PromptGroup` re-renders per keystroke (`draft.prompt` is in the `useShallow` selector, `DashboardNewWorkspaceDraftContext.tsx:60`), so that's 3×O(W) over every workspace on every host, per character. Absolute cost small; it's pure duplication.
Cheaper: find the master ROW once in a `useMemo` here, derive `mainWorkspaceId` from it, and pass the id into `useProjectGitState` (or memoize inside `useProjectMainWorkspaceId`).

**5. LOW — duplicate live query over the same two collections in the same component**
`AgentHooks/hooks/useSurfaceHiddenMainWorkspaces/useSurfaceHiddenMainWorkspaces.ts:48-73` vs `AgentHooks/hooks/useSidebarMirrorSync/useSidebarMirrorSync.ts:459-491`
Both mounted in `AgentHooks`. The mirror's `v2WorkspaceLocalState` projection is a strict SUPERSET of the reconciler's (adds projectId/sectionId/tabOrder/pinnedAt), and its `v2SidebarProjects` projection is a superset too. Two incremental views maintained and two result arrays allocated for every curation change where one pair would do.
Cheaper: lift one pair of queries into `AgentHooks` (or a shared `useSidebarCurationRows`) and pass rows to both consumers. Note the mirror gates on its own `isReady`, so this is a small refactor, not a one-liner.

**6. LOW — reconciler materialises every workspace, then filters**
`useSurfaceHiddenMainWorkspaces.ts:35-44` and `:75-93`
The memo allocates W objects (all workspaces, all hosts) though only local `main`s can be candidates (~1 per project); the effect then builds a Map over ALL local-state rows and a Set over ALL sidebar projects before the first candidate check. Frequency is fine — `workspaces` is memoized upstream with react-query structural sharing, so the 30s poll (`useHostWorkspaces.ts:24,208`) doesn't churn identity, and the live queries emit on genuine leaf changes — so this runs per real change, not per render.
Cheaper: filter `type === "main" && hostId === machineId` inside the memo (the pure selector re-checks both, so the bucket-only predicate is unweakened), and early-return before building Map/Set when that list is empty — the normal case. Polish, not a hot path.
Also note: unlike the mirror, this hook has no debounce/signature gate; today's per-run cost doesn't justify one, but if the effect ever grows past Map/Set building the mirror's gate is the precedent.

**7. TRIVIAL — dead memo**
`useAutoAdoptBackgroundSessions.ts:66-68`: the `useCallback` is pointless — `useWorkspaceEvent` already wraps the callback in `useEffectEvent` (`renderer/hooks/host-service/useWorkspaceEvent/useWorkspaceEvent.ts:64`), so identity never matters. Inline it.

CHECKED, CLEAN: `created` broadcast volume (one per session create, at `terminal.ts:3759`, after the row commit — no fan-out concern); the eventBus re-narrow (`workspace-client/src/lib/eventBus.ts:290-325`) still allocates exactly one payload per event, the added arm is one comparison; `submitDisabledReason`'s new `draft.prompt`/`visibleFiles` deps recompute per keystroke but the body is a handful of comparisons and one `trim()`; `useProjectGitState`'s new `isError` derivation adds no query and no allocation; `restoreWorkspace` + `ensureWorkspaceInSidebar` on the master submit path are two collection writes on a user action.
</agent-message>

---

<agent-message from="s3-alt">
ALTITUDE REVIEW — master-workspace diff. 8 findings, none correctness. Ordered by weight.

**1. Refusal message + its predicate live at two altitudes and disagree.**
`PromptGroup.tsx:319-322` + `:355` / `useSubmitWorkspace.ts:130-140`.
The two-layer refusal itself is FINE — the file's precedent already doubles "Select a project" / "No active host" (submitBlocker + `submitWorkspace` lines 64-81). What's misplaced is the message and its inputs, composed independently in each layer:
- label: PromptGroup `masterTarget.masterName ?? selectedProject?.name ?? "this project"`; hook `masterTarget.masterName ?? "this project"`. Same state, two different strings — with a null `masterName` the inline blocker names the project and the toast says "this project".
- predicate: PromptGroup `visibleFiles.length > 0` (host-filtered draft files); hook `attachmentIds.length > 0` (post-upload ids). Two definitions of "has attachments" for one rule.
Right altitude: `resolveMasterMode` already carries `masterName` — have the `master` variant carry a resolved non-null display label, and export one message builder both layers call. Then the hook's copy is pure defense in depth, not a second authoring of the rule.

**2. Test-only parameter widened a public entry point; the cited precedent does the opposite.**
`packages/host-service/src/runtime/main-workspace-sweep.ts:17-24`.
`runMainWorkspaceSweep(ctx, probeIsGitRepo = isGitRepoStrict)` — the doc says this is "the same idiom as `runArchivedWorkspaceReconcile`'s injected `exists`". It isn't. That file keeps `runArchivedWorkspaceReconcile(ctx)` one-arg and injects `exists` into a PURE helper, `selectStranded(archived, livePaths, exists)` (`archived-workspace-reconcile.ts:35, 81-84`), which is what the test drives. Here the seam was cut at the entry point instead, and the per-row decision (missing path → skip, throw → skip, else `ensure({nonGit})`) stayed inline with ~18 lines of comment in the loop.
Right altitude: keep the entry point one-arg; extract the per-row classification into a pure helper taking the probe, and test that. Fix the comment either way — it currently points at an idiom the codebase doesn't use here.

**3. Pure resolver depends on a React component module.**
`resolveMasterMode.ts:1` imports `CLOUD_HOST_ID` from `DevicePicker.tsx` — a 186-line component pulling `posthog-js/react`, `react-icons/hi2`, UI dropdowns and `LocalHostServiceProvider`. The whole point of the file (and its 165-line unit test) is that the decision is pure; its single import drags the UI graph in.
Compounding: rung 2's `selectedHostId === CLOUD_HOST_ID` clause is already subsumed by the `selectedHostId !== machineId` clause on the next line — the constant is imported to express something the ladder decides anyway.
Right altitude: move `CLOUD_HOST_ID` to a `DevicePicker/constants.ts` leaf (AGENTS.md co-location: constants beside the component), or drop the clause and keep the reason in the comment.

**4. FEATURES.md marker coverage stops at the renderer.**
`FEATURES.md:153` adds only `(MASTER-PLUS-LAUNCH)  apps/desktop/src/renderer`. The marker also carries the load-bearing host half — `packages/host-service/src/terminal/terminal.ts` (the `created` broadcast) and `events/types.ts` — plus `packages/workspace-client/src/lib/eventBus.ts`. None gated. Its sibling `(MASTER-ALWAYS-ACTIVE)` correctly lists renderer AND host-service (`:151-152`), and `WS_NO_BUFFER_UTIL` shows the two-row convention. A merge that drops the host broadcast survives the marker gate today and the feature dies silently.
Right altitude: add `(MASTER-PLUS-LAUNCH)  packages/host-service` and a workspace-client row.

**5. Modal hook reaches into the kanban feature's hooks.**
`useMasterWorkspaceTarget.ts:3-4` imports `useProjectGitState` and `useProjectMainWorkspaceId` from `_dashboard/kanban/hooks/`. Per AGENTS.md ("used 2+ times → promote to highest shared parent"), both belong in `_authenticated/hooks/`. `useProjectGitState` was already cross-feature before this diff (settings `MultiRepoMembersSection`), so it's pre-existing — but this diff makes it three consumers and edits the file; `useProjectMainWorkspaceId` gets its first outside consumer here. Low priority, flagging because the diff touches the file.

**6. Unrelated reflow in a merge-sensitive file.**
`V2WorkspaceView.tsx:353` — `onAddChatV3` collapsed from 3 lines to 1. Pure formatting, no relation to the feature, in a fork-marked file. Adds conflict surface for nothing. Drop it.

**7. New eventBus arm inserted at the head re-indents the whole chain.**
`packages/workspace-client/src/lib/eventBus.ts:290-327` — putting `created` first re-indented all three existing arms (~40 changed lines in an upstream package where ~10 would do). Inserting it as the last explicit arm before the `command-start` else would have left `exit` / `command-end` untouched, and the "every eventType needs its own arm" comment reads the same either way. Minor.

**8. Two small over-generalizations.**
- `useMasterWorkspaceTarget/index.ts` re-exports `MASTER_FOLDER_MISSING_REASON`, `ResolveMasterModeInput` and `resolveMasterMode`; none has a consumer outside the folder (only the hook, `MasterWorkspaceTarget` and `BRANCH_ONLY_TARGET` cross the boundary). Barrel is wider than the seam.
- `selectHiddenMainsToSurface.ts:17` exports `HiddenMainSidebarState`, referenced only inside its own file and its test.

**9. Trivial.** `lifecycleEvents.ts:154-160` — the `created` early return sits below `resolveTerminalWorkspaceId(payload.terminalId)`, so it pays for a lookup it discards. Move the guard above line 154.

CLEAN at the altitudes you asked about:
- Reconciler as an `AgentHooks` sibling: right. Placement under `AgentHooks/hooks/` matches co-location, the ordering rationale (after `usePlaceLocalWorktreesInSidebar`, before `useSidebarMirrorSync`) is stated and correct, and the pure `selectHiddenMainsToSurface` / React-wiring split is the right seam. Reading state exclusively through `getWorkspaceSidebarBucket` puts the exclusion list in the classifier that already owns it, rather than restating it.
- No rung of `resolveMasterMode` is duplicated as a UI conditional. Every branch-shaped drop in PromptGroup keys off the single `isMasterMode` boolean; nothing re-derives git-ness, host identity or worktree presence in the component. The only thing that leaks is the display label (finding 1).
- Tri-state probe: policy at the caller, primitive reports — right way round. `isGitRepoStrict` sits beside `isGitRepo` in the module that owns git-ness, and the sweep decides what "unknown" means. Only the seam for testing it is wrong (finding 2).
- Created-event consumers: `eventBus` owning the wire re-narrow, `lifecycleEvents` owning "which eventTypes are dot events", and per-hook invalidate subscriptions in `useAutoAdoptBackgroundSessions` all sit at the right layer — the last matching established precedent (`useTerminalAgentBindings.ts:66`, `useTerminalResumeCandidate.ts:58`).
- Restoring the auto-adopt mount in `V2WorkspaceCenter` rather than upstream's `page.tsx` is correct: the hook needs `store` + `isLayoutReady`, which the center owns.
- Test altitude: the `resolveMasterMode.test.ts` (which mode) vs `PromptGroup.test.tsx` (what the form does in one) split is explicit and correct, and stubbing at the data leaves rather than at `useMasterWorkspaceTarget` covers the hook's own plumbing.
- FEATURES.md prose matches enforced behaviour everywhere I checked (rung ladder, dropped UI, `restoreWorkspace` semantics, transient "Checking project…", tri-state sweep). Only the marker-path coverage is short (finding 4).
</agent-message>

---

<agent-message from="c3-renderer">
Renderer + shared-visibility cleanups applied. All 6 items done, 2 deviations noted. No commits. Host-service / workspace-client / FEATURES.md untouched.

TESTS: 323 pass / 0 fail across the affected suites (AgentHooks, DashboardNewWorkspaceModal, useDashboardSidebarState, CollectionsProvider, kanban, useAutoAdoptBackgroundSessions, V2NotificationController). Per-suite vs baseline: reconciler+resolver+sidebarMutations 63→65 (resolveMasterMode +2 cases), PromptGroup 21→21 (−1 vacuous, +1 real), useProjectGitState 5→5, useAutoAdopt 4→5. tsc: no new errors in any touched file (repo's pre-existing 38-error type debt unchanged). biome: every touched file clean when LF-normalised (on-disk CRLF is repo-wide pre-existing noise).

1. EFFICIENCY — useAutoAdoptBackgroundSessions: subscription now `if (payload.eventType !== "created") return;` first line; useCallback inlined (useWorkspaceEvent already wraps in useEffectEvent); `useCallback` import dropped. Test asserts command-start + command-end produce zero invalidates.

2. Refusal single-ownership — resolveMasterMode's master variant now carries `masterLabel: string` (non-null; masterName ?? projectName ?? "this project"), replacing `masterName`. New exported `getMasterMissingAgentRefusal({hasAgent, prompt, hasAttachments, masterLabel})` returns the message or null; PromptGroup's submitBlocker and useSubmitWorkspace both call it. Attachment predicate unified on one rule with the divergence documented in that function's doc: hook passes `attachmentIds.length > 0` (submit-relevant truth), blocker passes `visibleFiles.length > 0` (same notion, best fact a render has). PromptGroup passes `selectedProject?.name ?? null` into the hook as the fallback source.

3. useSubmitWorkspace — linkedPR / hostUrl / no-agent refusals all hoisted above the in-flight latch; latch set is now immediately before the try, and only the catch resets it. resolveMasterMode:154 is `if (!hostUrl)`, so the hook's hostUrl guard is a one-line defensive check (kept its toast — silently returning would drop user feedback). Added a resolver case asserting hostUrl "" → loading.

4. useProjectGitState — `isError` is now `(gitProbeEnabled && gitProbeErrored) || (multiRepoEnabled && multiRepoProbeErrored)`; new third param `enabled = true` gates BOTH probes. useMasterWorkspaceTarget passes `selectedHostId != null && !== CLOUD_HOST_ID && === machineId`. useMasterWorkspaceTarget finds the master row once in a useMemo (id + name + worktreeExists off it, predicate identical to useProjectMainWorkspaceId's incl. the `hostId ? … : true` fallback) and no longer calls useProjectMainWorkspaceId directly; useProjectMainWorkspaceId memoised internally.

5. Reconciler dedup — (a) LocalWorkspaceForPlacement gained `hostId`, shared by both selectors; LocalWorkspaceForSurfacing deleted. (b) New `isLocalMainWorkspaceInSidebarScope` in sidebarVisibility.ts (generic type predicate, narrows projectId to string) used by both isAutoIncludedLocalMainWorkspace and selectHiddenMainsToSurface — exact complements, row-presence inverts at the call sites. (c) Hook memo filters type==="main" && hostId===machineId and the effect early-returns on empty before any work; selector predicate unweakened. (d) Selector takes flat arrays (localStateRows, sidebarProjectRows) and builds the Map/Set itself; SidebarRowForSurfacing wrapper + hook-side scaffolding gone; new `HiddenMainSidebarRow` type. All 24 hidden-main tests + 6 placement tests green after mechanical reshaping. Live queries NOT merged, per your decision.

6. Small — CLOUD_HOST_ID moved to DevicePicker/constants.ts (redundant clause + comment kept in rung 2); PromptGroup blocker uses isMasterMode; lifecycleEvents created-return now above the resolveTerminalWorkspaceId lookup; V2WorkspaceView reflow reverted (file diff is now exactly the 7 intended lines); sidebarMutations absent-code comment trimmed to one clause on the tombstone loop; PromptGroup.test lost masterWorktreeExists + masterRow and the vacuous Recycle-Bin duplicate (comment folded into the restore test), gained a real errored-probe case using the previously-dead gitProbeErrored knob, and awaits GlobalRegistrator.unregister(); resolveMasterMode.test has `MASTER_RESULT` (as const).

TWO DEVIATIONS:
- Barrel: useMasterWorkspaceTarget/index.ts exports hook + MasterWorkspaceTarget + BRANCH_ONLY_TARGET + getMasterMissingAgentRefusal. The builder has two external call sites (item 2), so a strict 3-export barrel would have forced deep imports. MASTER_FOLDER_MISSING_REASON and ResolveMasterModeInput are dropped as asked (test imports them directly).
- selectHiddenMainsToSurface still exports HiddenMainSidebarState — the condition ("if internal-only") isn't met; the test builds its state literals from it.
- Files touched beyond your list, forced by item 5a and 6a: usePlaceLocalWorktreesInSidebar.ts (+hostId in the map), selectWorktreesToPlace.test.ts (+hostId on rows), DevicePicker.tsx, NewWorkspaceScreen.tsx and useBranchContext.ts (CLOUD_HOST_ID import path only — I repointed all importers rather than leaving a re-export shim).

UNRELATED PRE-EXISTING FAILURE (not mine, do not chase): running the whole v2-workspace dir, browserRuntimeRegistry.test.ts's process-global mock of renderer/lib/trpc-client breaks useTerminalInterruptClear.test.ts ("electronTrpcClient.keyboardLayout" undefined). Reproduces with just those two untouched files; both pass alone.
</agent-message>