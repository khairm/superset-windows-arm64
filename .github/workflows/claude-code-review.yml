name: Claude Code Review

on:
  pull_request:
    types: [opened]

jobs:
  claude-review:
    permissions:
      contents: read
      pull-requests: read
      issues: read
      id-token: write
    uses: khairm/gh-claude-workflows/.github/workflows/claude-review.yml@main
    secrets: inherit
